# Deployed models

Place INT8 TFLite classifier here:

- `block_classifier_int8.tflite` — 4-class block face model (128×128 RGB input)

Train/export pipeline: see `scripts/train_classifier.md` (Phase 5 scaffold).
