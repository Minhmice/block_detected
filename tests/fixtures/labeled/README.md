# Labeled evaluation set (TEST-01)

Add frames here with optional `labels.json`:

```json
{
  "frame_001.png": { "block_id": 1, "corners": [[x,y], ...] }
}
```

Run: `python scripts/eval_offline.py tests/fixtures/labeled`
