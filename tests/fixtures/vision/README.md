# Vision fixtures

- `square_face.png` — synthetic 640×480 BGR with a centered high-contrast square face (Phase 3 integration).
- `four_pallets_reference.png` — reference diagram with 4 pallet columns (block types 01–04); used by `test_reference_four_blocks.py`.
- Add captured reference frames from `debug_frames/` here when tuning thresholds on real hardware.
