---
phase: 15-block-detection-v2-classical-cv-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/block_detection_v2/__init__.py
  - src/block_detection_v2/config.py
  - src/block_detection_v2/camera.py
  - src/block_detection_v2/preprocessing.py
  - src/block_detection_v2/edges.py
  - src/block_detection_v2/polygon.py
  - src/block_detection_v2/geometry.py
  - src/block_detection_v2/tracker.py
  - src/block_detection_v2/renderer.py
  - src/block_detection_v2/models.py
  - src/block_detection_v2/main.py
  - src/block_detection_v2/requirements.txt
autonomous: true
requirements:
  - ISO-01
  - ISO-02
  - PIP-01
  - PIP-02
  - PIP-03
  - PIP-04
  - PIP-05
  - PIP-06
  - PIP-07
  - PIP-08
  - PIP-09
  - POL-01
must_haves:
  truths:
    - "All pipeline modules exist under src/block_detection_v2 with no v1 imports"
    - "process_frame returns dict with detected, points A-F, center, front_width, right_width, yaw_deg"
    - "main loop shows overlay and exits on ESC"
    - "main.py contains TODO for future YOLO ROI"
  artifacts:
    - path: src/block_detection_v2/polygon.py
      provides: "6-point hexagon detection and ordering"
      exports: ["find_hexagon"]
    - path: src/block_detection_v2/geometry.py
      provides: "Face widths, yaw, homography block lines"
      exports: ["compute_geometry"]
    - path: src/block_detection_v2/tracker.py
      provides: "EMA stability for 6 points"
      exports: ["Tracker"]
---

<objective>
Complete and verify the isolated classical-CV hexagon pipeline per milestone v2.0 spec (D-01–D-05).

Purpose: Runnable OpenCV-only block detector independent of v1 YOLO stack.
Output: 11-file module tree passing isolation grep + synthetic detection smoke test.
</objective>

<execution_context>
@.planning/phases/15-block-detection-v2-classical-cv-module/15-CONTEXT.md
@.planning/phases/15-block-detection-v2-classical-cv-module/15-RESEARCH.md
@.planning/REQUIREMENTS.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Module scaffold and isolation audit</name>
  <files>src/block_detection_v2/*.py, src/block_detection_v2/requirements.txt</files>
  <read_first>
    - src/block_detection_v2/main.py
    - src/block_detection_v2/models.py
    - .planning/phases/15-block-detection-v2-classical-cv-module/15-CONTEXT.md
  </read_first>
  <action>
    Ensure all 11 module files exist per schema: main, config, camera, preprocessing, edges, polygon, geometry, tracker, renderer, models, requirements.txt (+ __init__.py).

    Per D-01/D-02: grep all `src/block_detection_v2/**/*.py` — zero matches for `block_detected`, `ultralytics`, `yolo`.

    `requirements.txt` lists only `opencv-python` and `numpy`.

    `models.py` defines `Point2D`, `HexagonDetection`, `GeometryResult` dataclasses.

    `config.py` holds camera index, Canny thresholds, min contour area, polygon epsilon, EMA alpha, debug flags.
  </action>
  <acceptance_criteria>
    - `rg "block_detected|ultralytics|yolo" src/block_detection_v2/` returns no matches (case-insensitive)
    - `PYTHONPATH=src .venv/bin/python -m py_compile src/block_detection_v2/*.py` exits 0
    - `requirements.txt` contains opencv and numpy only
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Pipeline wiring preprocess → polygon → geometry → tracker → render</name>
  <files>src/block_detection_v2/preprocessing.py, src/block_detection_v2/edges.py, src/block_detection_v2/polygon.py, src/block_detection_v2/geometry.py, src/block_detection_v2/tracker.py, src/block_detection_v2/renderer.py, src/block_detection_v2/main.py</files>
  <read_first>
    - src/block_detection_v2/polygon.py
    - src/block_detection_v2/geometry.py
    - src/block_detection_v2/tracker.py
    - src/block_detection_v2/renderer.py
    - .planning/phases/15-block-detection-v2-classical-cv-module/15-CONTEXT.md
  </read_first>
  <action>
    Per D-03/D-04: wire `process_frame` in main.py:

    preprocess (resize optional, gray, CLAHE, blur) → Canny/morph/Hough → `find_hexagon` with 6-point order A-F → `compute_geometry` (front A-B-E-F, right B-C-D-E, W_front, W_right, yaw, homography splits) → `Tracker.update` (EMA, jump reject, 4-frame hold) → `render` + `frame_output`.

    Polygon validation per POL-01: min area, near-convex, top/bottom rows, min face area, reject self-intersecting.

    `main.py` line comment: `# TODO: replace full-frame processing with YOLO Detection ROI in future.` (PIP-09)

    `frame_output` returns keys: detected, points, center, front_width, right_width, yaw_deg.
  </action>
  <acceptance_criteria>
    - Synthetic white-background hexagon test: `detected=True` when running process_frame inline
    - `geometry.yaw_deg` is a finite float when detected
    - `main.py` contains exact TODO string for YOLO ROI
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Entry point smoke test</name>
  <files>src/block_detection_v2/main.py, src/block_detection_v2/camera.py</files>
  <read_first>
    - src/block_detection_v2/main.py
    - src/block_detection_v2/camera.py
  </read_first>
  <action>
    Per PIP-08: `python -m block_detection_v2.main` starts without ImportError when `PYTHONPATH=src`.

    `Camera` class in camera.py: open, read, release via cv2.VideoCapture.

    Main loop: read frame, process, imshow, waitKey; ESC (27) breaks and releases capture + destroys windows.
  </action>
  <acceptance_criteria>
    - `PYTHONPATH=src .venv/bin/python -c "import block_detection_v2.main"` exits 0
    - `Camera` class has methods open, read, release
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `rg "block_detected" src/block_detection_v2/` — empty
2. Synthetic hexagon → detected=True
3. `py_compile` all modules
</verification>

## Artifacts this phase produces

- `Point2D`, `HexagonDetection`, `GeometryResult` dataclasses
- `find_hexagon`, `compute_geometry`, `Tracker`, `frame_output`, `render`
- `Camera` class
- `process_frame`, `main()` entry in `block_detection_v2.main`
