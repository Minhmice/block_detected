---
phase: 06-detection-post-processing-reject-rules-and-temporal-stabilit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_postprocess.py
  - tests/test_engine_process.py
autonomous: true
requirements: [REQ-01, REQ-04]
must_haves:
  truths:
    - DetectionPostProcessor.update_config rebuilds tracker when window/votes/IoU change
    - update_config reset() called when stability disabled
    - Engine process_frame applies postprocess when stability.enabled and reports filtered detection_count
    - All spatial reject paths (conf, area, edge, duplicate merge, temporal) covered in pytest
  artifacts:
    - path: tests/test_postprocess.py
      provides: Postprocess unit tests including update_config
      min_lines: 100
    - path: tests/test_engine_process.py
      provides: Engine + postprocess integration test
      contains: "stability.enabled"
    - path: src/block_detected/runtime/postprocess.py
      provides: DetectionPostProcessor pipeline
      exports: ["DetectionPostProcessor", "filter_min_confidence"]
  key_links:
    - from: tests/test_engine_process.py
      to: DetectionPostProcessor.process
      via: process_frame with detections filtered by min_confidence
      pattern: "detection_count"
    - from: src/block_detected/runtime/engine.py
      to: runtime/postprocess.py
      via: self._postprocess.process in process_frame
      pattern: "_postprocess\\.process"
---

<objective>
Close postprocess test gaps for update_config, tracker reset, and engine integration — retroactive Phase 6 closure.

Purpose: Core filters exist and partial tests pass; this plan ensures hot-reload and engine wiring are regression-tested.
Output: Expanded test_postprocess.py and test_engine_process.py; no Phase 9 margin/unknown scope.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-detection-post-processing-reject-rules-and-temporal-stabilit/06-CONTEXT.md
@.planning/phases/06-detection-post-processing-reject-rules-and-temporal-stabilit/06-VERIFICATION.md
@src/block_detected/runtime/postprocess.py
@src/block_detected/runtime/engine.py
@src/block_detected/runtime/config_schema.py
@tests/test_postprocess.py
@tests/test_engine_process.py

<interfaces>
From postprocess.py:
```python
class DetectionPostProcessor:
    def update_config(self, config: StabilityConfig) -> None: ...
    def reset(self) -> None: ...
    def process(self, detections, *, frame_width, frame_height) -> list[Detection]: ...
```

StabilityConfig defaults: temporal_window=5, required_stable_votes=3, duplicate_merge_iou=0.5, enabled=False

From engine.py (process_frame excerpt):
```python
filtered = self._postprocess.process(detections, frame_width=w, frame_height=h)
# status.detection_count = len(filtered)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PostProcessor update_config and reset tests</name>
  <files>tests/test_postprocess.py</files>
  <behavior>
    - update_config with changed temporal_window rebuilds tracker (votes reset — first frame after change returns [])
    - update_config disabling stability calls reset (history cleared)
    - update_config with only min_confidence change does not reset temporal history (second identical frame still accumulates votes)
    - filter functions with threshold 0 or empty input return passthrough/copy safely
  </behavior>
  <action>
    Extend tests/test_postprocess.py. Build processor with enabled=True, temporal_window=3, required_stable_votes=2; feed same detection twice to get stable output; call update_config with temporal_window=5; next process returns [] (tracker rebuilt). Test disable: enabled=True with history, update_config enabled=False then enabled=True via new config — assert reset behavior. Test min_confidence-only hot change preserves tracker state. Add edge case: filter_min_confidence([], 0.5) == [].
  </action>
  <verify>
    <automated>python -m pytest tests/test_postprocess.py -q</automated>
  </verify>
  <done>update_config and reset paths covered; existing tests still pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Engine postprocess integration test</name>
  <files>tests/test_engine_process.py</files>
  <behavior>
    - process_frame with stability.enabled=True and min_confidence=0.9 filters low-conf fake detections from status.detection_count
    - apply_hot_config changing stability.min_confidence affects subsequent process_frame counts
  </behavior>
  <action>
    Extend test_engine_process.py. Enhance _FakeDetector.predict to return FrameResult with two detections (conf 0.2 and 0.95). Configure engine with stability.enabled=True, min_confidence=0.5. Assert process_frame status.detection_count reflects postprocess (edge reject may apply on 8x8 frame — use interior box coords). Add test: apply_hot_config raises min_confidence to 0.99, next process_frame detection_count drops. Do not import ultralytics or open camera.
  </action>
  <verify>
    <automated>python -m pytest tests/test_engine_process.py tests/test_postprocess.py -q</automated>
  </verify>
  <done>Engine applies DetectionPostProcessor in process_frame loop with hot-reload</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Detector → postprocess | Untrusted bounding boxes filtered before render/metrics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | filter_min_confidence | mitigate | Unit tests assert low-score drops |
| T-06-02 | I | TemporalStabilityTracker | accept | Local CV pipeline; no external input channel |
</threat_model>

<verification>
```bash
python -m pytest tests/test_postprocess.py tests/test_engine_process.py -q
python -m pytest tests/ -q
```
</verification>

<success_criteria>
1. update_config and engine integration tests pass
2. Full pytest suite green
3. Phase 9 margin/unknown explicitly out of scope (deferred in CONTEXT)
</success_criteria>

<output>
After completion, create `.planning/phases/06-detection-post-processing-reject-rules-and-temporal-stabilit/06-01-SUMMARY.md`
</output>
