---
phase: 03-preprocess-contour-detection
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - config/vision.example.json
  - src/block_detected/preprocess.py
  - tests/test_preprocess.py
  - tests/fixtures/vision/README.md
autonomous: true
requirements:
  - GEO-01
user_setup: []

must_haves:
  truths:
    - "640×480 uint8 BGR frames preprocess to named uint8 masks without mutating the source BGR array"
    - "Adaptive threshold and Canny modes are selectable via frozen settings with validated odd block sizes"
    - "Morphology open/close runs after mask generation and returns intermediate artifacts for debug"
  artifacts:
    - path: src/block_detected/preprocess.py
      provides: "PreprocessSettings, PreprocessResult, preprocess_bgr"
      exports: ["PreprocessSettings", "PreprocessResult", "preprocess_bgr"]
    - path: config/vision.example.json
      provides: "Documented preprocess defaults (mode, blur, adaptive, canny, morphology)"
    - path: tests/test_preprocess.py
      provides: "GEO-01 automated coverage for modes, dtypes, and input validation"
  key_links:
    - from: tests/test_preprocess.py
      to: src/block_detected/preprocess.py
      via: "synthetic BGR arrays; assert mask shape (480, 640) dtype uint8"
      pattern: "preprocess_bgr"
    - from: config/vision.example.json
      to: src/block_detected/preprocess.py
      via: "field names match PreprocessSettings constructor keys"
      pattern: "adaptive_block_size|morph_kernel"
---

<objective>
Implement GEO-01 preprocessing: BGR → grayscale → Gaussian blur → adaptive threshold or Canny → morphology open/close, returning named intermediate images for tuning and debug.

Purpose: Unlock contour detection (Plan 02) with deterministic, config-driven masks; satisfy Nyquist Wave 0 gaps for preprocess tests.
Output: `preprocess.py`, `config/vision.example.json`, `tests/test_preprocess.py` green for validation tasks 03-01-01 and 03-01-02.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-preprocess-contour-detection/03-RESEARCH.md
@.planning/phases/03-preprocess-contour-detection/03-VALIDATION.md
@src/block_detected/camera.py
@src/block_detected/detection_contract.py

<interfaces>
From Phase 2 — downstream preprocess input:

```python
@dataclass(frozen=True)
class CaptureFrame:
    frame_id: str
    image_bgr: np.ndarray  # (480, 640, 3) uint8 after normalization
```

Phase 3 does NOT emit `DetectionResult` or ordered corners. Preprocess accepts raw BGR `np.ndarray` or helpers in Plan 03 accept `CaptureFrame`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" validation_task_id="03-01-01">
  <name>Task 1: PreprocessSettings, validation, and vision.example.json</name>
  <read_first>
    - `03-RESEARCH.md` → Pattern 1 (PreprocessConfig), Pitfall 2 (odd block size), Pitfall 1 (threshold polarity)
    - `03-VALIDATION.md` → Wave 0 file list
    - `pyproject.toml` — opencv/numpy already in `[dev]` from Phase 2
  </read_first>
  <files>src/block_detected/preprocess.py, config/vision.example.json, tests/fixtures/vision/README.md</files>
  <behavior>
    - `PreprocessSettings` is a frozen dataclass with `mode: Literal["adaptive_threshold", "canny"]`
    - `adaptive_block_size` must be odd and >= 3 or construction raises `ValueError`
    - `blur_kernel` and `morph_kernel` tuples must be odd positive ints
    - `config/vision.example.json` includes `preprocess` object with documented defaults (not secrets)
  </behavior>
  <action>
    Create `src/block_detected/preprocess.py` skeleton:
    - `@dataclass(frozen=True) PreprocessSettings` with fields from research: `mode`, `blur_kernel`, `adaptive_block_size`, `adaptive_c`, `adaptive_threshold_type` (`THRESH_BINARY` / `THRESH_BINARY_INV` as string or int enum), `canny_low`, `canny_high`, `morph_kernel`, `morph_open_iterations`, `morph_close_iterations`
    - `__post_init__` validation for kernels and block size
    - `@dataclass(frozen=True) PreprocessResult` with `gray`, `blurred`, `raw_mask`, `opened`, `mask` (all single-channel uint8, shape `(480, 640)` for standard input)
    - Stub `preprocess_bgr(frame_bgr, settings)` raising `NotImplementedError` until Task 2
    Add `config/vision.example.json` with top-level `_comment`, `preprocess` section mirroring settings defaults.
    Add `tests/fixtures/vision/README.md` explaining synthetic vs future captured reference frames.
    Do NOT implement contour logic or `detect_block` wiring.
  </action>
  <verify>
    <automated>python -m pytest tests/test_preprocess.py::test_preprocess_rejects_non_bgr_input -q</automated>
  </verify>
  <acceptance_criteria>
    - Settings validate invalid block sizes at construction
    - Example JSON parses and keys align with `PreprocessSettings`
  </acceptance_criteria>
  <done>Preprocess types and example config committed; input validation test scaffolded</done>
</task>

<task type="auto" tdd="true" validation_task_id="03-01-02">
  <name>Task 2: preprocess_bgr implementation and GEO-01 tests</name>
  <read_first>
    - `03-RESEARCH.md` → Pattern 2 (`preprocess_frame_bgr` chain), Anti-pattern: grayscale directly to findContours
    - OpenCV: `COLOR_BGR2GRAY`, `GaussianBlur`, `adaptiveThreshold`, `Canny`, `morphologyEx` OPEN/CLOSE
  </read_first>
  <files>src/block_detected/preprocess.py, tests/test_preprocess.py</files>
  <behavior>
    - `preprocess_bgr` copies nothing from input into outputs incorrectly — source `frame_bgr` unchanged after call
    - Rejects non-(H,W,3) uint8 BGR or wrong spatial size with clear `ValueError` (default expect 480×640)
    - Adaptive mode: mask values in {0, 255} for threshold path
    - Canny mode: uint8 edge mask; morphology still applied
    - Both modes return all `PreprocessResult` fields with shape `(480, 640)`
  </behavior>
  <action>
    Implement `preprocess_bgr(frame_bgr: np.ndarray, settings: PreprocessSettings) -> PreprocessResult`:
    - `cv.cvtColor` → gray → `GaussianBlur`
    - Branch on `settings.mode` for `adaptiveThreshold` (Gaussian C, configurable `threshold_type`) or `Canny`
    - `getStructuringElement(MORPH_RECT)` + open then close on mask
    - Enforce input `frame_bgr.shape == (480, 640, 3)` and `dtype == uint8` (match `camera.TARGET_SHAPE`)
    Implement `tests/test_preprocess.py`:
    - `test_preprocess_rejects_non_bgr_input` — wrong ndim, dtype, or shape (03-01-01)
    - `test_preprocess_adaptive_outputs_uint8_mask` — bright square on dark field; mask non-empty; source unchanged
    - `test_preprocess_canny_outputs_uint8_mask` — same size/dtype checks for canny mode
    - `test_adaptive_threshold_config_validation` — even block size raises
    - `test_preprocess_polarity_inversion_configurable` — both THRESH_BINARY and INV produce usable masks on opposing synthetic backgrounds
    Use numpy to synthesize 640×480 BGR fixtures in tests (no large binary blobs in git).
  </action>
  <verify>
    <automated>python -m pytest tests/test_preprocess.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Validation tasks 03-01-01 and 03-01-02 commands pass
    - GEO-01 chain implemented per REQUIREMENTS.md
  </acceptance_criteria>
  <done>GEO-01 preprocess module and unit tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ndarray input | Untrusted size only in tests; production frames from camera adapter |
| vision.example.json | Local config documentation, not loaded automatically in v1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | huge/non-uint8 arrays | mitigate | Reject invalid shape/dtype before OpenCV calls |
| T-03-02 | Information Disclosure | debug intermediates | accept | No disk write in Plan 01; debug writer remains Phase 2 `debug.py` |
</threat_model>

<verification>
- `python -m pytest tests/test_preprocess.py -q` passes
- `python -c "from block_detected.preprocess import preprocess_bgr, PreprocessSettings, PreprocessResult"`
</verification>

<success_criteria>
- GEO-01 satisfied: grayscale, blur, adaptive/Canny, morphology with named outputs
- Wave 0 preprocess artifacts from 03-VALIDATION.md exist
</success_criteria>

<output>
After completion, create `.planning/phases/03-preprocess-contour-detection/03-01-SUMMARY.md`
</output>
