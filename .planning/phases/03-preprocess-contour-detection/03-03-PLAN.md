---
phase: 03-preprocess-contour-detection
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/block_detected/vision.py
  - src/block_detected/__init__.py
  - tests/test_vision.py
  - tests/fixtures/vision/square_face.png
autonomous: true
requirements:
  - GEO-01
  - GEO-02
user_setup: []

must_haves:
  truths:
    - "A single frame-level helper runs preprocess → detector and preserves CaptureFrame.frame_id in its result"
    - "A committed 640×480 fixture with a visible square face yields at least one SquareCandidate"
    - "Debug overlay draws accepted quads on a BGR copy without mutating the source frame"
  artifacts:
    - path: src/block_detected/vision.py
      provides: "VisionSettings, FrameCandidates, find_square_candidates_from_frame"
      exports: ["VisionSettings", "FrameCandidates", "find_square_candidates_from_frame", "draw_candidate_overlay"]
    - path: tests/test_vision.py
      provides: "Integration test for frame helper and overlay (03-03-01)"
    - path: tests/fixtures/vision/square_face.png
      provides: "Reference-style synthetic visible-face image for success criterion 3"
  key_links:
    - from: src/block_detected/vision.py
      to: src/block_detected/camera.py
      via: "accepts CaptureFrame.image_bgr"
      pattern: "CaptureFrame"
    - from: src/block_detected/vision.py
      to: src/block_detected/preprocess.py
      via: "preprocess_bgr"
      pattern: "preprocess_bgr"
    - from: src/block_detected/vision.py
      to: src/block_detected/detector.py
      via: "find_square_candidates"
      pattern: "find_square_candidates"
---

<objective>
Integrate preprocess + detector behind a frame-level API, add debug overlay for field tuning, and satisfy Phase 3 success criterion 3 (visible square face → ≥1 candidate) using a committed fixture.

Purpose: Give Phase 4 a single call site; keep `detect_block` stub unchanged until Phase 7.
Output: `vision.py`, fixture PNG, integration tests green for 03-03-01; full `pytest -q` still passes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-preprocess-contour-detection/03-RESEARCH.md
@.planning/phases/03-preprocess-contour-detection/03-VALIDATION.md
@src/block_detected/pipeline.py
@src/block_detected/debug.py

<interfaces>
`pipeline.detect_block` remains stub — do NOT replace NO_DETECTION behavior in this plan.

```python
@dataclass(frozen=True)
class FrameCandidates:
    frame_id: str
    candidates: list[SquareCandidate]
    preprocess: PreprocessResult  # optional exposure for debug saves
```

Overlay uses `cv.drawContours` / `putText` on `frame_bgr.copy()` per research Pattern 4.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" validation_task_id="03-03-01">
  <name>Task 1: VisionSettings, frame helper, and square_face fixture</name>
  <read_first>
    - `03-RESEARCH.md` → Pattern 4 (debug overlay), Open Questions #1 (reference images)
    - `camera.TARGET_WIDTH/HEIGHT` for normalization check
    - `debug.py` — optional reuse of path confinement if saving overlay via DebugFrameWriter in test only
  </read_first>
  <files>src/block_detected/vision.py, src/block_detected/__init__.py, tests/test_vision.py, tests/fixtures/vision/square_face.png</files>
  <behavior>
    - `find_square_candidates_from_frame(capture: CaptureFrame, settings: VisionSettings) -> FrameCandidates`
    - `FrameCandidates.frame_id == capture.frame_id`
    - Fixture-backed test finds `len(candidates) >= 1`
    - Wrong-size `CaptureFrame.image_bgr` raises before OpenCV
  </behavior>
  <action>
    Add `src/block_detected/vision.py`:
    - `@dataclass(frozen=True) VisionSettings` bundling `PreprocessSettings` + `DetectorSettings` (or nested fields)
    - `find_square_candidates_from_frame(capture, settings) -> FrameCandidates`
    - Validate `capture.image_bgr` shape `(480, 640, 3)` uint8
    - Chain `preprocess_bgr` → `find_square_candidates` on `preprocess.mask`
    Generate and commit `tests/fixtures/vision/square_face.png`: 640×480 BGR with high-contrast filled square face (centered, ~120px) suitable for default thresholds in `vision.example.json`. Script in test setup or one-off generation during execute is fine; file must be small (<50KB).
    Export public symbols from `src/block_detected/__init__.py` if project convention exports phase APIs (match Phase 2 style).
    Implement `tests/test_vision.py`:
    - `test_frame_helper_preserves_frame_id_and_finds_candidate` — load fixture via `ImageSequenceFrameSource` or direct imread
    - `test_frame_helper_rejects_wrong_shape`
    Do NOT modify `detect_block` implementation.
  </action>
  <verify>
    <automated>python -m pytest tests/test_vision.py::test_frame_helper_preserves_frame_id_and_finds_candidate -q</automated>
  </verify>
  <acceptance_criteria>
    - Validation task 03-03-01 passes
    - ROADMAP Phase 3 success criterion 3 met for fixture image
  </acceptance_criteria>
  <done>Frame-level vision helper and integration test green</done>
</task>

<task type="auto" tdd="true" validation_task_id="03-03-02">
  <name>Task 2: draw_candidate_overlay and full phase test gate</name>
  <read_first>
    - `03-RESEARCH.md` → Pattern 4
    - `03-VALIDATION.md` → full suite command before verify-work
  </read_first>
  <files>src/block_detected/vision.py, tests/test_vision.py</files>
  <behavior>
    - Overlay returns new array; input `frame_bgr` unchanged (assert `np.shares_memory` is False or byte-compare before/after)
    - Draws green polyline for each candidate `approx_xy`
    - Labels show area/aspect near bbox origin
  </behavior>
  <action>
    Implement `draw_candidate_overlay(frame_bgr, candidates) -> np.ndarray` in `vision.py` (or `debug_overlay.py` if preferred — keep single export path from `vision.py`).
    Add `test_overlay_does_not_mutate_source` and `test_overlay_draws_when_candidates_present`.
    Run full suite: `python -m pytest -q` — all Phase 1–3 tests green.
    Update ROADMAP Phase 3 plan checkboxes in SUMMARY only (executor updates ROADMAP after phase complete per GSD).
  </action>
  <verify>
    <automated>python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Full pytest suite passes
    - Overlay helper suitable for optional `DebugFrameWriter` use in Phase 8
  </acceptance_criteria>
  <done>Phase 3 integration and debug overlay complete</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|------------|------------|
| T-03-05 | Information Disclosure | overlay saved to disk | mitigate | Tests use in-memory arrays; any disk write must go through existing `DebugFrameWriter` path rules from Phase 2 |
</threat_model>

<verification>
- `python -m pytest tests/test_preprocess.py tests/test_detector.py tests/test_vision.py -q`
- `python -m pytest -q`
</verification>

<success_criteria>
- Phase 3 ROADMAP success criteria 1–3 TRUE on dev host with `.[dev]` install
- `detect_block` still returns contract stub results for ordinary frames
- Ready for Phase 4 corner ordering on `SquareCandidate.approx_xy`
</success_criteria>

<output>
After completion, create `.planning/phases/03-preprocess-contour-detection/03-03-SUMMARY.md`
</output>
