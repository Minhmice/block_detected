---
phase: 03-preprocess-contour-detection
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - config/vision.example.json
  - src/block_detected/detector.py
  - tests/test_detector.py
autonomous: true
requirements:
  - GEO-02
user_setup: []

must_haves:
  truths:
    - "Binary masks yield ranked square-face candidates with 4 vertices, convex, area and aspect within config"
    - "Empty or noise-only masks return an empty list without fabricated geometry"
    - "Rotated squares pass aspect filter via minAreaRect, not axis-aligned bbox alone"
  artifacts:
    - path: src/block_detected/detector.py
      provides: "DetectorSettings, SquareCandidate, find_square_candidates"
      exports: ["DetectorSettings", "SquareCandidate", "find_square_candidates"]
    - path: tests/test_detector.py
      provides: "GEO-02 filtering, ranking, and edge-case coverage"
  key_links:
    - from: tests/test_detector.py
      to: src/block_detected/detector.py
      via: "synthetic uint8 masks with drawn rectangles/quads"
      pattern: "find_square_candidates"
    - from: src/block_detected/detector.py
      to: src/block_detected/preprocess.py
      via: "optional integration tests chain preprocess → detector in Plan 03"
      pattern: "find_square_candidates"
---

<objective>
Implement GEO-02 contour detection: `findContours` + `approxPolyDP` filtering for convex 4-vertex quads within area min/max and ~1:1 aspect bounds, returning `SquareCandidate` records (unordered corners — Phase 4 orders TL/TR/BR/BL).

Purpose: Produce ranked candidates for geometry phase; do not build `DetectionResult` or wire `detect_block` yet.
Output: `detector.py`, extended `vision.example.json` detector section, `tests/test_detector.py` green for 03-02-01 and 03-02-02.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-preprocess-contour-detection/03-RESEARCH.md
@.planning/phases/03-preprocess-contour-detection/03-VALIDATION.md
@src/block_detected/preprocess.py

<interfaces>
Internal candidate contract (Phase 3 only — not `DetectionResult`):

```python
@dataclass(frozen=True)
class SquareCandidate:
    approx_xy: np.ndarray       # shape (4, 2) float — unordered quad vertices
    area_px: float
    aspect: float               # minAreaRect long/short ratio
    bbox_xywh: tuple[int, int, int, int]
    score: float                # default sort key, e.g. area_px
```

Do NOT assign TL/TR/BR/BL. Do NOT call `make_success_result` or populate `CornersPx`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" validation_task_id="03-02-01">
  <name>Task 1: DetectorSettings, SquareCandidate, find_square_candidates</name>
  <read_first>
    - `03-RESEARCH.md` → Pattern 3, Pitfall 5 (axis-aligned aspect), Don't Hand-Roll table
    - `RETR_EXTERNAL`, `CHAIN_APPROX_SIMPLE`, `approxPolyDP`, `isContourConvex`, `minAreaRect`
  </read_first>
  <files>src/block_detected/detector.py, config/vision.example.json</files>
  <behavior>
    - Mask must be 2D uint8; wrong shape raises `ValueError`
    - Returns `list[SquareCandidate]` sorted by `score` descending (area default)
    - Filters: area bounds, exactly 4 vertices after approx, convex, aspect from `minAreaRect`
    - Rejects rectangles with extreme aspect, tiny noise blobs, concave polys, >4 vertices
  </behavior>
  <action>
    Implement `src/block_detected/detector.py`:
    - `@dataclass(frozen=True) DetectorSettings`: `min_area_px`, `max_area_px`, `aspect_min`, `aspect_max`, `approx_epsilon_ratio`
    - `@dataclass(frozen=True) SquareCandidate` as in interfaces (use numpy arrays for `approx_xy`)
    - `find_square_candidates(mask: np.ndarray, settings: DetectorSettings) -> list[SquareCandidate]`
    - Use `cv.findContours(mask, RETR_EXTERNAL, CHAIN_APPROX_SIMPLE)`; skip contours failing filters silently (no exceptions per contour)
    - Optional: attach `rejection_reason` only in debug/test hooks if needed — keep production return list to accepted only
    Extend `config/vision.example.json` with `detector` section matching `DetectorSettings` defaults (broad area range per research A2).
    Do NOT import `detection_contract` corner types for ordering.
  </action>
  <verify>
    <automated>python -m pytest tests/test_detector.py::test_square_candidate_filters_area_aspect_and_convexity -q</automated>
  </verify>
  <acceptance_criteria>
    - Module exports settings, candidate type, and finder function
    - Example JSON documents detector thresholds
  </acceptance_criteria>
  <done>Detector core implemented; primary filter test scaffolded</done>
</task>

<task type="auto" tdd="true" validation_task_id="03-02-02">
  <name>Task 2: GEO-02 test suite — synthetic masks and edge cases</name>
  <read_first>
    - `03-VALIDATION.md` per-task commands
    - `03-RESEARCH.md` → Pitfall 3 (Canny broken edges) — test empty mask path
  </read_first>
  <files>tests/test_detector.py</files>
  <behavior>
    - Filled white square on black mask → at least one candidate
    - Rotated square (45°) → accepted when aspect within bounds
    - Wide rectangle → rejected (zero or no valid candidate)
    - Tiny 3×3 noise → rejected
    - All-black mask → `[]`
    - Random salt noise only → `[]`
  </behavior>
  <action>
    Implement `tests/test_detector.py`:
    - `test_square_candidate_filters_area_aspect_and_convexity` (03-02-01) — square vs rectangle vs concave (draw with cv2)
    - `test_empty_mask_returns_no_candidates` (03-02-02)
    - `test_rotated_square_accepted_with_min_area_rect_aspect`
    - `test_candidates_sorted_by_area_descending` when two valid quads present
    - `test_synthetic_visible_square_yields_candidate` — draw 120×120 white quad centered on 480×640 mask
    Helper: `_make_mask_with_quad(...)` in test file or conftest to avoid duplication.
    Chain smoke: one test calling `preprocess_bgr` then `find_square_candidates` on synthetic BGR (validates mask→contour without Plan 03 helper).
  </action>
  <verify>
    <automated>python -m pytest tests/test_detector.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Validation tasks 03-02-01 and 03-02-02 pass
    - GEO-02 requirements covered
  </acceptance_criteria>
  <done>GEO-02 detector tests green</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|------------|------------|
| T-03-03 | Tampering | mask array | mitigate | Validate 2D uint8 before findContours |
| T-03-04 | Elevation | fabricated geometry | mitigate | Empty list on no valid contours; never synthesize default quad |
</threat_model>

<verification>
- `python -m pytest tests/test_detector.py -q` passes
- `python -m pytest tests/test_preprocess.py tests/test_detector.py -q` passes (combined quick run from VALIDATION.md)
</verification>

<success_criteria>
- GEO-02 satisfied: 4-vertex convex quads within area/aspect configuration
- Candidates are internal types only; no `DetectionResult` geometry
</success_criteria>

<output>
After completion, create `.planning/phases/03-preprocess-contour-detection/03-02-SUMMARY.md`
</output>
