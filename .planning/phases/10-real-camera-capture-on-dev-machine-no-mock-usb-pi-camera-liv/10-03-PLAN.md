---
phase: 10-real-camera-capture-on-dev-machine-no-mock-usb-pi-camera-liv
plan: 03
type: execute
wave: 2
depends_on:
  - 10-02-PLAN.md
files_modified:
  - tests/test_api_health.py
  - tests/test_api_detection.py
  - backend/app/services/detection_loop.py
  - frontend/components/TopStatusBar.tsx
autonomous: true
requirements:
  - CAM-10-03
must_haves:
  truths:
    - "Backend does not auto-start detection loop when MOCK_CAMERA is false"
    - "POST /api/detection/start opens mocked USB source and sets detectionRunning true"
    - "Console shows LIVE_CAMERA hint when not in mock mode"
  artifacts:
    - path: tests/test_api_detection.py
      provides: "Detection start integration tests"
      contains: "test_start_with_mocked_usb_source"
    - path: tests/test_api_health.py
      provides: "Idle loop when real camera mode"
      contains: "test_loop_idle_when_not_mock"
  key_links:
    - from: POST /api/detection/start
      to: DetectionLoopService._run_loop
      via: "create_frame_source_from_env when mock off"
      pattern: "create_frame_source_from_env"
---

<objective>
Wire and verify Phase 9 console path for real camera: idle boot, explicit START, non-blocking camera open.

Purpose: CAM-10-03 — MJPEG + WebSocket after operator starts detection with mock off.
Output: API integration tests, optional loop start in thread, frontend LIVE_CAMERA badge.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/app/main.py
@backend/app/services/detection_loop.py
@frontend/components/TopStatusBar.tsx
@.planning/phases/10-real-camera-capture-on-dev-machine-no-mock-usb-pi-camera-liv/10-RESEARCH.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Camera opened on boot without operator consent | low | test_loop_idle_when_not_mock guards lifespan behavior |
</threat_model>

<tasks>

<task type="auto" validation_task_id="10-03-01">
  <name>Task 1: Health test — loop idle when not mock</name>
  <read_first>
    - `backend/app/main.py` → lifespan mock-only auto-start
    - `tests/test_api_health.py` → TestClient pattern with `with client:`
  </read_first>
  <files>tests/test_api_health.py</files>
  <action>
    Add `test_loop_idle_when_not_mock(monkeypatch)`:
    - monkeypatch.setenv("MOCK_CAMERA", "false")
    - monkeypatch.setenv("DETECTION_MODE", "live")
    - monkeypatch.setenv("CAMERA_CONFIG", "config/camera.usb.mac.json")
    - Import app fresh or reload; use TestClient with lifespan
    - GET `/health` → assert `detectionRunning` is False and `mockCamera` is False
  </action>
  <acceptance_criteria>
    - `grep -q 'test_loop_idle_when_not_mock' tests/test_api_health.py` succeeds
    - `PYTHONPATH=backend:src pytest tests/test_api_health.py::test_loop_idle_when_not_mock -x` passes
    - `PYTHONPATH=backend:src pytest tests/test_api_health.py::test_health_returns_200 -x` still passes
  </acceptance_criteria>
  <automated>PYTHONPATH=backend:src pytest tests/test_api_health.py -q</automated>
</task>

<task type="auto" validation_task_id="10-03-02">
  <name>Task 2: Detection start test with mocked USB source</name>
  <read_first>
    - `backend/app/services/detection_loop.py` → _run_loop create_frame_source_from_env
    - `tests/test_api_ws.py` → lifespan + start pattern
  </read_first>
  <files>tests/test_api_detection.py, backend/app/services/detection_loop.py</files>
  <action>
    1. Create `tests/test_api_detection.py` with `test_start_with_mocked_usb_source(monkeypatch)`:
       - Set MOCK_CAMERA=false, CAMERA_CONFIG=config/camera.usb.mac.json
       - Mock `app.services.detection_loop.create_frame_source_from_env` to return a fake source:
         - `start()` no-op, `read()` returns object with `image_bgr` numpy zeros (480,640,3), `stop()` no-op
       - TestClient POST `/api/detection/start` → 200, `started: true`
       - GET `/health` → `detectionRunning` True
       - POST `/api/detection/stop` → cleanup
    2. In `detection_loop._run_loop`, wrap blocking `self._frame_source.start()` in `await asyncio.to_thread(self._frame_source.start)` so real camera warmup does not block event loop (replace direct call on line ~128).
  </action>
  <acceptance_criteria>
    - `test -f tests/test_api_detection.py` succeeds
    - `grep -q 'asyncio.to_thread(self._frame_source.start)' backend/app/services/detection_loop.py` succeeds
    - `PYTHONPATH=backend:src pytest tests/test_api_detection.py::test_start_with_mocked_usb_source -x` passes
  </acceptance_criteria>
  <automated>PYTHONPATH=backend:src pytest tests/test_api_detection.py tests/test_api_health.py -q</automated>
</task>

<task type="auto">
  <name>Task 3: Frontend LIVE_CAMERA badge when not mock</name>
  <read_first>
    - `frontend/components/TopStatusBar.tsx` → MOCK_MODE badge block lines 89-93
    - `frontend/stores/useVisionStore.ts` → mockCamera state
  </read_first>
  <files>frontend/components/TopStatusBar.tsx</files>
  <action>
    In TopStatusBar, after MOCK_MODE badge block, add when `!mockCamera && !cameraRunning`:
    ```tsx
    {!mockCamera && !cameraRunning && (
      <span className="label-caps rounded border border-secondary/40 px-2 py-0.5 text-secondary">
        LIVE_CAMERA — RUN_DETECTION
      </span>
    )}
    ```
    When `!mockCamera && cameraRunning`, change LIVE span text from "LIVE" to "LIVE_CAMERA" (keep existing green indicator).
  </action>
  <acceptance_criteria>
    - `grep -q 'LIVE_CAMERA' frontend/components/TopStatusBar.tsx` succeeds
    - `grep -q 'RUN_DETECTION' frontend/components/TopStatusBar.tsx` succeeds
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `PYTHONPATH=backend:src pytest tests/test_api_detection.py tests/test_api_health.py -q` green
- `cd frontend && npx tsc --noEmit` green
</verification>
