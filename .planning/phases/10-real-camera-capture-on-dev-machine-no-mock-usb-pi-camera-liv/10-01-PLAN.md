---
phase: 10-real-camera-capture-on-dev-machine-no-mock-usb-pi-camera-liv
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/block_detected/camera.py
  - tests/test_camera_source.py
autonomous: true
requirements:
  - CAM-10-01
  - CAM-10-02
must_haves:
  truths:
    - "UsbVideoCaptureFrameSource no longer hardcodes cv2.CAP_V4L2"
    - "darwin auto-selects CAP_AVFOUNDATION; linux auto-selects CAP_V4L2"
    - "Unknown cv_backend in config raises ValueError before VideoCapture"
  artifacts:
    - path: src/block_detected/camera.py
      provides: "Platform-aware _select_cv_backend and cv_backend field"
      contains: "_select_cv_backend"
    - path: tests/test_camera_source.py
      provides: "Backend selector unit tests"
      contains: "test_backend_selector_darwin"
  key_links:
    - from: UsbVideoCaptureFrameSource.start
      to: _select_cv_backend
      via: "backend_api = _select_cv_backend(self._settings.cv_backend)"
      pattern: "_select_cv_backend"
---

<objective>
Fix macOS USB capture by replacing hardcoded `cv2.CAP_V4L2` with platform-aware backend selection.

Purpose: Root cause of Phase 10 — real camera fails on dev Mac because V4L2 is Linux-only.
Output: `_select_cv_backend()`, `CameraSettings.cv_backend`, updated `load_camera_settings()`, 4 new pytest functions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-real-camera-capture-on-dev-machine-no-mock-usb-pi-camera-liv/10-RESEARCH.md
@.planning/phases/10-real-camera-capture-on-dev-machine-no-mock-usb-pi-camera-liv/10-VALIDATION.md
@src/block_detected/camera.py
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Malformed cv_backend in JSON | low | `_select_cv_backend` raises ValueError on unknown key |
| Arbitrary CAMERA_CONFIG path | low | Existing dev-tool behavior; no change |
</threat_model>

<tasks>

<task type="auto" tdd="true" validation_task_id="10-01-01">
  <name>Task 1: Add _select_cv_backend and CameraSettings.cv_backend</name>
  <read_first>
    - `src/block_detected/camera.py` → current UsbVideoCaptureFrameSource.start() lines 168-177
    - `.planning/phases/10-real-camera-capture-on-dev-machine-no-mock-usb-pi-camera-liv/10-RESEARCH.md` → Pattern 1 backend map
  </read_first>
  <files>src/block_detected/camera.py</files>
  <action>
    1. Add `import sys` near top of `camera.py` if not present.
    2. After imports, add module-level `_CV_BACKEND_MAP` and public `_select_cv_backend(cv_backend: str) -> int`:
       - Keys: `"v4l2"` → `cv2.CAP_V4L2`, `"avfoundation"` → `cv2.CAP_AVFOUNDATION`, `"dshow"` → `getattr(cv2, "CAP_DSHOW", 700)`, `"any"` → `cv2.CAP_ANY`
       - When `cv_backend == "auto"`: `sys.platform == "darwin"` → `CAP_AVFOUNDATION`; `sys.platform.startswith("linux")` → `CAP_V4L2`; else → `CAP_ANY`
       - Unknown explicit value → `ValueError(f"unknown cv_backend: {cv_backend!r}")`
    3. Add field to `CameraSettings`: `cv_backend: str = "auto"`
    4. In `load_camera_settings()`, add `cv_backend=str(merged.get("cv_backend", "auto"))` to `CameraSettings(...)` constructor call.
    5. In `UsbVideoCaptureFrameSource.start()`, replace both `cv2.CAP_V4L2` with `backend_api = _select_cv_backend(self._settings.cv_backend)` passed as second arg to `cv2.VideoCapture`.
    6. Extend RuntimeError message to include `backend={self._settings.cv_backend!r}`.
  </action>
  <acceptance_criteria>
    - `grep -q '_select_cv_backend' src/block_detected/camera.py` succeeds
    - `grep -q 'cv_backend: str = "auto"' src/block_detected/camera.py` succeeds
    - `grep -c 'CAP_V4L2' src/block_detected/camera.py` returns 1 (only inside `_CV_BACKEND_MAP`, not in start())
    - `grep -q 'cv_backend=str(merged.get' src/block_detected/camera.py` succeeds
  </acceptance_criteria>
  <automated>PYTHONPATH=backend:src pytest tests/test_camera_source.py::test_backend_selector_darwin tests/test_camera_source.py::test_backend_selector_linux tests/test_camera_source.py::test_backend_selector_explicit_override tests/test_camera_source.py::test_backend_selector_unknown_raises -x</automated>
</task>

<task type="auto" tdd="true" validation_task_id="10-01-03">
  <name>Task 2: Unit tests for backend selector and mocked USB capture</name>
  <read_first>
    - `tests/test_camera_source.py` → existing `test_backend_open_failure_is_explicit` mock pattern
    - `src/block_detected/camera.py` → `_select_cv_backend` signature after Task 1
  </read_first>
  <files>tests/test_camera_source.py</files>
  <action>
    Add imports: `import cv2`, `import sys` if needed; import `_select_cv_backend` from `block_detected.camera`.

    Add tests:
    - `test_backend_selector_darwin(monkeypatch)`: monkeypatch `sys.platform` to `"darwin"`, assert `_select_cv_backend("auto") == cv2.CAP_AVFOUNDATION`
    - `test_backend_selector_linux(monkeypatch)`: monkeypatch `sys.platform` to `"linux"`, assert `_select_cv_backend("auto") == cv2.CAP_V4L2`
    - `test_backend_selector_explicit_override(monkeypatch)`: monkeypatch platform `"linux"`, assert `_select_cv_backend("avfoundation") == cv2.CAP_AVFOUNDATION`
    - `test_backend_selector_unknown_raises`: `pytest.raises(ValueError, match="unknown cv_backend")` for `_select_cv_backend("bogus")`
    - `test_usb_capture_platform_backend`: mock `cv2.VideoCapture` returning opened cap with `(True, np.zeros((480,640,3)))`; start/read/stop UsbVideoCaptureFrameSource with `cv_backend="avfoundation"`; assert `frame.image_bgr.shape == (480, 640, 3)` and `frame.source == "usb-opencv"`; assert VideoCapture second positional arg is `cv2.CAP_AVFOUNDATION`
  </action>
  <acceptance_criteria>
    - `grep -q 'test_backend_selector_darwin' tests/test_camera_source.py` succeeds
    - `grep -q 'test_usb_capture_platform_backend' tests/test_camera_source.py` succeeds
    - `PYTHONPATH=backend:src pytest tests/test_camera_source.py -q` exits 0
    - `PYTHONPATH=backend:src pytest tests/test_camera_source.py::test_backend_open_failure_is_explicit -x` still passes
  </acceptance_criteria>
  <automated>PYTHONPATH=backend:src pytest tests/test_camera_source.py -q</automated>
</task>

</tasks>

<verification>
- `PYTHONPATH=backend:src pytest tests/test_camera_source.py -q` green
- `grep -c 'CAP_V4L2' src/block_detected/camera.py` is 1
</verification>
