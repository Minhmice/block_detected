---
phase: 10-real-camera-capture-on-dev-machine-no-mock-usb-pi-camera-liv
plan: 04
type: execute
wave: 3
depends_on:
  - 10-03-PLAN.md
files_modified:
  - README.md
  - scripts/camera_smoke.py
  - tests/conftest.py
  - pyproject.toml
  - .planning/phases/10-real-camera-capture-on-dev-machine-no-mock-usb-pi-camera-liv/10-UAT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - CAM-10-03
  - CAM-10-04
must_haves:
  truths:
    - "README documents Mac real-camera setup with TCC permission note"
    - "camera_smoke.py docstring references camera.usb.mac.json"
    - "Manual UAT checklist exists for live MJPEG verification"
  artifacts:
    - path: .planning/phases/10-real-camera-capture-on-dev-machine-no-mock-usb-pi-camera-liv/10-UAT.md
      provides: "Human verification steps"
      contains: "camera_smoke.py"
    - path: README.md
      provides: "Real camera dev section"
      contains: "camera.usb.mac.json"
  key_links:
    - from: README.md
      to: .env.real.example
      via: "copy env template"
      pattern: ".env.real.example"
---

<objective>
Document real-camera workflow and define human UAT for Phase 10 completion.

Purpose: Operators and developers can enable live USB capture without reading source code.
Output: README section, UAT doc, hw_camera pytest marker, ROADMAP/REQUIREMENTS traceability.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@scripts/camera_smoke.py
@.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-UAT.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| N/A | — | Documentation-only plan |
</threat_model>

<tasks>

<task type="auto" validation_task_id="10-04-01">
  <name>Task 1: README and camera_smoke documentation</name>
  <read_first>
    - `README.md` → existing Mock/Real USB table
    - `scripts/camera_smoke.py` → docstring Examples block
  </read_first>
  <files>README.md, scripts/camera_smoke.py</files>
  <action>
    1. In README.md, add subsection **Real camera on dev Mac** after mock table with:
       - Copy `.env.real.example` → `.env`
       - Grant Terminal/Cursor camera permission (System Settings → Privacy → Camera)
       - Smoke: `python scripts/camera_smoke.py --config config/camera.usb.mac.json --frames 3`
       - Console: `make dev`, INITIALIZE, RUN_DETECTION
       - Note: detection does NOT auto-start when MOCK_CAMERA=false
    2. Update `scripts/camera_smoke.py` docstring Examples to include:
       `python scripts/camera_smoke.py --config config/camera.usb.mac.json --frames 3`
  </action>
  <acceptance_criteria>
    - `grep -q 'camera.usb.mac.json' README.md` succeeds
    - `grep -q 'Privacy' README.md` succeeds
    - `grep -q 'camera.usb.mac.json' scripts/camera_smoke.py` succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: hw_camera pytest marker</name>
  <read_first>
    - `tests/conftest.py` if exists, else create
    - `pyproject.toml` → pytest ini options
  </read_first>
  <files>tests/conftest.py, pyproject.toml</files>
  <action>
    1. Add to `tests/conftest.py` (create if missing):
       - `_hw_camera_available()` using cv2.VideoCapture(0) probe with immediate release
       - `pytest.mark.hw_camera` skipif helper registered via `pytest_configure` hook adding marker description
    2. In `pyproject.toml` under `[tool.pytest.ini_options]`, add:
       `markers = ["hw_camera: requires physical camera (skipped in CI)"]`
  </action>
  <acceptance_criteria>
    - `grep -q 'hw_camera' pyproject.toml` succeeds
    - `grep -q '_hw_camera_available' tests/conftest.py` succeeds
  </acceptance_criteria>
</task>

<task type="checkpoint" validation_task_id="10-04-01">
  <name>Task 3: Human UAT — live camera on dev machine</name>
  <read_first>
    - `.planning/phases/10-real-camera-capture-on-dev-machine-no-mock-usb-pi-camera-liv/10-UAT.md` (create in this task first)
    - `.env.real.example`
  </read_first>
  <files>.planning/phases/10-real-camera-capture-on-dev-machine-no-mock-usb-pi-camera-liv/10-UAT.md</files>
  <action>
    Create `10-UAT.md` with 4 tests:
    1. **Smoke capture** — camera_smoke prints 3 frames shape [480,640,3]
    2. **Env boot** — health returns mockCamera=false, detectionRunning=false
    3. **Console start** — RUN_DETECTION → MJPEG live, FPS &gt; 0, WS telemetry
    4. **Permission recovery** — document TCC reset if isOpened false

    Update `.planning/ROADMAP.md` Phase 10 success criteria (replace TBD with 4 concrete bullets).
    Update `.planning/REQUIREMENTS.md` traceability table for CAM-10-01..04 → Phase 10.

    **Human checkpoint:** Operator runs Tests 1–3 on dev Mac with physical camera before phase sign-off.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/10-real-camera-capture-on-dev-machine-no-mock-usb-pi-camera-liv/10-UAT.md` succeeds
    - `grep -q 'CAM-10-01' .planning/REQUIREMENTS.md` succeeds
    - ROADMAP Phase 10 success criteria no longer contain "TBD"
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Full pytest suite green: `PYTHONPATH=backend:src pytest tests/ -q`
- Human UAT Tests 1–3 pass on dev Mac (checkpoint)
</verification>
