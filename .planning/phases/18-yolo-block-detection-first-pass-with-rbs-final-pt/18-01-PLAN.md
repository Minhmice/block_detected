---
phase: 18-yolo-block-detection-first-pass-with-rbs-final-pt
plan: 01
type: execute
wave: 1
depends_on:
  - 17-04
files_modified:
  - src/block_detection_v2/config.py
  - src/block_detection_v2/yolo_detector.py
  - tests/test_block_detection_v2_yolo.py
autonomous: true
requirements:
  - YOLO-01
  - YOLO-02
must_haves:
  truths:
    - "YoloBlockDetector loads models/rbs-final.pt via Ultralytics"
    - "detect() returns YoloBlockBox sorted by confidence descending"
    - "YOLO config constants live in config.py"
  artifacts:
    - path: src/block_detection_v2/yolo_detector.py
      provides: "Ultralytics YOLO wrapper"
      exports: ["YoloBlockDetector", "YoloBlockBox", "DEFAULT_MODEL_PATH"]
    - path: src/block_detection_v2/config.py
      provides: "YOLO thresholds"
      contains: "USE_YOLO_ROI"
    - path: tests/test_block_detection_v2_yolo.py
      provides: "YOLO unit tests"
  key_links:
    - from: src/block_detection_v2/yolo_detector.py
      to: models/rbs-final.pt
      via: "DEFAULT_MODEL_PATH"
      pattern: "rbs-final.pt"
---

<objective>
Finalize YOLO detector module and config constants (YOLO-01, YOLO-02). `yolo_detector.py` exists — align with `config.py`, add unit tests on `block_dataset/dt50.jpg`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/18-yolo-block-detection-first-pass-with-rbs-final-pt/18-CONTEXT.md
@.planning/phases/18-yolo-block-detection-first-pass-with-rbs-final-pt/18-RESEARCH.md
@.planning/phases/18-yolo-block-detection-first-pass-with-rbs-final-pt/18-PATTERNS.md
@src/block_detection_v2/yolo_detector.py
@src/block_detection_v2/config.py
@docs/BLOCK_DETECTION_V2.md
</context>

<artifacts_this_phase_produces>
- `config.USE_YOLO_ROI`, `YOLO_MODEL_PATH`, `YOLO_CONF`, `YOLO_IOU`, `YOLO_PAD_FRAC`, `YOLO_DEVICE`
- `YoloBlockDetector`, `YoloBlockBox` (existing, may read config defaults)
- `tests/test_block_detection_v2_yolo.py`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: YOLO config constants</name>
  <files>src/block_detection_v2/config.py</files>
  <read_first>
    - src/block_detection_v2/config.py
    - src/block_detection_v2/yolo_detector.py (DEFAULT_MODEL_PATH)
    - .planning/phases/18-yolo-block-detection-first-pass-with-rbs-final-pt/18-RESEARCH.md
  </read_first>
  <action>
    Add after WINDOW_NAME block:
    USE_YOLO_ROI = True
    YOLO_MODEL_PATH = str(_PKG_DIR.parents[1] / "models" / "rbs-final.pt")  # repo root models/
    YOLO_CONF = 0.25
    YOLO_IOU = 0.45
    YOLO_PAD_FRAC = 0.08
    YOLO_DEVICE = None
    Use Path: _REPO_ROOT = _PKG_DIR.parent.parent for clarity if preferred.
  </action>
  <acceptance_criteria>
    - config.py contains USE_YOLO_ROI = True
    - config.py contains YOLO_MODEL_PATH ending with rbs-final.pt
    - config.py contains YOLO_CONF, YOLO_IOU, YOLO_PAD_FRAC
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire yolo_detector defaults to config</name>
  <files>src/block_detection_v2/yolo_detector.py</files>
  <read_first>
    - src/block_detection_v2/yolo_detector.py
    - src/block_detection_v2/config.py
  </read_first>
  <action>
    Import config; default model_path to Path(config.YOLO_MODEL_PATH) when None.
    Default conf/iou from config.YOLO_CONF / YOLO_IOU.
    Default device from config.YOLO_DEVICE.
    Keep lazy _ensure_model(); no v1 imports.
  </action>
  <acceptance_criteria>
    - YoloBlockDetector() with no args uses config.YOLO_MODEL_PATH
    - File has zero imports from block_detected
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: YOLO unit tests</name>
  <files>tests/test_block_detection_v2_yolo.py</files>
  <read_first>
    - src/block_detection_v2/yolo_detector.py
    - tests/test_block_detection_v2_fit.py (dataset path pattern)
    - src/block_detection_v2/block_dataset/dt50.jpg
  </read_first>
  <action>
    Create tests:
    1. test_model_path_exists — Path(config.YOLO_MODEL_PATH).is_file()
    2. test_detect_dt50_returns_boxes — len(boxes) >= 1, conf > 0.5 for boxes[0]
    3. test_box_xyxy_ordering — x1 &lt; x2, y1 &lt; y2, sorted by conf desc
    4. test_crop_non_empty — detector.crop(frame, boxes[0]).size > 0
    Skip tests if model file missing (pytest.skip).
  </action>
  <acceptance_criteria>
    - pytest tests/test_block_detection_v2_yolo.py -q exits 0 when model present
    - test_detect_dt50_returns_boxes asserts len(boxes) >= 1
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `PYTHONPATH=src pytest tests/test_block_detection_v2_yolo.py -q` passes
- [ ] No `block_detected` imports in yolo_detector.py
</verification>
