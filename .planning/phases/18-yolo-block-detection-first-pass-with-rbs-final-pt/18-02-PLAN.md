---
phase: 18-yolo-block-detection-first-pass-with-rbs-final-pt
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/block_detection_v2/roi.py
  - tests/test_block_detection_v2_yolo_roi.py
autonomous: true
requirements:
  - YOLO-03
must_haves:
  truths:
    - "roi_from_bbox converts YOLO xyxy to ROIBox with rectangular mask"
    - "3-block mode applies same 22% right trim as edge-CC ROI"
  artifacts:
    - path: src/block_detection_v2/roi.py
      provides: "YOLO bbox ROI"
      exports: ["roi_from_bbox"]
    - path: tests/test_block_detection_v2_yolo_roi.py
      provides: "bbox ROI unit tests"
  key_links:
    - from: src/block_detection_v2/roi.py
      to: src/block_detection_v2/config.py
      via: "YOLO_PAD_FRAC, BLOCK_MODE, ROI_RIGHT_TRIM_FRAC"
      pattern: "YOLO_PAD_FRAC"
---

<objective>
Add `roi_from_bbox()` so YOLO detections seed classical CV ROI without edge connected-components (YOLO-03 prerequisite).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/18-yolo-block-detection-first-pass-with-rbs-final-pt/18-PATTERNS.md
@src/block_detection_v2/roi.py
@src/block_detection_v2/yolo_detector.py
</context>

<artifacts_this_phase_produces>
- `roi_from_bbox(x1, y1, x2, y2, frame_shape) -> ROIBox`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Implement roi_from_bbox</name>
  <files>src/block_detection_v2/roi.py</files>
  <read_first>
    - src/block_detection_v2/roi.py (extract_cluster_roi trim logic lines 77-80)
    - src/block_detection_v2/config.py
    - .planning/phases/18-yolo-block-detection-first-pass-with-rbs-final-pt/18-PATTERNS.md
  </read_first>
  <action>
    Add function roi_from_bbox(x1, y1, x2, y2, frame_shape, *, block_mode=None, pad_frac=None) -> ROIBox:
    - Normalize x1&lt;x2, y1&lt;y2
    - Pad by pad_frac * width/height (default config.YOLO_PAD_FRAC), clamp to frame
    - If block_mode==3: trim right edge by ROI_RIGHT_TRIM_FRAC of bbox width (same rule as extract_cluster_roi)
    - Build full-frame uint8 mask, rectangle filled 255 in bbox region
    - area = mask sum / 255 or bbox pixel count
    - Return ROIBox(x0, y0, w, h, mask, area, block_mode)
  </action>
  <acceptance_criteria>
    - roi.py exports roi_from_bbox
    - roi_from_bbox(100,100,200,200,(480,640)).mask.shape == (480, 640)
    - 3-block trim reduces x1+x+w vs untrimmed when BLOCK_MODE=3
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: roi_from_bbox tests</name>
  <files>tests/test_block_detection_v2_yolo_roi.py</files>
  <read_first>
    - src/block_detection_v2/roi.py
    - src/block_detection_v2/yolo_detector.py
    - tests/test_block_detection_v2_roi.py
  </read_first>
  <action>
    Tests:
    1. test_roi_from_bbox_shape — mask matches frame HxW
    2. test_roi_from_yolo_box — run YoloBlockDetector on dt50, roi_from_bbox from first box, mask nonzero inside bbox
    3. test_roi_trim_reduces_width — block_mode 3 has w less than raw bbox width after trim
  </action>
  <acceptance_criteria>
    - pytest tests/test_block_detection_v2_yolo_roi.py -q exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `roi_from_bbox` imported and used-ready for pipeline
- [ ] Tests pass
</verification>
