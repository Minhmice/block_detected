---
phase: 18-yolo-block-detection-first-pass-with-rbs-final-pt
plan: 03
type: execute
wave: 3
depends_on:
  - 18-02
files_modified:
  - src/block_detection_v2/pipeline.py
  - tests/test_block_detection_v2_yolo_pipeline.py
autonomous: true
requirements:
  - YOLO-03
  - YOLO-04
must_haves:
  truths:
    - "detect_raw_hexagons runs YOLO before edge-CC when USE_YOLO_ROI=True"
    - "Each YOLO box produces at most one HexagonDetection via classical fit"
    - "Empty YOLO result falls back to extract_cluster_roi path"
  artifacts:
    - path: src/block_detection_v2/pipeline.py
      provides: "YOLO-first orchestration"
      exports: ["detect_raw_hexagons", "_detect_hex_in_roi"]
  key_links:
    - from: src/block_detection_v2/pipeline.py
      to: src/block_detection_v2/yolo_detector.py
      via: "YoloBlockDetector.detect"
      pattern: "YoloBlockDetector"
    - from: src/block_detection_v2/pipeline.py
      to: src/block_detection_v2/roi.py
      via: "roi_from_bbox"
      pattern: "roi_from_bbox"
---

<objective>
Wire YOLO-first detection into `pipeline.py`: detect blocks with YOLO, classical hex fit per bbox ROI, edge-CC fallback when YOLO misses (YOLO-03, YOLO-04).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/18-yolo-block-detection-first-pass-with-rbs-final-pt/18-RESEARCH.md
@.planning/phases/18-yolo-block-detection-first-pass-with-rbs-final-pt/18-PATTERNS.md
@src/block_detection_v2/pipeline.py
@src/block_detection_v2/fit.py
@src/block_detection_v2/score.py
</context>

<artifacts_this_phase_produces>
- `_detect_hex_in_roi(color, gray, roi, edges, lines) -> tuple[HexagonDetection | None, dict]`
- `_default_yolo() -> YoloBlockDetector` lazy singleton
- `detect_raw_hexagons` YOLO branch with meta `stage: yolo_roi | edge_roi`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract _detect_hex_in_roi</name>
  <files>src/block_detection_v2/pipeline.py</files>
  <read_first>
    - src/block_detection_v2/pipeline.py (current detect_raw_hexagons body)
    - src/block_detection_v2/fit.py
    - src/block_detection_v2/score.py
  </read_first>
  <action>
    Refactor: move ROI→fit→score block into _detect_hex_in_roi(color, gray, roi) returning (Optional[HexagonDetection], meta_dict).
    Reuse _roi_lines helper. Meta includes lines, roi_lines, roi_area, score, stage ok|fit|low_score.
    Keep point coordinates in full-frame space (roi mask is full-frame sized).
  </action>
  <acceptance_criteria>
    - pipeline.py defines _detect_hex_in_roi
    - Existing edge-CC path calls _detect_hex_in_roi after extract_cluster_roi
    - detect_raw_hexagons behavior unchanged when USE_YOLO_ROI=False
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: YOLO-first branch</name>
  <files>src/block_detection_v2/pipeline.py</files>
  <read_first>
    - src/block_detection_v2/pipeline.py
    - src/block_detection_v2/yolo_detector.py
    - src/block_detection_v2/roi.py (roi_from_bbox)
    - src/block_detection_v2/config.py
  </read_first>
  <action>
    When config.USE_YOLO_ROI:
    - edges, lines = detect_edges(gray) once per frame
    - boxes = _default_yolo().detect(color)
    - If boxes: for each box up to MAX_BLOCKS, roi = roi_from_bbox(...), det, meta = _detect_hex_in_roi(...); meta["stage"]="yolo_roi", meta["yolo_conf"]=box.confidence
    - Collect all detections passing DETECTION_SCORE_MIN; return list + merged meta (first box meta + yolo_count)
    - If no boxes: fall through to extract_cluster_roi path; meta["stage"]="edge_roi"
    Lazy singleton _default_yolo reads config paths.
  </action>
  <acceptance_criteria>
    - dt50 with USE_YOLO_ROI=True yields meta stage yolo_roi
    - Mock empty YOLO list yields stage edge_roi
    - Multiple YOLO boxes can return multiple HexagonDetection when scores pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Pipeline integration tests</name>
  <files>tests/test_block_detection_v2_yolo_pipeline.py</files>
  <read_first>
    - src/block_detection_v2/pipeline.py
    - tests/test_block_detection_v2_score.py
  </read_first>
  <action>
    1. test_yolo_pipeline_dt50 — detect_raw_hexagons on dt50, assert dets and meta stage in (yolo_roi, edge_roi)
    2. test_yolo_fallback — monkeypatch _default_yolo to return detector whose detect() returns []; assert stage edge_roi
    3. test_yolo_disabled — USE_YOLO_ROI=False uses edge path only
  </action>
  <acceptance_criteria>
    - pytest tests/test_block_detection_v2_yolo_pipeline.py -q exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] YOLO-first path exercised on dt50
- [ ] Fallback path tested with mock
</verification>
