---
phase: 18-yolo-block-detection-first-pass-with-rbs-final-pt
plan: 04
type: execute
wave: 4
depends_on:
  - 18-03
files_modified:
  - src/block_detection_v2/main.py
  - src/block_detection_v2/renderer.py
  - src/block_detection_v2/benchmark.py
  - docs/BLOCK_DETECTION_V2.md
autonomous: true
requirements:
  - YOLO-04
must_haves:
  truths:
    - "main.py no longer contains YOLO TODO comment"
    - "benchmark accept_rate >= 0.80 with YOLO-first pipeline"
    - "docs describe YOLO-first flow as implemented"
  artifacts:
    - path: src/block_detection_v2/main.py
      provides: "entry without TODO"
    - path: docs/BLOCK_DETECTION_V2.md
      provides: "YOLO-first documentation"
  key_links:
    - from: src/block_detection_v2/main.py
      to: src/block_detection_v2/pipeline.py
      via: "detect_raw_hexagons"
      pattern: "detect_raw_hexagons"
---

<objective>
Remove main.py TODO, optional YOLO overlay in renderer, benchmark meta for YOLO stage, update docs, verify ≥80% accept gate (YOLO-04).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@src/block_detection_v2/main.py
@src/block_detection_v2/renderer.py
@src/block_detection_v2/benchmark.py
@docs/BLOCK_DETECTION_V2.md
@tests/test_block_detection_v2_benchmark.py
</context>

<artifacts_this_phase_produces>
- Updated main.py (TODO removed)
- benchmark.json fields: yolo_count, stage
- docs section 12 updated to implemented status
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Clean main.py</name>
  <files>src/block_detection_v2/main.py</files>
  <read_first>
    - src/block_detection_v2/main.py line 27
  </read_first>
  <action>
    Remove TODO comment about YOLO ROI. process_frame stays unchanged — pipeline owns YOLO.
    Optional one-line docstring on process_frame: "YOLO-first when USE_YOLO_ROI."
  </action>
  <acceptance_criteria>
    - main.py does not contain "TODO" and "YOLO" on same line
    - python -m block_detection_v2.main still imports cleanly
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Benchmark YOLO meta</name>
  <files>src/block_detection_v2/benchmark.py</files>
  <read_first>
    - src/block_detection_v2/benchmark.py process_image
    - src/block_detection_v2/pipeline.py meta keys
  </read_first>
  <action>
    In process_image result dict, include meta fields: stage, yolo_count (if present), yolo_conf (if present).
    run_benchmark summary: count stages yolo_roi vs edge_roi in benchmark.json.
  </action>
  <acceptance_criteria>
    - benchmark.json entries include "stage" key
    - test_benchmark_accept_rate still asserts accept_rate >= 0.80
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Renderer YOLO boxes (optional debug)</name>
  <files>src/block_detection_v2/renderer.py</files>
  <read_first>
    - src/block_detection_v2/renderer.py
    - src/block_detection_v2/config.py
  </read_first>
  <action>
    If config.DEBUG_YOLO (add DEBUG_YOLO=False default in config): draw orange rectangles for YOLO boxes passed via render(..., yolo_boxes=None).
    main.py can pass yolo boxes from a lightweight detect call OR skip if too slow — prefer reading boxes from process_frame return if pipeline exposes them in output dict.
    Minimal: add yolo_boxes optional param to render; draw cv2.rectangle when provided.
  </action>
  <acceptance_criteria>
    - renderer.render accepts optional yolo_boxes parameter without breaking existing calls
    - DEBUG_YOLO defaults False
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update docs + full test run</name>
  <files>docs/BLOCK_DETECTION_V2.md</files>
  <read_first>
    - docs/BLOCK_DETECTION_V2.md section 12
    - .planning/phases/18-yolo-block-detection-first-pass-with-rbs-final-pt/18-CONTEXT.md
  </read_first>
  <action>
    Update section 12 from "Phase 18 planned" to implemented: USE_YOLO_ROI, pipeline flow, fallback.
    Update mermaid in section "Luồng xử lý" to show YOLO branch.
    Run: PYTHONPATH=src pytest tests/test_block_detection_v2_*.py -q
  </action>
  <acceptance_criteria>
    - docs/BLOCK_DETECTION_V2.md mentions USE_YOLO_ROI and yolo_roi stage
    - Full test_block_detection_v2 suite passes including benchmark gate
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `PYTHONPATH=src pytest tests/test_block_detection_v2_*.py -q` — all pass
- [ ] benchmark accept_rate >= 0.80
- [ ] main.py TODO removed
</verification>
