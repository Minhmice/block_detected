---
phase: 07-web-telemetry-api-and-frame-streaming-for-stitch-console
plan: 03
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - src/block_detected/runtime/api/deps.py
  - src/block_detected/runtime/api/routes/telemetry.py
  - src/block_detected/runtime/api/routes/__init__.py
  - src/block_detected/apps/web/server.py
  - src/block_detected/apps/web/__main__.py
  - pyproject.toml
  - tests/test_web_api.py
autonomous: true
requirements:
  - REQ-01
  - REQ-04
user_setup:
  - service: uvicorn
    why: "ASGI server for FastAPI web entry"
    env_vars: []
    dashboard_config:
      - task: "Install web extras"
        location: "pip install -e \".[web]\" from repo root"
must_haves:
  truths:
    - "GET /api/telemetry returns fps, latency_ms, render_ms JSON for Stitch toolbar"
    - "GET /api/logs returns last N lines from get_log_lines()"
    - "pytest tests/test_web_api.py passes with TestClient and mocked EngineService (no camera)"
    - "pip install -e '.[web]' exposes block-detected-web console script"
    - "Optional: GET /ui/ serves code.html for local Stitch dev"
  artifacts:
    - path: src/block_detected/runtime/api/routes/telemetry.py
      provides: "Telemetry and log tail endpoints"
      exports: ["router"]
    - path: tests/test_web_api.py
      provides: "FastAPI TestClient coverage"
      min_lines: 80
    - path: pyproject.toml
      provides: "[web] optional deps + block-detected-web script"
      contains: "[project.optional-dependencies]"
  key_links:
    - from: src/block_detected/runtime/api/routes/telemetry.py
      to: src/block_detected/runtime/logging_setup.py
      via: "get_log_lines() for /api/logs"
      pattern: "get_log_lines"
    - from: tests/test_web_api.py
      to: src/block_detected/apps/web/server.py
      via: "TestClient(create_app()) with overridden EngineService"
      pattern: "TestClient"
---

<objective>
Complete the web API surface: telemetry JSON + log tail, optional static UI mount, packaging (`[web]` extras), entry scripts, and automated TestClient tests.

Purpose: Stitch viewport toolbar (FPS/Latency/Render) and system log panel can poll JSON; developers can run `block-detected-web` and open `code.html` locally.
Output: Full Phase 7 API testable without webcam hardware.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-web-telemetry-api-and-frame-streaming-for-stitch-console/07-CONTEXT.md
@example_ui/stitch_block_pickup_vision_console/html_data_requirements.md
@example_ui/stitch_block_pickup_vision_console/BACKEND_GAP_ANALYSIS.md
@src/block_detected/runtime/api/schemas.py
@src/block_detected/runtime/logging_setup.py
@tests/test_log_buffer.py
@tests/test_engine_process.py

<interfaces>
Telemetry contract (html_data_requirements.md §2):
- `fps`: float
- `latency_ms`: aggregate (frame_read_ms + inference_ms)
- `render_ms`: float

Log tail (§4.3): lines with timestamp + level from ring buffer formatter.

Test pattern from `test_engine_process.py`: `_FakeCap`, `_FakeDetector` for engine; for API tests mock `EngineService` entirely via dependency override.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Telemetry and log routes</name>
  <files>src/block_detected/runtime/api/routes/telemetry.py, src/block_detected/runtime/api/routes/__init__.py, src/block_detected/runtime/api/deps.py, src/block_detected/apps/web/server.py</files>
  <behavior>
    - GET /api/telemetry when service has status with frame_read_ms=4, inference_ms=8, render_ms=1.5, fps=25 → JSON latency_ms=12, render_ms=1.5, fps=25, running=true
    - GET /api/telemetry when idle (no status) → fps=0, latency_ms=0, render_ms=0, running=false
    - GET /api/logs → {"lines": [...], "count": N}; respects ?limit=10 query (default 50, max 500)
    - GET /api/logs lines come from get_log_lines() snapshot (not private handler._records)
  </behavior>
  <action>
    Implement telemetry router (per CONTEXT telemetry contract):

    - `GET /api/telemetry` → `telemetry_from_status(service.get_status(), running=service.is_running())`.
    - `GET /api/logs?limit=50` → tail = `get_log_lines()[-limit:]`; return `LogsResponse(lines=tail, count=len(tail))`.
    - Clamp limit to [1, 500].

    Wire router in `create_app()`.

    Optional static dev serve (CONTEXT discretion — implement minimal):
    - Mount `StaticFiles` at `/ui` pointing to `example_ui/stitch_block_pickup_vision_console/` (resolve path via `config.paths.PROJECT_ROOT`).
    - Only mount if directory exists; lazy-import StaticFiles.
    - Add comment in server.py: update code.html `<img src="/stream">` and fetch URLs manually (Phase 7 does not modify code.html).

    Ensure `runtime/api/deps.py` holds `get_engine_service` if not created in 07-02.
  </action>
  <verify>
    <automated>python -m pytest tests/test_web_api.py -q -k "telemetry or logs" --co -q</automated>
  </verify>
  <done>/api/telemetry and /api/logs registered; static /ui mount when example_ui dir present</done>
</task>

<task type="auto">
  <name>Task 2: pyproject web extras and entry scripts</name>
  <files>pyproject.toml, src/block_detected/apps/web/__main__.py</files>
  <action>
    Add web packaging (per CONTEXT D-API-Stack):

    - `[project.optional-dependencies] web = ["fastapi>=0.115", "uvicorn[standard]>=0.32"]`
    - Keep `dev = ["pytest>=8.0", "httpx>=0.27"]` — add httpx to dev (Starlette TestClient uses it); alternatively add httpx to `web` extras if TestClient needs it at runtime.
    - `[project.scripts] block-detected-web = "block_detected.apps.web.server:main"`
    - Create `apps/web/__main__.py` with `from block_detected.apps.web.server import main; main()` so `python -m block_detected.apps.web` works.

    Do NOT add FastAPI to core dependencies (stay optional `[web]`).
    Do NOT modify `block-detected` GUI script.
  </action>
  <verify>
    <automated>python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert 'web' in d['project']['optional-dependencies'] and 'block-detected-web' in d['project']['scripts']"</automated>
  </verify>
  <done>[web] extra and block-detected-web script declared; python -m block_detected.apps.web entry exists</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TestClient API tests</name>
  <files>tests/test_web_api.py</files>
  <behavior>
    - test_health_returns_ok: GET /health → 200, {"status":"ok"}
    - test_telemetry_json_shape: override mock service with status → GET /api/telemetry has fps, latency_ms, render_ms keys; latency_ms == read+infer
    - test_logs_tail: seed log buffer via logging.info → GET /api/logs returns lines
    - test_control_start_stop: mock service start/stop called; POST /api/start and /api/stop return ok
    - test_stream_content_type: GET /stream headers include multipart/x-mixed-replace (read first chunk only; mock jpeg bytes on service)
    - test_camera_next_requires_running: POST /api/camera/next when not running → 400/409 with ok=false
  </behavior>
  <action>
    Create `tests/test_web_api.py` using FastAPI `TestClient`:

    - pytest.importorskip("fastapi") at module level OR skip entire module if fastapi missing (mark `@pytest.mark.skipif`).
    - Fixture `mock_service`: MagicMock or lightweight stub implementing EngineService interface with controllable return values.
    - Fixture `client`: `create_app()` then `app.dependency_overrides[get_engine_service] = lambda: mock_service`.
    - Use `httpx`/`TestClient` context manager.

    Tests must NOT open real camera or load .pt weights.

    Run full test suite to ensure no regressions: `python -m pytest tests/ -q`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_web_api.py -q</automated>
  </verify>
  <done>All test_web_api tests pass; full tests/ suite still green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /api/logs | Log lines may contain paths/errors |
| Static /ui → code.html | Serves example UI from repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Information Disclosure | GET /api/logs | accept | Local dev; no auth by design for Phase 7 |
| T-07-08 | Tampering | StaticFiles /ui | accept | Read-only example_ui mount; no upload |
| T-07-09 | Spoofing | Telemetry JSON | accept | Read-only metrics; no security decisions based on FPS |
</threat_model>

<verification>
- `python -m pytest tests/test_web_api.py -q` passes
- `python -m pytest tests/ -q` passes (no regressions)
- `pip install -e ".[web]"` then `block-detected-web --help` or import check succeeds
- Telemetry latency_ms documented and tested as frame_read_ms + inference_ms
</verification>

<success_criteria>
- Stitch toolbar fields available via GET /api/telemetry
- System log panel pollable via GET /api/logs from get_log_lines()
- Web entry installable via [web] extras; tests run without hardware
</success_criteria>

<output>
After completion, create `.planning/phases/07-web-telemetry-api-and-frame-streaming-for-stitch-console/07-03-SUMMARY.md`
</output>
