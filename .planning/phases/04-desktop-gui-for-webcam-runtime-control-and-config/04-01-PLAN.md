---
phase: 04-desktop-gui-for-webcam-runtime-control-and-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_gui_controls.py
  - tests/test_gui_smoke.py
  - tests/test_gui_optional.py
autonomous: true
requirements: [REQ-01, REQ-04]
must_haves:
  truths:
    - MainWindow loads AppConfig defaults into camera, inference, and stability controls
    - Hot stability fields round-trip through _config_from_controls without touching restart-only camera/model keys while running
    - Log panel reads get_log_lines() snapshot, not LogBufferHandler internals
    - main.py and block-detected console script delegate to apps.gui.app.main
    - PySide6 import failure prints install hint and exits non-zero
  artifacts:
    - path: tests/test_gui_controls.py
      provides: Offscreen GUI control wiring tests
      min_lines: 60
    - path: src/block_detected/apps/gui/app.py
      provides: MainWindow + FrameThread
      contains: "get_log_lines"
    - path: main.py
      provides: Primary GUI entry
      contains: "apps.gui.app import main"
  key_links:
    - from: tests/test_gui_controls.py
      to: MainWindow._config_from_controls
      via: mutate spinboxes/checkboxes then assert AppConfig fields
      pattern: "_config_from_controls"
    - from: src/block_detected/apps/gui/app.py
      to: block_detected.runtime.logging_setup.get_log_lines
      via: _refresh_logs timer callback
      pattern: "get_log_lines\\(\\)"
    - from: main.py
      to: block_detected.apps.gui.app.main
      via: import and SystemExit(main())
      pattern: "apps\\.gui\\.app import main"
---

<objective>
Expand automated GUI tests proving PySide6 desktop shell delegates to WebcamEngine config types and logging API — retroactive Phase 4 closure.

Purpose: Code exists in `apps/gui/app.py`; this plan locks regression coverage for control wiring and entry points before hardening (Phase 5).
Output: `tests/test_gui_controls.py` plus extended smoke/optional tests; all pass with `QT_QPA_PLATFORM=offscreen`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-desktop-gui-for-webcam-runtime-control-and-config/04-CONTEXT.md
@.planning/ROADMAP.md
@src/block_detected/apps/gui/app.py
@main.py
@src/block_detected/runtime/logging_setup.py
@tests/test_gui_smoke.py
@tests/test_gui_optional.py
@tests/test_log_buffer.py

<interfaces>
From apps/gui/app.py (PySide6-gated):
```python
class MainWindow(QtWidgets.QMainWindow):
    def _config_from_controls(self) -> AppConfig: ...
    def _hot_config_from_controls(self) -> AppConfig: ...
    def _load_controls(self, config: AppConfig) -> None: ...
    def _refresh_logs(self) -> None: ...
    def _update_restart_hint(self) -> None: ...

def main() -> int: ...
def _print_missing_qt() -> int: ...
```

From logging_setup.py:
```python
def get_log_lines() -> list[str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GUI control round-trip tests</name>
  <files>tests/test_gui_controls.py</files>
  <behavior>
    - MainWindow(AppConfig.defaults()) has frame_thread None and start enabled (offscreen)
    - Changing camera index/width/conf/stability widgets then _config_from_controls() reflects values
    - _hot_config_from_controls() updates stability.enabled without requiring camera index change
    - _restart_widgets includes camera_index_spin, model_name_edit, log_level_combo (len ≥ 6)
  </behavior>
  <action>
    Create `tests/test_gui_controls.py`. Use `pytest.importorskip("PySide6")` and `os.environ.setdefault("QT_QPA_PLATFORM", "offscreen")`. Reuse QApplication singleton pattern from test_gui_smoke.py. Instantiate MainWindow with AppConfig.defaults(), mutate widgets, call private methods via window._config_from_controls() / _hot_config_from_controls(). Do not start FrameThread or open camera. Do not duplicate test_gui_smoke basic instantiation — extend with field round-trips.
  </action>
  <verify>
    <automated>python -m pytest tests/test_gui_controls.py tests/test_gui_smoke.py -q</automated>
  </verify>
  <done>Four behaviors asserted; tests pass when PySide6 installed, skip otherwise via importorskip</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Entry point and log panel API tests</name>
  <files>tests/test_gui_optional.py, tests/test_gui_smoke.py</files>
  <behavior>
    - main.py imports block_detected.apps.gui.app.main (importlib or ast grep in test)
    - _refresh_logs updates log_view plain text when get_log_lines returns new content (mock get_log_lines)
    - Module-level PySide6=None path: calling main() returns 1 (monkeypatch QtWidgets to None if needed, or test _print_missing_qt directly)
  </behavior>
  <action>
    Extend test_gui_optional.py: assert pyproject console script target `block_detected.apps.gui.app:main` matches import path (read pyproject.toml or import block_detected.apps.gui.app.main). In test_gui_smoke.py or test_gui_controls.py add test patching `block_detected.apps.gui.app.get_log_lines` to return ["INFO test line"], call window._refresh_logs(), assert "test line" in log_view.toPlainText(). Add test for `_print_missing_qt()` returning 1 without requiring uninstall of PySide6.
  </action>
  <verify>
    <automated>python -m pytest tests/test_gui_optional.py tests/test_gui_smoke.py tests/test_gui_controls.py -q</automated>
  </verify>
  <done>Entry delegation, log snapshot wiring, and missing-Qt exit path covered</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI → runtime config | User-edited spinboxes become AppConfig passed to WebcamEngine on Start |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I | MainWindow._config_from_controls | mitigate | validate_config() before Start (existing); tests assert invalid paths use _show_errors |
| T-04-02 | D | get_log_lines ring buffer | accept | Local desktop app; no network exposure |
</threat_model>

<verification>
```bash
python -m pytest tests/test_gui_controls.py tests/test_gui_smoke.py tests/test_gui_optional.py -q
rg "destroy_cv_windows=False" src/block_detected/apps/gui/app.py
rg "get_log_lines" src/block_detected/apps/gui/app.py
```
</verification>

<success_criteria>
1. GUI control round-trip and log panel tests pass offscreen
2. main.py and block-detected script target verified
3. No direct LogBufferHandler._records access in apps/gui/
</success_criteria>

<output>
After completion, create `.planning/phases/04-desktop-gui-for-webcam-runtime-control-and-config/04-01-SUMMARY.md`
</output>
