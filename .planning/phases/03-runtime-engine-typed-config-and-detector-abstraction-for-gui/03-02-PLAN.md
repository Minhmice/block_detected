---
phase: 03-runtime-engine-typed-config-and-detector-abstraction-for-gui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_engine_process.py
  - tests/test_detector_loader.py
  - .planning/phases/03-runtime-engine-typed-config-and-detector-abstraction-for-gui/03-VERIFICATION.md
  - .planning/STATE.md
autonomous: false
requirements: [REQ-04]
must_haves:
  truths:
    - process_frame returns ProcessedFrame with stats when mock camera and detector succeed
    - process_frame returns None on read failure or inference exception without crashing
    - load_detector returns object satisfying DetectorBackend protocol
    - engine.py imports DetectorBackend and load_detector, not ultralytics.YOLO directly
    - Phase 3 success criteria documented in 03-VERIFICATION.md with test evidence
  artifacts:
    - path: tests/test_engine_process.py
      provides: Mocked process_frame and apply_hot_config tests
      min_lines: 50
    - path: tests/test_detector_loader.py
      provides: Protocol compliance and loader wiring tests
      min_lines: 25
    - path: .planning/phases/03-runtime-engine-typed-config-and-detector-abstraction-for-gui/03-VERIFICATION.md
      provides: Phase closure checklist
    - path: src/block_detected/runtime/engine.py
      provides: WebcamEngine.process_frame loop
      contains: "def process_frame"
  key_links:
    - from: tests/test_engine_process.py
      to: WebcamEngine.process_frame
      via: mock _cap.read and fake detector.predict
      pattern: "process_frame\\(\\)"
    - from: src/block_detected/runtime/detector_loader.py
      to: YoloDetector
      via: load_detector(model_path)
      pattern: "return YoloDetector"
    - from: src/block_detected/runtime/engine.py
      to: block_detected.core.protocols.DetectorBackend
      via: type hint and load_detector call
      pattern: "DetectorBackend"
---

<objective>
Verify WebcamEngine frame loop and DetectorBackend abstraction with mocked I/O, then close Phase 3 with a verification checklist.

Purpose: Retroactive closure — prove runtime loop and detector indirection work without real camera/YOLO weights; document evidence for ROADMAP success criteria.
Output: Engine/detector tests, 03-VERIFICATION.md, STATE.md Phase 3 complete.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-runtime-engine-typed-config-and-detector-abstraction-for-gui/03-CONTEXT.md
@.planning/ROADMAP.md
@src/block_detected/runtime/engine.py
@src/block_detected/runtime/detector_loader.py
@src/block_detected/core/protocols.py
@src/block_detected/core/domain.py
@src/block_detected/detection/yolo/backend.py
@tests/test_engine.py
@tests/conftest.py

<interfaces>
From core/domain.py:
```python
@dataclass(frozen=True, slots=True)
class Detection:
    box: Box
    class_id: int
    class_name: str
    confidence: float

@dataclass(slots=True)
class FrameResult:
    detections: list[Detection]
    raw: Any = None

@dataclass(slots=True)
class InferenceStats:
    fps: float
    frame_read_ms: float
    inference_ms: float
    render_ms: float
    model_name: str
    camera_index: int
```

From core/protocols.py:
```python
@runtime_checkable
class DetectorBackend(Protocol):
    @property
    def model_name(self) -> str: ...
    def predict(self, frame: Any, *, conf: float) -> FrameResult: ...
    def close(self) -> None: ...
```

From engine.py (key methods):
```python
class WebcamEngine:
    def process_frame(self) -> ProcessedFrame | None: ...
    def apply_hot_config(self, config: AppConfig) -> None: ...
    @property
    def detector(self) -> DetectorBackend: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mocked process_frame and apply_hot_config tests</name>
  <files>tests/test_engine_process.py</files>
  <behavior>
    - With mock cap returning (True, 8x8 uint8 numpy frame) and fake detector returning empty FrameResult(detections=[], raw=None), process_frame returns ProcessedFrame with status.detection_count==0 and stats.inference_ms &gt; 0
    - With mock cap returning (False, None), process_frame returns None
    - With detector.predict raising RuntimeError, process_frame returns None
    - apply_hot_config replaces engine.config.stability.enabled without touching _cap
  </behavior>
  <action>
    Create tests/test_engine_process.py. Build _FakeCap with read() and assign to engine._cap after WebcamEngine(..., fake_detector) construction (cap starts None — set before process_frame). Use numpy zeros for frame shape (H,W,3). Fake detector must implement model_name, predict, close. For raw.plot path when stability off, either enable stability in config or provide minimal raw stub with plot() returning frame copy — prefer stability.enabled=True to avoid Ultralytics plot dependency. Reuse _FakeDetector patterns from test_engine.py.
  </action>
  <verify>
    <automated>python -m pytest tests/test_engine_process.py -q</automated>
  </verify>
  <done>Four behaviors pass; no real camera or .pt weights required</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Detector loader and layer boundary tests</name>
  <files>tests/test_detector_loader.py</files>
  <behavior>
    - _FakeDetector from test_engine satisfies isinstance(x, DetectorBackend)
    - load_detector(path) returns instance when YoloDetector is monkeypatched to return fake
    - engine.py source does not contain "from ultralytics" or "import ultralytics"
    - core/protocols.py source does not contain "cv2" or "ultralytics"
  </behavior>
  <action>
    Create tests/test_detector_loader.py. Import DetectorBackend and load_detector. Monkeypatch block_detected.runtime.detector_loader.YoloDetector to a factory returning _FakeDetector. For source checks, read Path files relative to repo root via pathlib — assert substring absence (case-sensitive). Do not load real YOLO weights in tests.
  </action>
  <verify>
    <automated>python -m pytest tests/test_detector_loader.py -q</automated>
  </verify>
  <done>Protocol and indirection verified; layer import rules enforced by test</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 3 verification closure</name>
  <files>.planning/phases/03-runtime-engine-typed-config-and-detector-abstraction-for-gui/03-VERIFICATION.md, .planning/STATE.md</files>
  <what-built>Runtime verification tests (config + engine + detector) and phase closure checklist</what-built>
  <how-to-verify>
    1. Run: `python -m pytest tests/test_config_schema.py tests/test_config_store.py tests/test_config_apply.py tests/test_engine.py tests/test_engine_create.py tests/test_engine_process.py tests/test_detector_loader.py tests/test_metrics.py -q`
    2. Confirm all tests pass (expect 25+ tests).
    3. Optionally run `python main.py` — webcam window opens, inference runs (requires camera + models/*.pt).
    4. Review `.planning/phases/03-runtime-engine-typed-config-and-detector-abstraction-for-gui/03-VERIFICATION.md` — each ROADMAP success criterion maps to a test or artifact with pass/fail.
  </how-to-verify>
  <action>
    Before checkpoint: run full pytest command above; fix any failures. Write 03-VERIFICATION.md with table: Success Criterion | Evidence (test file or path) | Status. Update STATE.md: Phase 3 complete, Phase 4 current. Mark ROADMAP progress table row for Phase 3 as complete (plans 2/2).
  </action>
  <verify>
    <automated>python -m pytest tests/test_config_schema.py tests/test_config_store.py tests/test_config_apply.py tests/test_engine.py tests/test_engine_create.py tests/test_engine_process.py tests/test_detector_loader.py tests/test_metrics.py -q</automated>
  </verify>
  <done>Human approves 03-VERIFICATION.md; STATE.md and ROADMAP show Phase 3 complete</done>
  <resume-signal>Type "approved" if verification passes, or describe failing criterion</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mock detector → engine | Test doubles cross into process_frame; no production impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | D | process_frame inference errors | mitigate | try/except returns None; test asserts no crash on predict failure |
| T-03-04 | E | load_detector arbitrary path | accept | Local dev loads user-supplied .pt from models/; out of scope for Phase 3 |
</threat_model>

<verification>
- Full Phase 3 pytest subset passes (see Task 3 command)
- 03-VERIFICATION.md lists all five ROADMAP success criteria with evidence
</verification>

<success_criteria>
- Engine loop and detector abstraction covered by automated tests without hardware
- Phase 3 marked complete in STATE.md and ROADMAP progress table after human approves checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-engine-typed-config-and-detector-abstraction-for-gui/03-02-SUMMARY.md`
</output>
