---
phase: 02-cv-layered-folder-structure-for-scalable-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/block_detected/**/*
  - pyproject.toml
  - run_yolo_webcam.py
autonomous: true
requirements: [REQ-02, REQ-03]
must_haves:
  truths:
    - Package uses layered folders apps/config/core/detection/vision/io/ui
    - python run_yolo_webcam.py imports from block_detected.apps.webcam.app
    - Expansion stubs exist for batch app and image I/O
  artifacts:
    - src/block_detected/apps/webcam/app.py
    - src/block_detected/config/paths.py
    - src/block_detected/detection/yolo/loader.py
  key_links:
    - run_yolo_webcam.py → block_detected.apps.webcam.app.main
    - pyproject.toml scripts → block_detected.apps.webcam.app:main
---

<objective>
Finalize the layered CV package layout with expansion stubs and verify entry points.

Purpose: Lock in scalable folder boundaries before tests and docs sync.
Output: Complete src/block_detected tree with apps/config/core/detection/vision/io/ui layers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cv-layered-folder-structure-for-scalable-expansion/02-RESEARCH.md
@AGENTS.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Verify layered package tree and entry points</name>
  <files>src/block_detected/, pyproject.toml, run_yolo_webcam.py</files>
  <action>Confirm all six layers exist (apps, config, core, detection, vision, io, ui). Ensure no flat legacy modules at package root (config.py, webcam_app.py, etc.). Entry: run_yolo_webcam.py and pyproject [project.scripts] point to block_detected.apps.webcam.app:main.</action>
  <verify>python3 -m py_compile run_yolo_webcam.py && find src/block_detected -name '*.py' | xargs python3 -m py_compile</verify>
  <done>All modules compile; entry points reference apps.webcam.app</done>
</task>

<task type="auto">
  <name>Add expansion stubs for batch and image I/O</name>
  <files>src/block_detected/io/images/__init__.py, src/block_detected/apps/batch/__init__.py</files>
  <action>Create io/images/ with iter_image_paths() stub listing supported extensions from a directory. Create apps/batch/ package with module docstring only (no full app yet) documenting future batch pipeline.</action>
  <verify>python3 -c "from block_detected.io.images import iter_image_paths; print(list(iter_image_paths('images'))[:1] or 'ok')"</verify>
  <done>Stubs importable; documented as future expansion points in AGENTS.md</done>
</task>

</tasks>

<verification>
- py_compile passes on entire package
- block_detected.apps.webcam.app.main importable
</verification>

<success_criteria>
- Layered structure complete with expansion stubs
- Webcam entry unchanged for users
</success_criteria>
