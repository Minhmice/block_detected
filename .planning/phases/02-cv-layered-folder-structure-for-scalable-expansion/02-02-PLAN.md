---
phase: 02-cv-layered-folder-structure-for-scalable-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/**/*
  - pyproject.toml
autonomous: true
requirements: [REQ-02, REQ-03]
must_haves:
  truths:
    - pytest runs unit tests for pure vision/core modules without YOLO
    - Tests pass in CI-local run via python -m pytest
  artifacts:
    - tests/test_geometry.py
    - tests/test_boxes.py
    - tests/test_config_paths.py
  key_links:
    - tests import from block_detected.vision and block_detected.core
---

<objective>
Add pytest foundation mirroring the layered package for geometry, box parsing, and config paths.

Purpose: Enable safe CV expansion with regression tests on pure modules.
Output: tests/ tree + optional dev dependency pytest in pyproject.toml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/block_detected/vision/geometry.py
@src/block_detected/detection/boxes.py
@src/block_detected/config/paths.py
</context>

<tasks>

<task type="auto">
  <name>Add pytest dev dependency and test package</name>
  <files>pyproject.toml, tests/conftest.py</files>
  <action>Add [project.optional-dependencies] dev = ["pytest>=8.0"]. Add tests/conftest.py inserting src/ on sys.path for editable-less runs.</action>
  <verify>python3 -m pytest --version 2>/dev/null || pip install pytest && python3 -m pytest --version</verify>
  <done>pytest available; conftest configures import path</done>
</task>

<task type="auto">
  <name>Unit tests for geometry, boxes, paths</name>
  <files>tests/test_geometry.py, tests/test_boxes.py, tests/test_config_paths.py</files>
  <action>Test point_in_rect true/false cases. Test extract_boxes with a minimal mock result object (boxes=None and one fake box). Test PROJECT_ROOT and MODELS_DIR resolve to repo root models/.</action>
  <verify>python3 -m pytest tests/ -q</verify>
  <done>All tests pass</done>
</task>

</tasks>

<verification>
- python3 -m pytest tests/ -q exits 0
</verification>

<success_criteria>
- Pure-module test coverage for three layers
</success_criteria>
