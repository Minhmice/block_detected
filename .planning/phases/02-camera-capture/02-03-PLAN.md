---
phase: 02-camera-capture
plan: 03
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - src/block_detected/debug.py
  - tests/test_debug_writer.py
  - config/camera.example.json
  - .gitignore
autonomous: true
requirements:
  - CAM-03
user_setup: []

must_haves:
  truths:
    - "Debug saves use the same monotonic frame_id as CaptureFrame for raw and optional overlay files"
    - "Debug output path cannot escape configured project output root"
    - "Retention/sampling limits prevent unbounded disk growth during long runs"
  artifacts:
    - path: src/block_detected/debug.py
      provides: "DebugFrameWriter separate from capture backends"
      exports: ["DebugFrameWriter", "DebugSettings"]
    - path: tests/test_debug_writer.py
      provides: "CAM-03 automated tests (02-02-01, 02-02-02)"
  key_links:
    - from: src/block_detected/debug.py
      to: CaptureFrame.frame_id
      via: "write_raw(frame_id, image_bgr) names files frame_000001_raw.png"
      pattern: "_raw\\.png|_overlay\\.png"
    - from: tests/test_debug_writer.py
      to: src/block_detected/debug.py
      via: "pytest tmp_path assertions"
      pattern: "DebugFrameWriter"
---

<objective>
Implement `DebugFrameWriter` as a separate debug sink (not inside capture backends) for CAM-03: monotonic frame-id filenames, optional overlay, path confinement, and retention controls.

Purpose: Enable field tuning traceability without coupling I/O latency to camera hot paths.
Output: `debug.py`, green `test_debug_writer.py`, updated example config debug section.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-camera-capture/02-02-SUMMARY.md
@.planning/phases/02-camera-capture/02-RESEARCH.md
@.planning/phases/02-camera-capture/02-VALIDATION.md
@src/block_detected/camera.py
@detection_contract.py

<interfaces>
From Plan 01/02 — capture produces ids consumed by debug writer and later `DebugInfo.frame_id`:

```python
@dataclass(frozen=True)
class CaptureFrame:
    frame_id: str  # e.g. frame_000001
    image_bgr: np.ndarray
    ...
```

Research naming: `{frame_id}_raw.png`, `{frame_id}_overlay.png` under configurable directory (default `debug_frames/`). Treat "raw" as normalized 640×480 BGR pipeline input, not sensor Bayer RAW.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" validation_task_id="02-02-01">
  <name>Task 1: DebugFrameWriter raw and overlay persistence</name>
  <read_first>
    - `02-RESEARCH.md` → Pattern 3 (debug sink outside capture), CAM-03 raw frame definition in Open Questions
    - `detection_contract.py` DebugInfo.frame_id examples
  </read_first>
  <files>src/block_detected/debug.py, tests/test_debug_writer.py</files>
  <behavior>
    - `test_debug_writer_uses_monotonic_frame_ids` PASS: two writes produce `frame_000001_raw.png`, `frame_000002_raw.png`
    - `cv2.imwrite` failure raises `OSError` with path in message
    - Overlay optional; when provided, uses same frame_id prefix with `_overlay.png`
  </behavior>
  <action>
    Create `src/block_detected/debug.py`:
    - `@dataclass DebugSettings`: `enabled: bool`, `directory: Path`, `every_n_frames: int = 1`, `max_files: int | None`, `max_bytes: int | None`, `run_id: str | None` (subdir for collision resistance per research A3)
    - `DebugFrameWriter` with `write(frame: CaptureFrame, overlay_bgr=None) -> None` and `write_raw(frame_id, image_bgr, overlay_bgr=None)`
    - When `enabled=False`, no-op return immediately
    - Implement `every_n_frames` counter; enforce `max_files` by deleting oldest matching `*_raw.png` in directory (simple glob sort) when limit exceeded
    - Use `cv2.imwrite`; check bool return
    - Do NOT import or call camera backends from debug module
    Implement `test_debug_writer_uses_monotonic_frame_ids` (02-02-01); remove skip.
  </action>
  <verify>
    <automated>python -m pytest tests/test_debug_writer.py::test_debug_writer_uses_monotonic_frame_ids -q</automated>
  </verify>
  <acceptance_criteria>
    - Validation task 02-02-01 automated command passes
    - Filenames embed exact `CaptureFrame.frame_id` string
  </acceptance_criteria>
  <done>CAM-03 core writer and monotonic filename test green</done>
</task>

<task type="auto" tdd="true" validation_task_id="02-02-02">
  <name>Task 2: Path confinement, retention guards, and config wiring</name>
  <read_first>
    - `02-RESEARCH.md` → Security Domain, Pitfall 4 (disk growth)
    - `02-VALIDATION.md` T-02-03/T-02-04 threat refs
  </read_first>
  <files>src/block_detected/debug.py, tests/test_debug_writer.py, config/camera.example.json</files>
  <behavior>
    - Reject `directory` resolving outside `allowed_root` (e.g. project `debug_frames/` parent)
    - `max_files=2` keeps at most 2 raw artifacts after third write
    - `every_n_frames=2` writes only on frames 2, 4, ...
  </behavior>
  <action>
    Extend `DebugFrameWriter` / `DebugSettings`:
    - Add `allowed_root: Path` (default: repo root or cwd); `_resolve_debug_dir()` uses `Path.resolve()` and raises `ValueError` if not under `allowed_root`
    - Add tests: path traversal attempt (`../../etc/passwd` style relative) fails closed
    - Add tests: `max_files`, `every_n_frames`, `enabled=False` no writes
    Update `config/camera.example.json` debug block with documented defaults (`enabled: false` for production loops).
    Ensure `.gitignore` contains `debug_frames/`.
    Remove all skips from `tests/test_debug_writer.py`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_debug_writer.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Validation task 02-02-02 covered by full `test_debug_writer.py` run
    - T-02-03 disk exhaustion mitigated via max_files/max_bytes
    - T-02-04 path traversal mitigated via allowed_root check
  </acceptance_criteria>
  <done>CAM-03 security/retention behavior tested; example config documents debug options</done>
</task>

<task type="auto">
  <name>Task 3: Integrate debug writer with smoke path and phase verification</name>
  <read_first>
    - `scripts/camera_smoke.py` from Plan 02
    - `02-VALIDATION.md` phase gate: full pytest + optional debug write in smoke
  </read_first>
  <files>scripts/camera_smoke.py, src/block_detected/debug.py</files>
  <behavior>
    - Smoke `--save-debug` writes one raw frame via DebugFrameWriter using capture frame_id
    - Phase quick command from VALIDATION.md passes
  </behavior>
  <action>
    Add optional `--save-debug` flag to `scripts/camera_smoke.py` that constructs `DebugFrameWriter` from config debug section and writes first captured frame.
    Export `DebugFrameWriter` from `src/block_detected/__init__.py` or document import path `block_detected.debug`.
    Add module-level docstring in `debug.py` noting linkage to `DetectionResult.debug.frame_id` for later pipeline phases.
  </action>
  <verify>
    <automated>python -m pytest tests/test_camera_source.py tests/test_debug_writer.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Nyquist quick run command from 02-VALIDATION.md passes entirely green
    - Debug writer remains independent of `camera.py` capture implementations (no circular imports)
  </acceptance_criteria>
  <done>CAM-03 complete; Phase 2 automated validation gate satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config → debug directory path | Untrusted if user-supplied path outside project |
| filesystem write | Local debug artifact storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | DebugFrameWriter | mitigate | `max_files`, `max_bytes`, `every_n_frames`; default `enabled=false` |
| T-02-04 | Tampering | debug directory path | mitigate | Resolve under `allowed_root`; reject `..` escape |
| T-02-07 | Information Disclosure | debug_frames/ | mitigate | Gitignore directory; document retention in example config |
</threat_model>

<verification>
- `python -m pytest tests/test_camera_source.py tests/test_debug_writer.py -q` — all green
- `python -m pytest -q` if other tests exist
- Optional: `python scripts/camera_smoke.py --config config/camera.example.json --save-debug` creates one file under `debug_frames/`
</verification>

<success_criteria>
- CAM-03: Raw (and optional overlay) saved with monotonic frame_id; path confined; retention configurable
- All five validation task IDs (02-01-01 through 02-02-02) have passing automated commands or documented manual Pi smoke
</success_criteria>

<output>
After completion, create `.planning/phases/02-camera-capture/02-03-SUMMARY.md`
</output>
