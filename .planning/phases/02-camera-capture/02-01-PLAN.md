---
phase: 02-camera-capture
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - config/camera.example.json
  - src/block_detected/__init__.py
  - src/block_detected/camera.py
  - tests/conftest.py
  - tests/test_camera_source.py
  - tests/test_debug_writer.py
autonomous: true
requirements:
  - CAM-01
user_setup: []

must_haves:
  truths:
    - "pytest can import `block_detected.camera` and run capture contract tests locally without hardware"
    - "Image-sequence frame source returns monotonic frame_id and 640×480 BGR arrays"
    - "camera.example.json documents CSI/USB backends, manual controls, and debug options"
  artifacts:
    - path: pyproject.toml
      provides: "Editable install + pytest/opencv/numpy dev pins"
      contains: "[project]"
    - path: src/block_detected/camera.py
      provides: "CaptureFrame, FrameSource protocol, ImageSequenceFrameSource"
      exports: ["CaptureFrame", "FrameSource", "ImageSequenceFrameSource", "CameraSettings"]
    - path: config/camera.example.json
      provides: "Example capture profile for Pi CSI and USB"
    - path: tests/test_camera_source.py
      provides: "CAM-01 fake/image-sequence automated coverage"
  key_links:
    - from: tests/test_camera_source.py
      to: src/block_detected/camera.py
      via: "import ImageSequenceFrameSource; assert shape (480, 640, 3)"
      pattern: "ImageSequenceFrameSource"
    - from: config/camera.example.json
      to: src/block_detected/camera.py
      via: "documented fields match CameraSettings keys"
      pattern: "warmup_frames|backend|debug"
---

<objective>
Establish Phase 2 test infrastructure and the capture contract layer: normalized 640×480 BGR `CaptureFrame` output via a `FrameSource` protocol and `ImageSequenceFrameSource` for offline tests.

Purpose: Satisfy Nyquist Wave 0 gaps and unlock TDD for hardware adapters without requiring Pi Camera on the dev host.
Output: `pyproject.toml`, package skeleton, `camera.py` contracts + image-sequence source, example config, pytest suites (CAM-01 green; CAM-02/03 tests scaffolded).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-camera-capture/02-RESEARCH.md
@.planning/phases/02-camera-capture/02-VALIDATION.md
@detection_contract.py

<interfaces>
From `detection_contract.py` (downstream tracing — capture must supply compatible `frame_id` strings):

```python
@dataclass(frozen=True)
class DebugInfo:
    frame_id: Optional[str] = None
    method: Optional[str] = None
    rejection_reason: Optional[str] = None
    raw_score: Optional[float] = None
```

Contract samples use ids like `frame_000123`. Phase 2 should emit `frame_000001` style ids from capture for CAM-03 alignment.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" validation_task_id="02-01-01">
  <name>Task 1: Dev dependencies and package skeleton</name>
  <read_first>
    - `.planning/phases/02-camera-capture/02-RESEARCH.md` → Standard Stack, Recommended Project Structure
    - `.planning/research/STACK.md` → Pi inference pins (opencv/numpy guidance)
    - Assume `src/block_detected/` exists or create minimal layout per Phase 1 architecture
  </read_first>
  <files>pyproject.toml, .gitignore, src/block_detected/__init__.py</files>
  <behavior>
    - `pip install -e ".[dev]"` succeeds in a fresh venv
    - `import block_detected` resolves when run from repo root with `PYTHONPATH=src` or editable install
  </behavior>
  <action>
    Add `pyproject.toml` with `[project]` name `block_detected`, Python `>=3.11`, package dir `src/`.
    Add optional `[project.optional-dependencies] dev` pinning: `pytest>=9`, `opencv-python>=4.11,<4.14`, `numpy>=2,<3` (per research; do not pin picamera2 in pip — Pi installs via apt).
    Export version in `src/block_detected/__init__.py`.
    Extend `.gitignore` with `debug_frames/`, `.venv/`, `__pycache__/`, `.pytest_cache/`.
    Do NOT add OpenCV to production-only deps yet; dev extra is sufficient for Phase 2 tests.
  </action>
  <verify>
    <automated>python -m pip install -e ".[dev]" -q && python -c "import block_detected; print(block_detected.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - Editable install works from repo root
    - Dev extra installs pytest, numpy, opencv-python without error
  </acceptance_criteria>
  <done>Package importable; dev dependencies installable in venv</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CaptureFrame, FrameSource, ImageSequenceFrameSource, CameraSettings</name>
  <read_first>
    - `02-RESEARCH.md` → Pattern 1 (FrameSource), Pattern 2 (stabilization fields only — lock logic in Plan 02)
    - `config/` must not exist yet; create `camera.example.json` not secrets file
  </read_first>
  <files>src/block_detected/camera.py, config/camera.example.json</files>
  <behavior>
    - `CaptureFrame.image_bgr` is always `np.ndarray` dtype uint8, shape `(480, 640, 3)` after normalization
    - `ImageSequenceFrameSource.read()` increments `frame_id` like `frame_000001`, `frame_000002`
    - RGB888 Pi paths are NOT implemented here; only image sequence reads files and converts to BGR
  </behavior>
  <action>
    Implement in `src/block_detected/camera.py`:
    - `@dataclass(frozen=True) CaptureFrame` with `frame_id: str`, `image_bgr: np.ndarray`, `timestamp_ns: int`, `source: str`, `metadata: Mapping[str, object]`
    - `FrameSource` `Protocol` with `start()`, `read() -> CaptureFrame`, `stop()`
    - `@dataclass CameraSettings` with fields documented in example JSON: `backend` (`image_sequence`|`picamera2`|`usb`), `width=640`, `height=480`, `warmup_frames`, `lock_exposure`, `lock_white_balance`, optional manual exposure/gain/colour_gains, `camera_index`, `device_path`, `image_dir`, `glob_pattern`
    - `ImageSequenceFrameSource`: load sorted images from directory; resize to 640×480 if needed; `cv2.cvtColor` to BGR when input is RGB; raise clear `RuntimeError` if directory empty
    - Helper `_normalize_bgr(image, width, height) -> np.ndarray` enforcing exact shape
    - Do NOT import `picamera2` at module import time
    Create `config/camera.example.json` as valid JSON with `_comment` keys per profile: `image_sequence` (local dev), `picamera2` (CSI), `usb` (VideoCapture), plus `debug` (`enabled`, `directory`, `every_n_frames`, `max_files`) for Plan 03.
  </action>
  <verify>
    <automated>python -c "from block_detected.camera import CaptureFrame, FrameSource, ImageSequenceFrameSource, CameraSettings; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `camera.py` exports CaptureFrame, FrameSource, ImageSequenceFrameSource, CameraSettings
    - `config/camera.example.json` parses with `json.load` and field names match CameraSettings
  </acceptance_criteria>
  <done>Contract types and image-sequence adapter implemented; example config committed</done>
</task>

<task type="auto" tdd="true" validation_task_id="02-01-01">
  <name>Task 3: Pytest scaffolding for CAM-01 and Wave 1 placeholders</name>
  <read_first>
    - `02-VALIDATION.md` task IDs 02-01-01, 02-01-02, 02-01-03, 02-02-01, 02-02-02
    - Create minimal test fixtures under `tests/fixtures/` (single 640×480 PNG or synthetic array saved once)
  </read_first>
  <files>tests/conftest.py, tests/test_camera_source.py, tests/test_debug_writer.py, tests/fixtures/</files>
  <behavior>
    - `test_fake_source_returns_640x480_bgr` PASS (CAM-01 / 02-01-01)
    - `test_backend_open_failure_is_explicit` and `test_manual_control_metadata_records_support` exist as `@pytest.mark.skip(reason="Plan 02")` or xfail until adapters land
    - `test_debug_writer_*` exist as skip until Plan 03
  </behavior>
  <action>
    Add `tests/conftest.py` with `fixture_image_dir` (writes one 640×480 BGR PNG via numpy/cv2).
    Implement `tests/test_camera_source.py`:
    - `test_fake_source_returns_640x480_bgr` — start/read/stop ImageSequenceFrameSource; assert shape, dtype, monotonic frame_id (02-01-01)
    - Stub tests for 02-01-02 and 02-01-03 (skipped, named exactly per VALIDATION.md)
    Add `tests/test_debug_writer.py` with skipped stubs for 02-02-01 and 02-02-02.
    Document quick command in test module docstring matching VALIDATION.md.
  </action>
  <verify>
    <automated>python -m pytest tests/test_camera_source.py::test_fake_source_returns_640x480_bgr -q</automated>
  </verify>
  <acceptance_criteria>
    - Validation task 02-01-01 automated command passes
    - Skipped tests preserve exact names required by 02-VALIDATION.md for later plans
  </acceptance_criteria>
  <done>CAM-01 unit test green; downstream test names reserved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → ImageSequenceFrameSource | Image paths from config/glob are local files |
| dev pip install | Third-party wheels from PyPI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `image_dir` config | mitigate | Resolve paths; reject nonexistent dirs at `start()` with explicit error (no silent empty reads) |
| T-02-05 | Denial of Service | test fixtures | accept | Tiny single-frame fixture only; no large image blobs in git |
</threat_model>

<verification>
- `python -m pytest tests/test_camera_source.py::test_fake_source_returns_640x480_bgr -q` passes
- `python -m pytest tests/test_camera_source.py tests/test_debug_writer.py -q` runs with skips for not-yet-implemented tests
</verification>

<success_criteria>
- CAM-01 satisfied for offline `FrameSource` (image sequence) with 640×480 BGR and monotonic `frame_id`
- Wave 0 gaps from 02-VALIDATION.md closed: pyproject, test files, camera.example.json
</success_criteria>

<output>
After completion, create `.planning/phases/02-camera-capture/02-01-SUMMARY.md`
</output>
