---
phase: 02-camera-capture
plan: 02
type: execute
wave: 1
depends_on:
  - "01"
files_modified:
  - src/block_detected/camera.py
  - tests/test_camera_source.py
  - scripts/camera_smoke.py
autonomous: true
requirements:
  - CAM-01
  - CAM-02
user_setup:
  - service: Raspberry Pi Camera (CSI)
    why: "CAM-02 hardware verification for Picamera2 controls"
    dashboard_config:
      - task: "Install picamera2 via apt on target Pi"
        location: "sudo apt install python3-picamera2"
      - task: "Run libcamera/rpicam hardware smoke before Python debug"
        location: "rpicam-hello --list-cameras or libcamera-hello"
  - service: USB UVC camera
    why: "CAM-02 V4L2 control verification"
    dashboard_config:
      - task: "Install v4l-utils on target Linux/Pi"
        location: "sudo apt install v4l-utils"
      - task: "List controls: v4l2-ctl --device /dev/video0 --list-ctrls-menus"

must_haves:
  truths:
    - "CSI capture uses Picamera2 at 640×480, not cv2.VideoCapture for Pi Camera"
    - "USB capture uses OpenCV VideoCapture with explicit open/shape failures"
    - "Manual exposure/WB lock records requested, applied, verified, unsupported in metadata"
  artifacts:
    - path: src/block_detected/camera.py
      provides: "PiCamera2FrameSource, UsbVideoCaptureFrameSource, create_frame_source"
      exports: ["PiCamera2FrameSource", "UsbVideoCaptureFrameSource", "create_frame_source"]
    - path: scripts/camera_smoke.py
      provides: "Target hardware smoke CLI"
    - path: tests/test_camera_source.py
      provides: "Mocked CAM-01/CAM-02 adapter tests"
  key_links:
    - from: src/block_detected/camera.py
      to: picamera2
      via: "lazy import inside PiCamera2FrameSource.start only"
      pattern: "from picamera2 import Picamera2"
    - from: src/block_detected/camera.py
      to: cv2.VideoCapture
      via: "UsbVideoCaptureFrameSource.start"
      pattern: "VideoCapture.*CAP_V4L2"
    - from: scripts/camera_smoke.py
      to: create_frame_source
      via: "loads config/camera JSON path argument"
      pattern: "create_frame_source"
---

<objective>
Implement hardware capture adapters behind `FrameSource`: Picamera2 for CSI and OpenCV VideoCapture for USB, with warmup-then-lock exposure/WB (CAM-02) and honest control metadata.

Purpose: Deliver CAM-01 multi-backend abstraction and CAM-02 reproducible capture settings without leaking backend types to later pipeline phases.
Output: Production adapters, `create_frame_source()` factory, green mocked unit tests (02-01-02, 02-01-03), hardware smoke script.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-camera-capture/02-01-SUMMARY.md
@.planning/phases/02-camera-capture/02-RESEARCH.md
@.planning/phases/02-camera-capture/02-VALIDATION.md
@config/camera.example.json
@src/block_detected/camera.py

<interfaces>
From Plan 01 `src/block_detected/camera.py` (extend, do not rename):

```python
@dataclass(frozen=True)
class CaptureFrame:
    frame_id: str
    image_bgr: np.ndarray
    timestamp_ns: int
    source: str
    metadata: Mapping[str, object]

class FrameSource(Protocol):
    def start(self) -> None: ...
    def read(self) -> CaptureFrame: ...
    def stop(self) -> None: ...
```

Metadata contract for CAM-02 (add to every `read()` after lock applied):

```python
metadata = {
    "settings_requested": {...},
    "settings_applied": {...},
    "settings_verified": {...},
    "settings_unsupported": [...],
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" validation_task_id="02-01-02">
  <name>Task 1: UsbVideoCaptureFrameSource with explicit failures</name>
  <read_first>
    - `02-RESEARCH.md` → Pitfall 1 (property best-effort), USB skeleton, V4L2 probe shape
    - OpenCV docs: read back `CAP_PROP_FRAME_WIDTH/HEIGHT` after set
  </read_first>
  <files>src/block_detected/camera.py, tests/test_camera_source.py</files>
  <behavior>
    - `test_backend_open_failure_is_explicit` passes with mocked `VideoCapture` returning `isOpened() == False`
    - Wrong shape frame raises `RuntimeError` mentioning expected 640×480
    - Uses `cv.CAP_V4L2` when available; metadata includes actual width/height readback
  </behavior>
  <action>
    Implement `UsbVideoCaptureFrameSource` in `camera.py`:
    - `start()`: open index, set 640×480, read back dimensions into metadata seed
    - `read()`: fail if not opened, read fails, or `image_bgr.shape[:2] != (480, 640)`; convert to BGR if needed; monotonic `frame_00000N` ids; `source="usb-opencv"`
    - `stop()`: release cap
    - Optional `_apply_usb_manual_controls()` using OpenCV `CAP_PROP_AUTO_EXPOSURE`, `CAP_PROP_EXPOSURE`, `CAP_PROP_AUTO_WB` when `lock_exposure`/`lock_white_balance` true; populate CAM-02 metadata keys; never assume set succeeded
    Add mocked unit test `test_backend_open_failure_is_explicit` (02-01-02) — patch `cv2.VideoCapture`.
    Remove skip marker from that test.
  </action>
  <verify>
    <automated>python -m pytest tests/test_camera_source.py::test_backend_open_failure_is_explicit -q</automated>
  </verify>
  <acceptance_criteria>
    - Validation task 02-01-02 passes without hardware
    - No `picamera2` import on USB-only code paths
  </acceptance_criteria>
  <done>USB adapter and open-failure test complete</done>
</task>

<task type="auto" tdd="true" validation_task_id="02-01-03">
  <name>Task 2: PiCamera2FrameSource with warmup and manual lock</name>
  <read_first>
    - `02-RESEARCH.md` → Pattern 2, Pitfall 3 (lock too early), color order (RGB888 → BGR)
    - Picamera2: `create_video_configuration(main={"size": (640, 480), "format": "RGB888"})`, `set_controls`, `capture_metadata`
  </read_first>
  <files>src/block_detected/camera.py, tests/test_camera_source.py</files>
  <behavior>
    - `test_manual_control_metadata_records_support` passes with mocked Picamera2: records applied/unsupported lists
    - Module import does not require picamera2 installed (lazy import in `start`)
    - Warmup discards N frames before lock when `warmup_frames > 0`
  </behavior>
  <action>
    Implement `PiCamera2FrameSource`:
    - Lazy `from picamera2 import Picamera2` in `start()`; raise `RuntimeError` with install hint if ImportError
    - Configure 640×480 RGB888; `capture_array()` → `cv2.cvtColor(..., COLOR_RGB2BGR)` per research
    - Warmup loop: `warmup_frames` reads without assigning final lock values
    - After warmup: `capture_metadata()`, then `set_controls` with `AeEnable=False`, `AwbEnable=False`, and snapshot `ExposureTime`, `AnalogueGain`, `ColourGains` from metadata (or overrides from `CameraSettings`)
    - Populate CAM-02 metadata on each `read()`
    Add `test_manual_control_metadata_records_support` using unittest.mock on Picamera2 class (02-01-03); remove skip.
    Do NOT use `cv2.VideoCapture` for CSI path.
  </action>
  <verify>
    <automated>python -m pytest tests/test_camera_source.py::test_manual_control_metadata_records_support -q</automated>
  </verify>
  <acceptance_criteria>
    - Validation task 02-01-03 passes with mocks
    - CSI adapter documents `source="picamera2"` in CaptureFrame
  </acceptance_criteria>
  <done>CSI adapter with CAM-02 metadata contract; manual-control test green</done>
</task>

<task type="auto">
  <name>Task 3: create_frame_source factory and hardware smoke script</name>
  <read_first>
    - `config/camera.example.json` backend values
    - `02-VALIDATION.md` Manual-Only Verifications table
  </read_first>
  <files>src/block_detected/camera.py, scripts/camera_smoke.py</files>
  <behavior>
    - `create_frame_source(settings: CameraSettings) -> FrameSource` selects adapter by `backend` enum/string
    - Smoke script reads JSON path, captures one frame, prints shape + metadata JSON to stdout
  </behavior>
  <action>
    Add `load_camera_settings(path: Path) -> CameraSettings` parsing example JSON shape (stdlib `json`).
    Add `create_frame_source(settings) -> FrameSource` dispatching to ImageSequence, PiCamera2, Usb implementations.
    Add `scripts/camera_smoke.py` CLI: `--config path`, optional `--frames N`, writes no debug files (debug is Plan 03); prints `frame_id`, shape, `settings_*` metadata; exit code 1 on failure.
    Document in script docstring: run on target Pi for CSI/USB manual verification per 02-VALIDATION.md.
  </action>
  <verify>
    <automated>python -m pytest tests/test_camera_source.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All non-skipped tests in `test_camera_source.py` pass locally
    - `python scripts/camera_smoke.py --config config/camera.example.json` works when example uses `image_sequence` backend and fixture dir exists
  </acceptance_criteria>
  <done>Factory + smoke script; CAM-01/CAM-02 unit coverage complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config JSON → create_frame_source | Local file settings |
| v4l2-ctl subprocess (if used) | Device path from config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | `v4l2-ctl` helper | mitigate | `subprocess.run([...], shell=False)`; validate `device_path` matches `^/dev/video[0-9]+$` before invoke |
| T-02-06 | Information Disclosure | smoke script stdout | accept | Prints metadata only on target hardware; document no secrets in config |
| T-02-01 | Denial of Service | warmup_frames | mitigate | Cap `warmup_frames` at reasonable max (e.g. 60) in settings validation |
</threat_model>

<verification>
- `python -m pytest tests/test_camera_source.py -q` all tests pass (no skips for 02-01-02/03)
- Manual (target Pi): `python scripts/camera_smoke.py --config <csi-or-usb-config>` records control metadata
</verification>

<success_criteria>
- CAM-01: Three backends behind one `FrameSource` / `create_frame_source`
- CAM-02: Metadata honestly reports control lock state; Pi uses Picamera2 controls not VideoCapture
</success_criteria>

<output>
After completion, create `.planning/phases/02-camera-capture/02-02-SUMMARY.md`
</output>
