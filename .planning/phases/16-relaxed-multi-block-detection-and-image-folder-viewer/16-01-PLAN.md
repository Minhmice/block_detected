---
phase: 16-relaxed-multi-block-detection-and-image-folder-viewer
plan: 01
type: execute
wave: 1
depends_on:
  - 15-01
files_modified:
  - src/block_detection_v2/config.py
  - src/block_detection_v2/image_source.py
  - src/block_detection_v2/camera.py
  - src/block_detection_v2/polygon.py
  - src/block_detection_v2/tracker.py
  - src/block_detection_v2/renderer.py
  - src/block_detection_v2/models.py
  - src/block_detection_v2/main.py
autonomous: true
requirements:
  - V2-IMG-01
  - V2-IMG-02
  - V2-MULTI-01
  - V2-MULTI-02
  - V2-RELAX-01
must_haves:
  truths:
    - "image_source.py owns ImageFolder and open_image_source for block_dataset"
    - "Arrow keys navigate images; overlay shows index/total and filename"
    - "find_hexagons returns multiple blocks with score filtering"
    - "frame output includes blocks array with per-block geometry"
  artifacts:
    - path: src/block_detection_v2/image_source.py
      provides: "Dataset image navigation source"
      exports: ["ImageFolder", "open_image_source", "wait_key", "navigation_hint"]
    - path: src/block_detection_v2/polygon.py
      provides: "Multi hexagon detection"
      exports: ["find_hexagons"]
    - path: src/block_detection_v2/tracker.py
      provides: "Per-block EMA tracking"
      exports: ["MultiTracker"]
---

<objective>
Dataset-driven dev loop: relaxed detection thresholds, multi-block per frame, dedicated image source on `block_dataset/` with arrow navigation (D-01–D-04).

Purpose: Lightweight UAT on 108 real photos without webcam.
Output: `main.py` uses `open_image_source()` by default; `block_dataset/` wired in config.
</objective>

<execution_context>
@.planning/phases/16-relaxed-multi-block-detection-and-image-folder-viewer/16-CONTEXT.md
@.planning/phases/16-relaxed-multi-block-detection-and-image-folder-viewer/16-RESEARCH.md
@src/block_detection_v2/image_source.py
@src/block_detection_v2/block_dataset/
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: image_source.py + block_dataset config</name>
  <files>src/block_detection_v2/image_source.py, src/block_detection_v2/config.py, src/block_detection_v2/camera.py</files>
  <read_first>
    - src/block_detection_v2/image_source.py
    - src/block_detection_v2/config.py
    - src/block_detection_v2/camera.py
    - .planning/phases/16-relaxed-multi-block-detection-and-image-folder-viewer/16-CONTEXT.md
  </read_first>
  <action>
    Per D-01: `image_source.py` contains `ImageFolder`, `open_image_source`, `navigation_hint`, `wait_key`, `is_prev_key`, `is_next_key`.

    `config.IMAGE_DIR` = package dir / `block_dataset`.

    Natural sort filenames (`dt1.jpg` before `dt10.jpg`).

    `camera.py` retains only `Camera` class — no ImageFolder.

    KEY_LEFT includes 65361 (arrow left); KEY_RIGHT includes 65363 (arrow right).
  </action>
  <acceptance_criteria>
    - `open_image_source()` opens with count 108 when block_dataset present
    - First image name is `dt1.jpg` after natural sort
    - `camera.py` has no class ImageFolder
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Relaxed multi-block detection</name>
  <files>src/block_detection_v2/polygon.py, src/block_detection_v2/config.py, src/block_detection_v2/tracker.py, src/block_detection_v2/models.py</files>
  <read_first>
    - src/block_detection_v2/polygon.py
    - src/block_detection_v2/tracker.py
    - src/block_detection_v2/config.py
  </read_first>
  <action>
    Per D-03/D-04:

    `find_hexagons` collects candidates with `_detection_score` and filters `score >= DETECTION_SCORE_MIN`.

    Config: `DETECTION_SCORE_MIN` (~0.38), lowered `MIN_CONTOUR_AREA`, raised `CONVEXITY_TOLERANCE`, `MAX_BLOCKS`, `MIN_BLOCK_CENTER_DIST`.

    `MultiTracker` in tracker.py matches detections to trackers by center distance.

    `HexagonDetection` includes `score` field.
  </action>
  <acceptance_criteria>
    - `find_hexagons` exported from polygon.py
    - `MultiTracker` class exists with update() returning list
    - Config contains DETECTION_SCORE_MIN and MAX_BLOCKS
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Main loop image navigation + multi-block render</name>
  <files>src/block_detection_v2/main.py, src/block_detection_v2/renderer.py</files>
  <read_first>
    - src/block_detection_v2/main.py
    - src/block_detection_v2/renderer.py
    - src/block_detection_v2/image_source.py
  </read_first>
  <action>
    Per D-02: `main()` calls `open_image_source()` (not Camera).

    Loop uses `wait_key`; prev on left arrow/p, next on right arrow/n/space.

    `render` draws multiple blocks with distinct colors; hint shows `[i/N] filename`.

    `frame_output` includes `blocks` list; top-level keys preserved for first block compat.

    `process_frame` uses `find_hexagons` + `MultiTracker`.
  </action>
  <acceptance_criteria>
    - `main.py` imports from image_source not camera for source
    - `frame_output` return dict has key `blocks` (list)
    - `navigation_hint` string contains `<- ->` or `prev/next`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `open_image_source().count() == 108`
2. Multi-block synthetic image → len(blocks) >= 2
3. Manual: run main, arrow through dt1-dt5 on block_dataset
</verification>

## Artifacts this phase produces

- `image_source.py` module (ImageFolder, open_image_source, wait_key, key helpers)
- `find_hexagons`, `MultiTracker`, `BlockResult`
- Config keys: DETECTION_SCORE_MIN, MAX_BLOCKS, IMAGE_DIR→block_dataset
