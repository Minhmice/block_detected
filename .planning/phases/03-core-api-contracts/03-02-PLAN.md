---
phase: 03-core-api-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/detect_only_v4/__init__.py
  - src/detect_only_v4/models/__init__.py
  - src/detect_only_v4/models/inspector.py
  - src/detect_only_v4/models/_path_utils.py
  - src/detect_only_v4/models/loader.py
  - src/detect_only_v4/cameras/__init__.py
  - src/detect_only_v4/cameras/discovery.py
  - src/detect_only_v4/detectors/__init__.py
  - src/detect_only_v4/detectors/normalize.py
  - src/detect_only_v4/render/__init__.py
  - src/detect_only_v4/render/overlay.py
  - src/detect_only_v4/pipeline/__init__.py
  - src/detect_only_v4/pipeline/detect.py
  - tests/detect_only_v4/conftest.py
  - tests/detect_only_v4/test_inspector.py
  - tests/detect_only_v4/test_inspector_metadata.py
  - tests/detect_only_v4/test_stubs.py
  - tests/detect_only_v4/fixtures/metadata_detect.yaml
  - tests/detect_only_v4/fixtures/metadata_segment.yaml
  - tests/detect_only_v4/fixtures/metadata_pose.yaml
  - tests/detect_only_v4/fixtures/ncnn_dir/metadata.yaml
autonomous: true
requirements:
  - CORE-01
  - CORE-03
  - CORE-04
  - CORE-05
must_haves:
  truths:
    - "inspect_model(path) returns ModelInfo with family, task, format, class_names from model.task + metadata when available"
    - "Identification chain follows D-06 order; returns unknown when ambiguous — never guesses task from filename alone"
    - "inspect_model is the only module that imports ultralytics YOLO"
    - "All 7 public API functions importable from detect_only_v4 package root"
    - "Six non-inspect functions raise NotImplementedError with docstring citing target phase"
    - ".engine on non-CUDA returns loadable=False with clear TensorRT error per D-08"
    - "timing_ms records init, metadata, dry_infer, total on every inspect_model call"
  artifacts:
    - path: src/detect_only_v4/models/inspector.py
      provides: inspect_model()
      exports: ["inspect_model"]
    - path: src/detect_only_v4/__init__.py
      provides: public API surface
      exports: ["load_model", "inspect_model", "discover_cameras", "probe_camera", "detect_frame", "normalize_results", "draw_overlay"]
    - path: src/detect_only_v4/models/loader.py
      provides: load_model stub
      contains: "NotImplementedError"
    - path: tests/detect_only_v4/test_inspector.py
      provides: mocked YOLO identification chain tests
  key_links:
    - from: src/detect_only_v4/models/inspector.py
      to: ultralytics.YOLO
      via: lazy import inside inspector only
      pattern: "from ultralytics import YOLO"
    - from: src/detect_only_v4/__init__.py
      to: models.inspector.inspect_model
      via: "__getattr__ lazy re-export (root import must not load inspector/ultralytics)"
      pattern: "def __getattr__"
    - from: src/detect_only_v4/models/inspector.py
      to: core.types.ModelInfo
      via: return type
      pattern: "ModelInfo\\("
    - from: tests/detect_only_v4/test_inspector.py
      to: detect_only_v4.models.inspector.YOLO
      via: unittest.mock.patch
      pattern: 'patch\\("detect_only_v4\\.models\\.inspector\\.YOLO"'
---

<objective>
Implement `inspect_model()` with the full D-06 identification chain, wire public API exports and typed stubs for deferred functions, and add comprehensive mocked unit tests.

Purpose: Phase 4–8 import a stable public API; model family/task/format resolution is authoritative before any loader or adapter runs.
Output: Working `inspect_model`, six NotImplementedError stubs, fixtures, and full `tests/detect_only_v4/` suite for Phase 3 scope.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-api-contracts/03-CONTEXT.md
@.planning/phases/03-core-api-contracts/03-RESEARCH.md
@.planning/phases/03-core-api-contracts/03-01-SUMMARY.md

<interfaces>
From src/detect_only_v4/core/types.py (created in 03-01):
- DetectionResult, ModelInfo, TaskKind, ModelFamily, ModelFormat, CameraInfo, InferConfig
- Each dataclass has to_dict() returning JSON-serializable dict

From src/detect_only_v4/core/errors.py:
- InspectError, ModelFormatError, PlatformUnsupportedError

From src/detect_only_v4/core/logging.py:
- setup_logging(), get_logger(name), LOGGER_NAME = "detect_only_v4"
</interfaces>

Ultralytics import ONLY in models/inspector.py (D-11). Stubs must import without torch/ultralytics side effects.

Package Legitimacy Audit (RESEARCH): ultralytics [Approved], pyyaml [Approved] — no new installs required; PyYAML transitive via ultralytics.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Path utilities and inspect_model identification chain</name>
  <files>src/detect_only_v4/models/__init__.py, src/detect_only_v4/models/_path_utils.py, src/detect_only_v4/models/inspector.py, tests/detect_only_v4/conftest.py, tests/detect_only_v4/fixtures/metadata_detect.yaml, tests/detect_only_v4/fixtures/metadata_segment.yaml, tests/detect_only_v4/fixtures/metadata_pose.yaml, tests/detect_only_v4/fixtures/ncnn_dir/metadata.yaml, tests/detect_only_v4/test_inspector_metadata.py, tests/detect_only_v4/test_inspector.py</files>
  <read_first>
    .planning/phases/03-core-api-contracts/03-CONTEXT.md (D-06 to D-08, D-11, D-17, D-18)
    .planning/phases/03-core-api-contracts/03-RESEARCH.md (inspect_model pseudocode, Pitfalls 1-6)
    src/detect_only_v4/core/types.py
    src/detect_only_v4/core/errors.py
    src/detect_only_v4/core/logging.py
  </read_first>
  <behavior>
    - detect_format resolves .pt, .onnx, .engine, .tflite, *_ncnn_model/ dir, *_openvino_model/ dir
    - Step 1: mocked YOLO with task=detect, yaml_file=yolo26n.yaml → family=yolo26, task=detect without dry infer
    - Step 2: NCNN dir with metadata.yaml task=segment → task=segment without YOLO init success
    - Step 3: filename yolov8n-seg.pt alone does NOT set task without step 1/2/4 confirmation
    - Step 4: dry infer checks obb > keypoints > masks > boxes attribute presence (is not None), not detection count
    - Step 5: ambiguous mock → task=unknown, family=unknown
    - .engine + cuda_available=False → loadable=False, error mentions CUDA/TensorRT
    - timing_ms keys: init, metadata, dry_infer, total (all present, numeric)
    - identify_steps lists steps taken (e.g. format:pt, yolo_init:task=detect)
    - metadata.yaml string-encoded names field parsed via ast.literal_eval
    - classify task from Ultralytics maps to TaskKind.UNKNOWN with WARNING log
  </behavior>
  <action>
    Implement inspect_model per D-06–D-08, D-11, D-17–D-18:

    1. `models/_path_utils.py` (internal):
       - detect_format(path: Path) -> ModelFormat — extension + directory suffix rules per RESEARCH
       - find_metadata_yaml(path: Path) -> Path | None — check dir/metadata.yaml and parent for exports
       - parse_metadata_yaml(path: Path) -> dict — yaml.safe_load + ast.literal_eval for string fields (stride, imgsz, names, end2end, args, batch) mirroring Ultralytics BaseBackend.apply_metadata
       - filename_hints(stem: str) -> tuple[TaskKind, ModelFamily] — hints only; yolo26/yolo11/yolov8 patterns, -seg/-pose/-obb suffixes
       - cuda_available() -> bool — try torch.cuda.is_available() inside try/except; False if torch missing
       - map_ultralytics_task(task: str) -> TaskKind — classify/semantic → UNKNOWN

    2. `models/inspector.py`:
       - `from ultralytics import YOLO` at module level is acceptable ONLY in this file (D-11)
       - `inspect_model(path: str | Path) -> ModelInfo` implements full chain from RESEARCH pseudocode
       - Use get_logger("inspector"); log INFO on start/complete, DEBUG per step, WARNING on filename hint or dry infer fallback, ERROR on init failure
       - Single YOLO instance per call only — no global cache (Claude discretion)
       - Dry infer: np.zeros((640, 640, 3), dtype=np.uint8) single predict call
       - infer_family from model.model.yaml.get("yaml_file") stem mapping
       - infer_task_from_results: check result.obb, .keypoints, .masks, .boxes with `is not None` priority
       - Catch exceptions → populate ModelInfo.error, append to identify_steps; do not raise to caller unless path truly invalid (use ModelFormatError for unrecognized format)
       - Early gate D-08 for .engine without CUDA before YOLO init

    3. `models/__init__.py` — export inspect_model

    4. Test fixtures under tests/detect_only_v4/fixtures/ — metadata YAML files with realistic Ultralytics export content including one with string-encoded names: "names: \"{0: 'person'}\""

    5. test_inspector_metadata.py — parse fixtures without YOLO mock

    6. test_inspector.py — patch("detect_only_v4.models.inspector.YOLO") for all default tests; cover step order, unknown fallback, .engine gate (patch cuda_available), dry infer path

    Optional: @pytest.mark.integration test_inspect_real_yolo26n skipped by default — not required for plan completion.
  </action>
  <verify>
    <automated>pytest tests/detect_only_v4/test_inspector_metadata.py tests/detect_only_v4/test_inspector.py -x -q -m "not integration"</automated>
  </verify>
  <acceptance_criteria>
    All inspector tests pass without real weights; identification chain order verified; unknown returned when ambiguous; D-08 engine gate works.
  </acceptance_criteria>
  <done>CORE-04 and CORE-05 satisfied; inspect_model is production-ready for Phase 4 discovery</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Public API stubs and package exports</name>
  <files>src/detect_only_v4/__init__.py, src/detect_only_v4/models/loader.py, src/detect_only_v4/cameras/__init__.py, src/detect_only_v4/cameras/discovery.py, src/detect_only_v4/detectors/__init__.py, src/detect_only_v4/detectors/normalize.py, src/detect_only_v4/render/__init__.py, src/detect_only_v4/render/overlay.py, src/detect_only_v4/pipeline/__init__.py, src/detect_only_v4/pipeline/detect.py, tests/detect_only_v4/test_stubs.py</files>
  <read_first>
    .planning/phases/03-core-api-contracts/03-CONTEXT.md (D-09, D-10)
    .planning/phases/03-core-api-contracts/03-RESEARCH.md (stub module layout)
    .planning/REQUIREMENTS.md (CORE-03)
  </read_first>
  <behavior>
    - from detect_only_v4 import load_model, inspect_model, discover_cameras, probe_camera, detect_frame, normalize_results, draw_overlay succeeds
    - load_model(any) raises NotImplementedError mentioning Phase 4
    - discover_cameras(), probe_camera(id) raise NotImplementedError mentioning Phase 6
    - normalize_results(results, task), draw_overlay(frame, detections, config) raise NotImplementedError mentioning Phase 5
    - detect_frame(frame, model, config) raises NotImplementedError mentioning Phase 5/7
    - import detect_only_v4 does NOT load ultralytics — verify ultralytics not in sys.modules after `import detect_only_v4` (before calling inspect_model)
    - `from detect_only_v4 import inspect_model` may load ultralytics (acceptable); `import detect_only_v4` alone must not
    - All stub functions have complete type hints on parameters and return types
  </behavior>
  <action>
    Implement D-10 stubs in dedicated modules per RESEARCH file layout:

    1. `models/loader.py` — `def load_model(path: str | Path) -> Any: raise NotImplementedError("Implemented in Phase 4")` with full type hints and docstring

    2. `cameras/discovery.py` — `discover_cameras() -> list[CameraInfo]` and `probe_camera(camera_id: str) -> CameraInfo` stubs → Phase 6

    3. `detectors/normalize.py` — `normalize_results(raw_results: Any, task: TaskKind) -> list[DetectionResult]` stub → Phase 5

    4. `render/overlay.py` — `draw_overlay(frame: Any, detections: list[DetectionResult], config: InferConfig | None = None) -> Any` stub → Phase 5

    5. `pipeline/detect.py` — `detect_frame(frame: Any, model: Any, config: InferConfig | None = None) -> tuple[Any, list[DetectionResult]]` stub → Phase 5/7

    6. `__init__.py` — lazy public API wiring per CORE-03 and D-09:
       - Eagerly import and re-export types only: DetectionResult, ModelInfo, TaskKind, ModelFamily, ModelFormat, CameraInfo, InferConfig (no ultralytics dependency)
       - Implement `__getattr__(name: str)` to lazy-import function exports on first access: load_model, inspect_model, discover_cameras, probe_camera, detect_frame, normalize_results, draw_overlay — each from its submodule
       - Define `__all__` listing all public names
       - Root `import detect_only_v4` must NOT trigger ultralytics in sys.modules; `from detect_only_v4 import inspect_model` is allowed to load it

    7. Each submodule `__init__.py` — minimal package markers (no eager imports of inspector from package root)

    8. test_stubs.py — assert NotImplementedError for each stub; test `import detect_only_v4` leaves ultralytics out of sys.modules; test `from detect_only_v4 import inspect_model` resolves callable

    NO ultralytics import in stub modules. Module-level `from ultralytics import YOLO` ONLY in models/inspector.py (D-11).
  </action>
  <verify>
    <automated>pytest tests/detect_only_v4/test_stubs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    All 7 functions importable from package root; six stubs raise NotImplementedError with phase reference; type hints present on all public functions.
  </acceptance_criteria>
  <done>CORE-03 satisfied; public API surface declared for downstream phases</done>
</task>

<task type="auto">
  <name>Task 3: Full Phase 3 test suite and greenfield verification</name>
  <files>tests/detect_only_v4/test_no_legacy_imports.py</files>
  <read_first>
    tests/detect_only_v4/test_no_legacy_imports.py
    .planning/ROADMAP.md (Phase 3 success criteria)
  </read_first>
  <action>
    Extend test_no_legacy_imports.py (from 03-01) to cover ALL new modules added in this plan — models/, cameras/, detectors/, render/, pipeline/, updated __init__.py.

    Run full Phase 3 test suite:
    pytest tests/detect_only_v4/ -x -q -m "not integration"

    Verify pyproject.toml needs no change (setuptools packages.find where=["src"] already includes detect_only_v4). If import detect_only_v4 fails in editable install, document in SUMMARY — do not modify pyproject unless package truly not found.

    Add conftest.py fixtures if missing: mock_yolo_detect, tmp_ncnn_metadata_dir (copy fixtures/ncnn_dir), mock_cuda_unavailable.

    Confirm success criteria from ROADMAP Phase 3:
    1. DetectionResult JSON — covered by test_types
    2. inspect_model returns family/task/format — covered by test_inspector
    3. unknown not guess — covered by test_inspector ambiguous case
    4. Public API importable — covered by test_stubs
    5. No legacy imports — test_no_legacy_imports
  </action>
  <verify>
    <automated>pytest tests/detect_only_v4/ -x -q -m "not integration"</automated>
  </verify>
  <acceptance_criteria>
    Full tests/detect_only_v4/ suite passes; CORE-01 fully verified across entire src/detect_only_v4/ tree.
  </acceptance_criteria>
  <done>Phase 3 complete — all CORE-01 through CORE-06 requirements satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inspect_model(path) | User-supplied filesystem path crosses trust boundary |
| metadata.yaml parsing | Untrusted YAML from export directories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information disclosure | inspect_model(path) | mitigate | Path.resolve() to absolute; document user-supplied path scope |
| T-03-05 | Tampering | metadata.yaml | mitigate | yaml.safe_load only; never yaml.load; ast.literal_eval on known keys only |
| T-03-06 | Denial of service | dry infer in inspect | accept | Single 640×640 frame; record timing_ms; only when steps 1–2 fail task |
| T-03-07 | Spoofing | filename heuristics | mitigate | Hints only per D-06; never sole source for task; log WARNING |
| T-03-SC | Tampering | ultralytics pip package | mitigate | Package Legitimacy Audit [Approved]; pin >=8.4.0,<9.0 already in pyproject |
</threat_model>

<verification>
- `pytest tests/detect_only_v4/ -x -q -m "not integration"` all pass
- `python -c "from detect_only_v4 import inspect_model, load_model, DetectionResult; print(inspect_model)"` succeeds
- `python -c "import detect_only_v4; import sys; assert 'ultralytics' not in sys.modules"` succeeds (root import must not load ultralytics via __getattr__)
- `python -c "from detect_only_v4 import inspect_model; assert callable(inspect_model)"` succeeds
- Grep gate: `rg "hex_detector|block_detected" src/detect_only_v4/` returns zero matches in import statements (supplement AST test)
</verification>

<success_criteria>
1. inspect_model returns ModelInfo with family, task, format, class_names per D-06/D-07
2. Unknown task/family when insufficient data — never guesses from filename alone (CORE-05)
3. All 7 public API functions importable from detect_only_v4 (CORE-03)
4. Only inspector.py imports ultralytics (D-11)
5. timing_ms and identify_steps populated on every inspect_model call (D-18)
6. Full test suite passes without real model weights in CI
</success_criteria>

<output>
Create `.planning/phases/03-core-api-contracts/03-02-SUMMARY.md` when done
</output>
