---
phase: 03-core-api-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/detect_only_v4/__init__.py
  - src/detect_only_v4/core/__init__.py
  - src/detect_only_v4/core/types.py
  - src/detect_only_v4/core/protocols.py
  - src/detect_only_v4/core/errors.py
  - src/detect_only_v4/core/logging.py
  - tests/detect_only_v4/__init__.py
  - tests/detect_only_v4/conftest.py
  - tests/detect_only_v4/test_types.py
  - tests/detect_only_v4/test_logging.py
  - tests/detect_only_v4/test_errors.py
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-06
must_haves:
  truths:
    - "DetectionResult round-trips to JSON with all fields; track_id defaults None"
    - "TaskKind, ModelFamily, ModelFormat serialize as plain strings via to_dict()"
    - "ModelInfo dataclass exists with path, format, family, task, class_names, imgsz, stride, loadable, error, identify_steps, timing_ms"
    - "CameraBackend, ModelBackend, TaskAdapter ABCs define contracts for later phases"
    - "Skip vs fatal error taxonomy exists with InspectError hierarchy"
    - "Root logger detect_only_v4 configures via setup_logging() with StreamHandler"
    - "No imports from hex_detector, block_detected*, view, or stream in core modules"
  artifacts:
    - path: src/detect_only_v4/core/types.py
      provides: DetectionResult, ModelInfo, StrEnums, minimal CameraInfo/InferConfig
      contains: "class DetectionResult"
    - path: src/detect_only_v4/core/protocols.py
      provides: CameraBackend, ModelBackend, TaskAdapter ABCs
      contains: "class CameraBackend"
    - path: src/detect_only_v4/core/errors.py
      provides: DetectOnlyError taxonomy
      contains: "class InspectError"
    - path: src/detect_only_v4/core/logging.py
      provides: setup_logging, get_logger
      contains: 'LOGGER_NAME = "detect_only_v4"'
  key_links:
    - from: src/detect_only_v4/core/types.py
      to: json.dumps
      via: DetectionResult.to_dict()
      pattern: "def to_dict"
    - from: src/detect_only_v4/core/logging.py
      to: logging.getLogger
      via: LOGGER_NAME constant
      pattern: 'getLogger\\(.*detect_only_v4'
---

<objective>
Establish the greenfield `detect_only_v4` package skeleton and full core contracts layer: typed dataclasses, StrEnums, protocol ABCs, error taxonomy, and stdlib structured logging.

Purpose: Downstream phases (models, adapters, cameras, pipeline) import stable types and protocols without touching legacy code.
Output: `src/detect_only_v4/core/` complete with unit tests for types, logging, and errors.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-core-api-contracts/03-CONTEXT.md
@.planning/phases/03-core-api-contracts/03-RESEARCH.md
@pyproject.toml

Greenfield only — D-19 forbids imports from `hex_detector`, `block_detected`, `block_detected_v1`, `view`, `stream`, or any legacy module.

`pyproject.toml` already has `[tool.setuptools.packages.find] where = ["src"]` — new package auto-discovered once directory exists.

For Python 3.10 compatibility: if `StrEnum` unavailable, use `class TaskKind(str, Enum)` pattern; repo requires `>=3.10`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Package skeleton and core/types.py</name>
  <files>src/detect_only_v4/__init__.py, src/detect_only_v4/core/__init__.py, src/detect_only_v4/core/types.py, tests/detect_only_v4/__init__.py, tests/detect_only_v4/conftest.py, tests/detect_only_v4/test_types.py</files>
  <read_first>
    .planning/phases/03-core-api-contracts/03-CONTEXT.md (D-01 to D-05, D-12 to D-15)
    .planning/phases/03-core-api-contracts/03-RESEARCH.md (Pattern 1: StrEnum + to_dict)
  </read_first>
  <behavior>
    - DetectionResult with required fields serializes via to_dict(); track_id defaults None; optional mask/keypoints/obb_points/angle are None when unset
    - mask shape is list[list[list[float]]]; keypoints are list[dict] with x, y, conf keys
    - TaskKind values: detect, segment, pose, obb, unknown
    - ModelFamily values: yolov8, yolo11, yolo26, unknown
    - ModelFormat values: pt, onnx, engine, tflite, ncnn, openvino, unknown
    - ModelInfo.to_dict() emits enum fields as plain strings, not Enum objects
    - json.dumps(DetectionResult(...).to_dict()) succeeds without TypeError
  </behavior>
  <action>
    Create package skeleton per D-09 (core layer only in this task):

    1. `src/detect_only_v4/__init__.py` — module docstring only; no public re-exports yet (plan 03-02 owns exports per D-10).

    2. `src/detect_only_v4/core/__init__.py` — re-export key types: DetectionResult, ModelInfo, TaskKind, ModelFamily, ModelFormat, CameraInfo, InferConfig.

    3. `src/detect_only_v4/core/types.py` per D-01–D-05, D-12–D-15:
       - `DetectionResult` dataclass with all specified fields; `track_id: int | None = None` per D-05
       - `ModelInfo` dataclass per D-07: path, format, family, task, class_names (dict[int, str]), imgsz, stride, loadable, error, identify_steps (list[str]), timing_ms (dict[str, float])
       - `CameraInfo` minimal dataclass (id, backend, label, width, height, fps fields as Optional) — ROADMAP alignment; no logic
       - `InferConfig` minimal dataclass (conf, iou, imgsz, class_filter fields with sensible defaults) — contract only for later phases
       - StrEnums TaskKind, ModelFamily, ModelFormat with exact string values from CONTEXT
       - Each dataclass implements explicit `to_dict() -> dict[str, Any]` — do NOT use dataclasses.asdict alone (enums must become strings)

    4. `tests/detect_only_v4/conftest.py` — empty or minimal pytest config marker.

    5. `tests/detect_only_v4/test_types.py` — write tests FIRST (RED), then implement types (GREEN).

    Zero legacy imports (D-19). No ultralytics, torch, or opencv imports in types.py.
  </action>
  <verify>
    <automated>pytest tests/detect_only_v4/test_types.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    All test_types.py tests pass; DetectionResult JSON round-trip includes track_id=None; enum values in to_dict() are strings.
  </acceptance_criteria>
  <done>CORE-02 satisfied; core type contracts importable from detect_only_v4.core</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Protocol ABCs, error taxonomy, and logging</name>
  <files>src/detect_only_v4/core/protocols.py, src/detect_only_v4/core/errors.py, src/detect_only_v4/core/logging.py, tests/detect_only_v4/test_logging.py, tests/detect_only_v4/test_errors.py</files>
  <read_first>
    .planning/phases/03-core-api-contracts/03-RESEARCH.md (Pattern 3 error taxonomy, Pattern 4 logging)
    src/detect_only_v4/core/types.py
  </read_first>
  <behavior>
    - DetectOnlyError is root; InspectError, ModelFormatError, PlatformUnsupportedError inherit correctly
    - SkipFrameError and FatalPipelineError exist for pipeline phases (stub usage OK)
    - setup_logging() creates StreamHandler on logger detect_only_v4 only once (idempotent)
    - get_logger("inspector") returns logger named detect_only_v4.inspector
    - CameraBackend defines abstract methods: open, read_frame, close, probe (signatures with type hints)
    - ModelBackend defines abstract methods: load, predict, unload
    - TaskAdapter defines abstract method: normalize(raw_results) -> list[DetectionResult]
  </behavior>
  <action>
    Implement core infrastructure per D-16–D-18 and CORE-06:

    1. `core/errors.py` — exception hierarchy from RESEARCH Pattern 3:
       DetectOnlyError → InspectError → ModelFormatError, PlatformUnsupportedError
       DetectOnlyError → SkipFrameError (recoverable, log+continue semantics documented in docstring)
       DetectOnlyError → FatalPipelineError (stop session)

    2. `core/logging.py` per D-16:
       LOGGER_NAME = "detect_only_v4"
       setup_logging(level=logging.INFO) — StreamHandler, format "%(asctime)s %(levelname)s [%(name)s] %(message)s", idempotent handler attach
       get_logger(name: str | None = None) → logging.Logger with optional submodule suffix

    3. `core/protocols.py` — ABCs using typing.Protocol or abc.ABC per project style:
       CameraBackend: open(), read_frame() -> ndarray placeholder (use Any or numpy type hint string), close(), probe() -> CameraInfo
       ModelBackend: load(path), predict(frame), unload()
       TaskAdapter: normalize(raw_results, task: TaskKind) -> list[DetectionResult]
       Import DetectionResult, CameraInfo, TaskKind from core.types only — no legacy imports

    4. Tests first in test_errors.py and test_logging.py, then implement.

    Document log level guidance in logging.py module docstring per D-17 (INFO inspect start/complete, DEBUG per step, WARNING fallback, ERROR failure).
  </action>
  <verify>
    <automated>pytest tests/detect_only_v4/test_logging.py tests/detect_only_v4/test_errors.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    Error hierarchy instantiable; setup_logging idempotent; logger name detect_only_v4 verified; ABCs cannot be instantiated directly.
  </acceptance_criteria>
  <done>CORE-06 satisfied; protocol contracts ready for Phase 4–7 implementers</done>
</task>

<task type="auto">
  <name>Task 3: Greenfield import gate for core package</name>
  <files>tests/detect_only_v4/test_no_legacy_imports.py</files>
  <read_first>
    .planning/phases/03-core-api-contracts/03-CONTEXT.md (D-19)
    .planning/phases/03-core-api-contracts/03-RESEARCH.md (test_no_legacy_imports.py strategy)
  </read_first>
  <action>
    Add `tests/detect_only_v4/test_no_legacy_imports.py` that AST-scans all `.py` files under `src/detect_only_v4/` (recursive) and asserts no ImportFrom/Import nodes reference forbidden modules:
    hex_detector, block_detected, block_detected_v1, view, stream, block_detection_v2.

    Use ast.parse — do not grep comments. Fail with clear message listing file and forbidden module.

    Also verify `import detect_only_v4.core.types` succeeds without importing ultralytics (no torch side-effect at import time for core-only import).

    Update `src/detect_only_v4/core/__init__.py` exports if any symbol missing from Task 1.
  </action>
  <verify>
    <automated>pytest tests/detect_only_v4/test_no_legacy_imports.py tests/detect_only_v4/ -x -q --ignore=tests/detect_only_v4/test_inspector.py --ignore=tests/detect_only_v4/test_inspector_metadata.py --ignore=tests/detect_only_v4/test_stubs.py</automated>
  </verify>
  <acceptance_criteria>
    AST scan passes for all detect_only_v4 source files; full plan-01 test subset green.
  </acceptance_criteria>
  <done>CORE-01 partial — greenfield core package verified with zero legacy imports</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core/types → JSON consumers | Wire format must be JSON-safe; no ndarray leakage |
| test AST scanner → src/ | Read-only verification; no runtime trust decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | types.to_dict() | mitigate | Explicit to_dict(); no asdict with raw Enums |
| T-03-02 | Information disclosure | error messages | accept | Errors are dev-facing; no secrets in Phase 3 |
| T-03-03 | Tampering | pip installs | mitigate | No new packages in plan 01; ultralytics already in pyproject |
| T-03-SC | Tampering | dependency supply chain | accept | No new pip installs in this plan |
</threat_model>

<verification>
- `pytest tests/detect_only_v4/test_types.py tests/detect_only_v4/test_logging.py tests/detect_only_v4/test_errors.py tests/detect_only_v4/test_no_legacy_imports.py -x -q` all pass
- `python -c "from detect_only_v4.core.types import DetectionResult, ModelInfo, TaskKind; print(TaskKind.DETECT.value)"` prints `detect`
- No file under `src/detect_only_v4/` imports legacy modules
</verification>

<success_criteria>
1. DetectionResult serializes to JSON with all fields; track_id defaults None
2. ModelInfo dataclass exists with D-07 fields and to_dict()
3. Error taxonomy distinguishes skip (SkipFrameError) vs fatal (FatalPipelineError) vs inspect (InspectError)
4. Logger `detect_only_v4` configurable via setup_logging()
5. Protocol ABCs defined for CameraBackend, ModelBackend, TaskAdapter
6. Zero legacy imports in src/detect_only_v4/ (AST verified)
</success_criteria>

<output>
Create `.planning/phases/03-core-api-contracts/03-01-SUMMARY.md` when done
</output>
