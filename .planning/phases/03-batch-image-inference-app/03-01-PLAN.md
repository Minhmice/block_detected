---
phase: 03-batch-image-inference-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/block_detected/vision/drawing/square.py
  - src/block_detected/vision/geometry.py
  - tests/test_square.py
autonomous: true
requirements: [REQ-03]
must_haves:
  truths:
    - Square box drawing centers on detection and uses max(w,h) as side
    - vision layer has no detection or YOLO imports
    - Unit tests cover square rect math without loading models
  artifacts:
    - src/block_detected/vision/drawing/square.py
    - tests/test_square.py
  key_links:
    - draw_square_box(Box, frame) → clamped pixel coords on numpy frame
    - Port logic from deleted batch_detect_square.py draw_square_box
---

<objective>
Add square-box annotation helpers in vision/drawing for batch output.

Purpose: Shared drawing primitive so apps/batch stays thin and matches legacy batch script visuals.
Output: `draw_square_box` + label helper callable from batch app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cv-layered-folder-structure-for-scalable-expansion/02-RESEARCH.md
@AGENTS.md
@src/block_detected/core/types.py
@src/block_detected/vision/geometry.py
</context>

<tasks>

<task type="auto">
  <name>Implement square box drawing module</name>
  <files>src/block_detected/vision/drawing/square.py</files>
  <action>Create square.py with draw_square_box(frame, x1, y1, x2, y2, ...) returning clamped square coords (cx, cy, side=max(w,h)). Reuse or add clamp helper in vision/geometry.py. Add draw_square_label optional helper for class+conf text above box. No ultralytics imports.</action>
  <verify>python3 -m py_compile src/block_detected/vision/drawing/square.py</verify>
  <done>Module compiles; matches legacy square-box geometry</done>
</task>

<task type="auto">
  <name>Unit tests for square box geometry</name>
  <files>tests/test_square.py</files>
  <action>Test square coords for known box inside frame, edge clamping at image bounds, and side length = max(x2-x1, y2-y1). Use small synthetic numpy arrays or pure coord functions if split.</action>
  <verify>python3 -m pytest tests/test_square.py -q</verify>
  <done>tests/test_square.py passes</done>
</task>

</tasks>

<verification>
- python3 -m pytest tests/test_square.py -q exits 0
- vision/drawing/square.py imports only cv2, core, geometry — not detection
</verification>

<success_criteria>
- Square annotator ready for batch app consumption
</success_criteria>
