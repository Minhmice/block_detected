---
phase: 03-batch-image-inference-app
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/block_detected/apps/batch/app.py
  - src/block_detected/apps/batch/__init__.py
  - src/block_detected/detection/boxes.py
autonomous: true
requirements: [REQ-03]
must_haves:
  truths:
    - Batch app runs YOLO over all images in --input folder
    - CLI flags --model, --input, --output, --conf, --show match legacy batch script
    - Uses detection.yolo.loader, detection.boxes, io.images.iter_image_paths
    - Annotates with vision.drawing.square; writes to output dir
  artifacts:
    - src/block_detected/apps/batch/app.py
  key_links:
    - apps/batch/app.py → load_yolo, extract_boxes, iter_image_paths, draw_square_box
    - Defaults from config.paths and config.inference where appropriate
---

<objective>
Implement batch image inference orchestration in apps/batch/app.py.

Purpose: Replace deleted batch_detect_square.py with layered pipeline.
Output: Runnable `main()` with argparse and folder loop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-batch-image-inference-app/03-01-PLAN.md
@src/block_detected/detection/yolo/loader.py
@src/block_detected/io/images/__init__.py
@src/block_detected/config/paths.py
@src/block_detected/config/inference.py
</context>

<tasks>

<task type="auto">
  <name>Extend box parsing for batch labels</name>
  <files>src/block_detected/detection/boxes.py</files>
  <action>If needed, add extract_boxes_with_meta(result) returning list of (Box, cls_id, conf) or keep extract_boxes and read cls/conf in app loop from result.boxes. Prefer minimal API surface; do not duplicate YOLO parsing in app.</action>
  <verify>python3 -m pytest tests/test_boxes.py -q</verify>
  <done>Box parsing supports batch labeling without app touching raw ultralytics tensors</done>
</task>

<task type="auto">
  <name>Implement batch app main loop</name>
  <files>src/block_detected/apps/batch/app.py, src/block_detected/apps/batch/__init__.py</files>
  <action>Create app.py with argparse (--model, --input, --output, --conf, --show). Defaults: MODELS_DIR/DEFAULT_MODEL_NAME, IMAGES_DIR, IMAGES_OUT_DIR, EVAL_CONF or DEFAULT_CONF. Loop iter_image_paths; cv2.imread; model.predict; draw square boxes + labels; imwrite; optional imshow/waitKey q to break. Print [INFO|WARN|ERROR] progress lines. Export main() -> int.</action>
  <verify>python3 -m py_compile src/block_detected/apps/batch/app.py && python3 -c "from block_detected.apps.batch.app import main; print(main.__name__)"</verify>
  <done>Batch app importable; compiles</done>
</task>

</tasks>

<verification>
- py_compile on apps/batch/app.py
- Manual smoke (if images present): `python -c "from block_detected.apps.batch.app import main"` with --help via argparse
</verification>

<success_criteria>
- Batch pipeline implemented in apps layer using existing detection/io/vision modules
</success_criteria>
