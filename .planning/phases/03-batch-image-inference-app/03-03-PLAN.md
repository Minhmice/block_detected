---
phase: 03-batch-image-inference-app
plan: 03
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - pyproject.toml
  - README.md
  - AGENTS.md
  - src/block_detected/__main__.py
autonomous: true
requirements: [REQ-03]
must_haves:
  truths:
    - block-detected-batch console script installed via pip install -e .
    - README documents batch usage (flags + console script)
    - AGENTS.md change map lists apps/batch and block-detected-batch
  artifacts:
    - pyproject.toml [project.scripts] block-detected-batch
  key_links:
    - block-detected-batch → block_detected.apps.batch.app:main
    - Webcam entry remains main.py only (no second root script for batch)
---

<objective>
Wire batch entry points and sync user-facing docs.

Purpose: Discoverable CLI matching webcam pattern (pyproject script; webcam uses main.py).
Output: Console script + README/AGENTS batch section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-batch-image-inference-app/03-02-PLAN.md
@pyproject.toml
@README.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Add block-detected-batch console script</name>
  <files>pyproject.toml</files>
  <action>Add `block-detected-batch = "block_detected.apps.batch.app:main"` under [project.scripts]. Keep block-detected-webcam unchanged.</action>
  <verify>pip install -e . -q && block-detected-batch --help 2>&1 | head -5</verify>
  <done>Console script resolves and shows argparse help</done>
</task>

<task type="auto">
  <name>Document batch usage in README and AGENTS</name>
  <files>README.md, AGENTS.md</files>
  <action>Add batch section: block-detected-batch with --model --input --output --conf --show. Note images/ and images_out/ defaults from config.paths. Update AGENTS change map row for apps/batch/app.py. Optional: document `python -m block_detected.apps.batch` only if __main__ hook added — prefer console script as primary.</action>
  <verify>grep -q block-detected-batch README.md AGENTS.md</verify>
  <done>Docs mention batch CLI and flags</done>
</task>

<task type="auto">
  <name>Phase verification</name>
  <files>tests/</files>
  <action>Run full pytest suite. If sample images exist, one-image batch smoke optional; otherwise rely on unit tests + --help.</action>
  <verify>python3 -m pytest tests/ -q</verify>
  <done>All tests pass; batch entry documented</done>
</task>

</tasks>

<verification>
- python3 -m pytest tests/ -q exits 0
- block-detected-batch --help works after editable install
</verification>

<success_criteria>
- Batch feature shippable via pip console script and documented flags
</success_criteria>
