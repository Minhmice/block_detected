---
phase: 17-roi-fit-score-detection-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 17-02
files_modified:
  - src/block_detection_v2/score.py
  - src/block_detection_v2/config.py
  - src/block_detection_v2/main.py
  - src/block_detection_v2/polygon.py
  - tests/test_block_detection_v2_score.py
autonomous: true
requirements:
  - SPIKE-SCORE-01
  - SPIKE-SCORE-02
must_haves:
  truths:
    - "Composite score rejects label-sized contours (area &lt; 3500 or area_ratio &lt; 0.12)"
    - "Accept threshold 0.42 in config gates detections before tracker"
    - "find_hexagons demoted to optional fallback only (D-05)"
    - "Primary path is ROI → fit → score — not contour contest"
  artifacts:
    - path: src/block_detection_v2/score.py
      provides: "Composite detection scoring"
      exports: ["score_candidate", "edge_support", "validate_topology"]
    - path: src/block_detection_v2/main.py
      provides: "Full ROI-fit-score pipeline"
      pattern: "score_candidate"
    - path: src/block_detection_v2/polygon.py
      provides: "Legacy contour fallback"
      pattern: "USE_CONTOUR_FALLBACK"
  key_links:
    - from: src/block_detection_v2/main.py
      to: src/block_detection_v2/score.py
      via: "score >= config.DETECTION_SCORE_MIN"
      pattern: "score_candidate"
    - from: src/block_detection_v2/score.py
      to: src/block_detection_v2/config.py
      via: "SCORE_HEX_AREA_MIN, SCORE_AREA_RATIO_MIN, weights"
      pattern: "config\\.SCORE_"
---

<objective>
Add composite scoring, complete `main.py` detection pipeline, and demote contour-primary `find_hexagons` to optional fallback (D-04, D-05, SPIKE-SCORE-01/02).

Purpose: Legacy `_detection_score` lacks ROI `area_ratio` — labels scored 0.76–0.97 on tiny contours.
Output: `score.py`, scoring config, production `process_frame` with accept gate.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-roi-fit-score-detection-pipeline/17-CONTEXT.md
@.planning/phases/17-roi-fit-score-detection-pipeline/17-RESEARCH.md
@.cursor/skills/spike-findings-block-detected/references/scoring-reject.md
@.cursor/skills/spike-findings-block-detected/sources/shared/block_spike_lib.py
@src/block_detection_v2/main.py
@src/block_detection_v2/polygon.py
@src/block_detection_v2/config.py
@src/block_detection_v2/fit.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Score unit tests (RED)</name>
  <files>tests/test_block_detection_v2_score.py</files>
  <read_first>
    - .planning/phases/17-roi-fit-score-detection-pipeline/17-PATTERNS.md (score.py section)
    - .cursor/skills/spike-findings-block-detected/sources/shared/block_spike_lib.py (lines 368-423)
    - .cursor/skills/spike-findings-block-detected/references/scoring-reject.md
    - src/block_detection_v2/polygon.py (legacy find_hexagons for FP comparison)
    - src/block_detection_v2/config.py
    - src/block_detection_v2/block_dataset/dt50.jpg
  </read_first>
  <behavior>
    - test_score_dt50_above_threshold: pipeline score on dt50 &gt;= config.DETECTION_SCORE_MIN (0.42)
    - test_label_contour_scores_below_threshold: legacy find_hexagons best candidate score &lt; 0.42 AND new pipeline rejects synthetic label-sized hex (score returns 0.0)
    - test_hard_reject_small_area: score_candidate with hex_area &lt; 3500 returns 0.0
    - test_hard_reject_low_area_ratio: score_candidate with area_ratio &lt; 0.12 returns 0.0
  </behavior>
  <action>
    Create `tests/test_block_detection_v2_score.py`.

    For dt50 test, run full chain: preprocess → edges → roi → fit → score_candidate.

    For label FP test: run `find_hexagons` on dt50 edges; assert max legacy score &lt; 0.42. Also assert `score_candidate` on synthetic tiny hex inside large ROI returns 0.0 (both must pass).

    Hard-reject tests may construct synthetic Point2D dict + small ROIBox.

    ISO-01: block_detection_v2 imports only.
  </action>
  <verify>
    <automated>cd /Users/minhmice/Documents/Projects/block_detected && PYTHONPATH=src pytest tests/test_block_detection_v2_score.py -x 2>&1 | grep -q "ImportError\|ModuleNotFoundError\|FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - Four tests defined with behaviors above
    - pytest fails before score.py exists
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: score.py + scoring config</name>
  <files>src/block_detection_v2/score.py, src/block_detection_v2/config.py</files>
  <read_first>
    - .planning/phases/17-roi-fit-score-detection-pipeline/17-PATTERNS.md (score.py section)
    - .cursor/skills/spike-findings-block-detected/sources/shared/block_spike_lib.py (lines 116-121, 368-423)
    - .planning/phases/17-roi-fit-score-detection-pipeline/17-CONTEXT.md (D-04)
  </read_first>
  <action>
    Add to `config.py` per D-04:
    - `SCORE_AREA_RATIO_MIN = 0.12`
    - `SCORE_HEX_AREA_MIN = 3500`
    - `DETECTION_SCORE_MIN = 0.42` (replace 0.38)
    - `SCORE_WEIGHT_AREA = 0.35`
    - `SCORE_WEIGHT_EDGE = 0.45`
    - `SCORE_WEIGHT_TOPO = 0.20`
    - `USE_CONTOUR_FALLBACK = False`

    Create `score.py`:
    - `validate_topology(points, *, strict=False) -> bool` — port spike lines 368-380; strict E-row check at accept time
    - `edge_support(points, edges, sample_step=4) -> float` — perimeter sampling with 5×5 patch
    - `score_candidate(points, edges, roi, *, strict_topology=True) -> float`
    - Formula: `weight_area*area_ratio + weight_edge*support + weight_topo*topo` using config weights
    - Hard reject per SPIKE-SCORE-02: return 0.0 if `hex_area &lt; SCORE_HEX_AREA_MIN` or `area_ratio &lt; SCORE_AREA_RATIO_MIN`
    - Private `_polygon_area` helper (or shared import — keep local to avoid cross-deps)

    Update `fit.py` if it duplicated `validate_topology` inline — import from `score.py` instead (single source).

    Do NOT extend `polygon._detection_score`.
  </action>
  <verify>
    <automated>cd /Users/minhmice/Documents/Projects/block_detected && PYTHONPATH=src pytest tests/test_block_detection_v2_score.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All four score tests pass
    - `DETECTION_SCORE_MIN` equals 0.42 in config
    - `score_candidate` uses config weights and hard-reject constants
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Complete main.py pipeline + demote polygon fallback</name>
  <files>src/block_detection_v2/main.py, src/block_detection_v2/polygon.py</files>
  <read_first>
    - src/block_detection_v2/main.py (interim pipeline from 17-02)
    - .planning/phases/17-roi-fit-score-detection-pipeline/17-CONTEXT.md (D-02, D-05)
    - .cursor/skills/spike-findings-block-detected/sources/shared/block_spike_lib.py (lines 458-492)
  </read_first>
  <action>
    Finalize `process_frame` per D-02 pipeline order:
    preprocess → detect_edges → extract_cluster_roi → masked edges → fit_hexagon_from_lines → score_candidate → tracker

    Replace interim `score=1.0` with:
    `score = score_candidate(points, masked, roi, strict_topology=True)`
    Accept only if `score >= config.DETECTION_SCORE_MIN`
    Build `HexagonDetection(points=points, contour_area=float(roi.area), score=score)`

    Per D-05: primary path NEVER calls `find_hexagons` by default.
    Add fallback only when `config.USE_CONTOUR_FALLBACK` and primary `raw` is empty:
    `raw = find_hexagons(edges, color.shape[:2])`

    Add module docstring to `polygon.py` noting legacy contour path; keep `find_hexagons` and `_detection_score` intact for fallback.

    Import `score_candidate` from `.score`.

    Unchanged: `MultiTracker`, `compute_geometry`, `BlockResult`, `renderer`, `image_source`.
  </action>
  <verify>
    <automated>cd /Users/minhmice/Documents/Projects/block_detected && PYTHONPATH=src python -c "
from block_detection_v2.main import process_frame
from block_detection_v2.tracker import MultiTracker
import cv2
f=cv2.imread('src/block_detection_v2/block_dataset/dt50.jpg')
blocks,out=process_frame(f, MultiTracker())
assert out['detected'] and len(blocks)>=1 and blocks[0].score>=0.42
"</automated>
  </verify>
  <acceptance_criteria>
    - dt50 `process_frame` detects block with score &gt;= 0.42
    - `grep -v '^#' src/block_detection_v2/main.py | grep -c 'score_candidate'` is at least 1
    - `find_hexagons` only referenced behind `USE_CONTOUR_FALLBACK` guard
    - `USE_CONTOUR_FALLBACK` defaults False in config
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detection score → tracker | Low-score reject prevents bad geometry propagation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering | config threshold edits | accept | Dev-tunable constants in config.py |
| T-17-SC | Tampering | pip installs | mitigate | ISO-02; no new packages |
</threat_model>

<verification>
- `pytest tests/test_block_detection_v2_score.py -q` passes
- `process_frame` on dt50 yields detected=True with score ≥ 0.42
- Legacy find_hexagons not called when USE_CONTOUR_FALLBACK=False
</verification>

<success_criteria>
- SPIKE-SCORE-01: composite score with area_ratio + edge_support + topology
- SPIKE-SCORE-02: hard rejects and configurable accept threshold 0.42
- Primary detection path is ROI-fit-score; contour demoted per D-05
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Description |
|----------|------|-------------|
| `src/block_detection_v2/score.py` | 17-03 | `score_candidate`, `validate_topology`, `edge_support` |
| Scoring config in `config.py` | 17-03 | Weights, mins, `DETECTION_SCORE_MIN=0.42`, `USE_CONTOUR_FALLBACK` |
| Production `process_frame` | 17-03 | Full ROI-fit-score with accept gate |
| `tests/test_block_detection_v2_score.py` | 17-03 | Score + label-rejection tests |

<output>
Create `.planning/phases/17-roi-fit-score-detection-pipeline/17-03-SUMMARY.md` when done
</output>
