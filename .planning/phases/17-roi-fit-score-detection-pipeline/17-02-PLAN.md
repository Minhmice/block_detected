---
phase: 17-roi-fit-score-detection-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/block_detection_v2/fit.py
  - src/block_detection_v2/main.py
  - tests/test_block_detection_v2_fit.py
autonomous: true
requirements:
  - SPIKE-FIT-01
  - SPIKE-FIT-02
must_haves:
  truths:
    - "fit_hexagon_from_lines returns valid A–F dict from ROI seed + Hough lines"
    - "main.py no longer discards Hough lines (_lines bug fixed per D-02)"
    - "Dominant-angle line histogram used — not fixed 0°/90° families"
  artifacts:
    - path: src/block_detection_v2/fit.py
      provides: "Line-assisted hexagon fit"
      exports: ["fit_hexagon_from_lines"]
    - path: src/block_detection_v2/main.py
      provides: "Lines wired to fitter"
      pattern: "fit_hexagon_from_lines"
    - path: tests/test_block_detection_v2_fit.py
      provides: "Fit unit tests"
  key_links:
    - from: src/block_detection_v2/main.py
      to: src/block_detection_v2/fit.py
      via: "roi_lines filtered by roi.mask midpoint"
      pattern: "fit_hexagon_from_lines"
    - from: src/block_detection_v2/fit.py
      to: src/block_detection_v2/edges.py
      via: "LineSegment type"
      pattern: "from \\.edges import LineSegment"
---

<objective>
Port `fit_hexagon_from_lines()` and wire Hough/LSD lines from `main.py` into the ROI-masked fit path (D-02, D-03, SPIKE-FIT-01/02).

Purpose: Lines are signal — baseline discards 150+ segments per frame at `main.py:30`.
Output: `fit.py`, interim `main.py` pipeline through fit (scoring gate deferred to 17-03).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-roi-fit-score-detection-pipeline/17-CONTEXT.md
@.planning/phases/17-roi-fit-score-detection-pipeline/17-RESEARCH.md
@.planning/phases/17-roi-fit-score-detection-pipeline/17-PATTERNS.md
@.cursor/skills/spike-findings-block-detected/references/hexagon-fit.md
@.cursor/skills/spike-findings-block-detected/sources/shared/block_spike_lib.py
@src/block_detection_v2/main.py
@src/block_detection_v2/roi.py
@src/block_detection_v2/models.py
@src/block_detection_v2/edges.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fit unit tests (RED)</name>
  <files>tests/test_block_detection_v2_fit.py</files>
  <read_first>
    - tests/test_block_detection_v2_roi.py (pattern from 17-01)
    - .cursor/skills/spike-findings-block-detected/sources/shared/block_spike_lib.py (lines 198-366, 368-380)
    - src/block_detection_v2/models.py (Point2D, LABELS contract)
    - src/block_detection_v2/block_dataset/dt50.jpg
  </read_first>
  <behavior>
    - test_fit_hexagon_dt50: after preprocess/edges/roi, fit returns dict with keys A–F; each value is Point2D
    - test_fit_topology_ordering: A.x &lt; B.x &lt; C.x and F.x &lt; E.x &lt; D.x for dt50 result
    - test_fit_returns_none_on_empty_roi: fit with synthetic zero edges returns None or falls back safely
  </behavior>
  <action>
    Create `tests/test_block_detection_v2_fit.py`.

    Import chain: `preprocess`, `detect_edges`, `extract_cluster_roi`, `fit_hexagon_from_lines`.

    For topology test, import `validate_topology` from `block_detection_v2.score` once available; until then duplicate minimal ordering asserts inline (replace when 17-03 lands).

    ISO-01: no v1 imports.

    Run pytest — must fail before `fit.py` exists.
  </action>
  <verify>
    <automated>cd /Users/minhmice/Documents/Projects/block_detected && PYTHONPATH=src pytest tests/test_block_detection_v2_fit.py -x 2>&1 | grep -q "ImportError\|ModuleNotFoundError\|FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - Test file with three tests above
    - pytest fails pre-implementation
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: fit.py — ROI seed + dominant-angle refinement</name>
  <files>src/block_detection_v2/fit.py</files>
  <read_first>
    - .cursor/skills/spike-findings-block-detected/sources/shared/block_spike_lib.py (lines 104-114, 198-366)
    - .planning/phases/17-roi-fit-score-detection-pipeline/17-CONTEXT.md (D-03)
    - src/block_detection_v2/roi.py
    - src/block_detection_v2/edges.py (LineSegment)
  </read_first>
  <action>
    Create `src/block_detection_v2/fit.py` porting spike logic:

    Private helpers (module-local, not exported): `_line_angle`, `_line_length`, `_merge_lines_by_angle`, `_dominant_angles`, `_intersect_lines`, `_pick_line_near_angle`, `_hex_from_roi`, `_snap_point_to_edges` (radius 12px per D-03), `_refine_with_lines`.

    Public API: `fit_hexagon_from_lines(lines, roi, frame_shape, edges=None) -> Optional[Dict[str, Point2D]]`

    Behavior per D-03:
    - Seed A–F from ROI isometric proportions (`_hex_from_roi`)
    - Refine via `_dominant_angles` histogram peaks — NOT fixed 0°/35°/90°/145° families
    - Snap all corners to edge pixels within 12px
    - Topology check: copy `validate_topology(points, strict=False)` inline temporarily OR import from `score.py` if 17-03 already merged; on relaxed fail revert to seed; on final fail return None

    Import `LineSegment` from `.edges`; `Point2D` from `.models`; `ROIBox` from `.roi`.

    ISO-01/02: OpenCV + NumPy + block_detection_v2 only.

    Do NOT implement `score_candidate` here.
  </action>
  <verify>
    <automated>cd /Users/minhmice/Documents/Projects/block_detected && PYTHONPATH=src pytest tests/test_block_detection_v2_fit.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `fit_hexagon_from_lines` exported from fit.py
    - dt50 fit returns 6 labeled Point2D corners
    - Topology ordering test passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire lines in main.py (interim — no score gate yet)</name>
  <files>src/block_detection_v2/main.py</files>
  <read_first>
    - src/block_detection_v2/main.py (line 30 `_lines` discard)
    - .planning/phases/17-roi-fit-score-detection-pipeline/17-CONTEXT.md (D-02)
    - .cursor/skills/spike-findings-block-detected/sources/shared/block_spike_lib.py (lines 463-477)
    - src/block_detection_v2/polygon.py (find_hexagons signature — still primary until 17-03)
  </read_first>
  <action>
    Per D-02 / SPIKE-FIT-02, replace contour-only entry in `process_frame`:

    1. `edges, lines = detect_edges(gray)` — remove `_lines` discard
    2. `roi = extract_cluster_roi(edges, color.shape[:2], block_mode=config.BLOCK_MODE)`
    3. If roi: `masked = cv2.bitwise_and(edges, roi.mask)`; filter `roi_lines` where line midpoint lies in roi.mask
    4. `points = fit_hexagon_from_lines(roi_lines or lines, roi, color.shape[:2], edges=masked)`
    5. If points: build `raw = [HexagonDetection(points=points, contour_area=float(roi.area), score=1.0)]` (temporary unity score until 17-03 scoring)
    6. Else: `raw = []` (do NOT call find_hexagons yet — 17-03 adds score gate + fallback flag)
    7. If roi is None: `raw = []`

    Add imports: `cv2`, `extract_cluster_roi` from `.roi`, `fit_hexagon_from_lines` from `.fit`, `HexagonDetection` from `.models`.

    Keep `tracker.update(raw)` → `compute_geometry` → `BlockResult` bridge unchanged.

    ISO-01: no imports from `block_detected` v1.
  </action>
  <verify>
    <automated>cd /Users/minhmice/Documents/Projects/block_detected && PYTHONPATH=src python -c "from block_detection_v2.main import process_frame; import cv2; from block_detection_v2.tracker import MultiTracker; f=cv2.imread('src/block_detection_v2/block_dataset/dt50.jpg'); b,o=process_frame(f, MultiTracker()); assert o['detected'] is True or len(b)>=0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/block_detection_v2/main.py | grep -c '_lines'` equals 0
    - `grep -v '^#' src/block_detection_v2/main.py | grep -c 'fit_hexagon_from_lines'` is at least 1
    - `process_frame` runs without ImportError on dt50.jpg
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frame ndarray → fit.py | Untrusted image buffer from camera/dataset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02 | Denial of Service | fit on huge line lists | accept | Single-frame dev loop; bounded Hough output |
| T-17-SC | Tampering | pip installs | mitigate | No new dependencies; ISO-02 |
</threat_model>

<verification>
- `pytest tests/test_block_detection_v2_fit.py -q` passes
- `main.py` uses `lines` variable and calls `fit_hexagon_from_lines`
</verification>

<success_criteria>
- SPIKE-FIT-01: fit module returns valid A–F on dt50
- SPIKE-FIT-02: main.py passes Hough lines to fitter (no `_lines` discard)
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Description |
|----------|------|-------------|
| `src/block_detection_v2/fit.py` | 17-02 | Dominant-angle line fit + edge snap |
| `tests/test_block_detection_v2_fit.py` | 17-02 | Fit regression on dt50 |
| Interim `main.py` pipeline | 17-02 | ROI → masked edges → fit (score in 17-03) |

<output>
Create `.planning/phases/17-roi-fit-score-detection-pipeline/17-02-SUMMARY.md` when done
</output>
