---
phase: 17-roi-fit-score-detection-pipeline
plan: 04
type: execute
wave: 4
depends_on:
  - 17-03
files_modified:
  - src/block_detection_v2/benchmark.py
  - tests/test_block_detection_v2_benchmark.py
  - .gitignore
autonomous: true
requirements:
  - SPIKE-BENCH-01
must_haves:
  truths:
    - "Benchmark runs dt1–dt108 and writes benchmark.json + overlays"
    - "Accept rate ≥ 80% on full dataset (spike baseline 84%)"
    - "Harness uses production modules — not block_spike_lib"
  artifacts:
    - path: src/block_detection_v2/benchmark.py
      provides: "Dataset regression harness"
      exports: ["run_benchmark", "list_dataset", "process_image"]
    - path: tests/test_block_detection_v2_benchmark.py
      provides: "Accept-rate integration test"
  key_links:
    - from: src/block_detection_v2/benchmark.py
      to: src/block_detection_v2/main.py
      via: "same pipeline as process_frame / shared process_image"
      pattern: "score_candidate|fit_hexagon_from_lines"
    - from: tests/test_block_detection_v2_benchmark.py
      to: src/block_detection_v2/benchmark.py
      via: "run_benchmark(write_overlays=False)"
      pattern: "run_benchmark"
---

<objective>
Port spike 004 benchmark harness into `block_detection_v2.benchmark` with dt1–dt108 overlays, JSON stats, and ≥80% accept gate (D-06, SPIKE-BENCH-01).

Purpose: Regression safety net for every detection change; spike proved 84% vs 25% baseline.
Output: `benchmark.py`, integration test, gitignored overlay output dir.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-roi-fit-score-detection-pipeline/17-CONTEXT.md
@.planning/phases/17-roi-fit-score-detection-pipeline/17-RESEARCH.md
@.cursor/skills/spike-findings-block-detected/references/benchmark-harness.md
@.cursor/skills/spike-findings-block-detected/sources/004-dataset-benchmark/run.py
@.cursor/skills/spike-findings-block-detected/sources/shared/block_spike_lib.py
@src/block_detection_v2/main.py
@src/block_detection_v2/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Benchmark integration test (RED)</name>
  <files>tests/test_block_detection_v2_benchmark.py</files>
  <read_first>
    - .cursor/skills/spike-findings-block-detected/sources/004-dataset-benchmark/run.py
    - .planning/phases/17-roi-fit-score-detection-pipeline/17-CONTEXT.md (D-06)
    - .planning/spikes/004-dataset-benchmark/output/benchmark.json (expected shape, if present)
    - src/block_detection_v2/config.py (IMAGE_DIR / block_dataset)
  </read_first>
  <behavior>
    - test_benchmark_accept_rate: `run_benchmark(write_overlays=False)` returns summary with accept_rate >= 0.80
    - test_list_dataset_count: `list_dataset()` returns 108 paths ending in dt*.jpg
    - test_benchmark_summary_keys: summary contains total, accepted, accept_rate, fail_roi, fail_fit, low_score
  </behavior>
  <action>
    Create `tests/test_block_detection_v2_benchmark.py`.

    Import `run_benchmark`, `list_dataset` from `block_detection_v2.benchmark` (module created in Task 2).

    Accept-rate test may take ~5–10s — acceptable for integration gate.

    Skip test if block_dataset missing fewer than 108 images (pytest.skip with message).

    ISO-01: no v1 imports.
  </action>
  <verify>
    <automated>cd /Users/minhmice/Documents/Projects/block_detected && PYTHONPATH=src pytest tests/test_block_detection_v2_benchmark.py -x 2>&1 | grep -q "ImportError\|ModuleNotFoundError\|FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - Three benchmark tests defined
    - pytest fails before benchmark.py exists
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: benchmark.py harness</name>
  <files>src/block_detection_v2/benchmark.py, .gitignore</files>
  <read_first>
    - .planning/phases/17-roi-fit-score-detection-pipeline/17-PATTERNS.md (benchmark.py section)
    - .cursor/skills/spike-findings-block-detected/sources/004-dataset-benchmark/run.py (full file)
    - .cursor/skills/spike-findings-block-detected/sources/shared/block_spike_lib.py (lines 96-101, 425-456, 458-492)
    - src/block_detection_v2/main.py (production pipeline reference)
    - src/block_detection_v2/preprocessing.py
    - src/block_detection_v2/edges.py
    - src/block_detection_v2/roi.py
    - src/block_detection_v2/fit.py
    - src/block_detection_v2/score.py
  </read_first>
  <action>
    Create `src/block_detection_v2/benchmark.py`:

    - `dataset_dir() -> Path` → package `block_dataset/` beside config
    - `list_dataset() -> List[Path]` — natural sort `dt*.jpg` by numeric stem
    - `process_image(path: Path) -> dict` — extract shared `detect_and_score_frame(color, gray)` helper in this module (or import from a new `pipeline.py` if cleaner) that mirrors `main.process_frame` detection steps without tracker; benchmark and tests call this helper to avoid drift
    - `draw_roi`, `draw_hexagon` — port from block_spike_lib lines 425-456 (benchmark-local helpers)
    - `render_overlay(path, out_dir) -> dict` — write `{stem}_bench.jpg` with ROI tint + hex + status text
    - `run_benchmark(*, write_overlays: bool = True, out_dir: Optional[Path] = None) -> dict` — iterate list_dataset; build summary: total, accepted, accept_rate, fail_roi, fail_fit, low_score, avg_score, avg_hex_area; write `benchmark.json` to out_dir parent; return summary dict
    - `main()` CLI entry; `if __name__ == "__main__": main()`
    - Runnable via `PYTHONPATH=src python -m block_detection_v2.benchmark`

    Add to `.gitignore`: `src/block_detection_v2/benchmark_output/` (or path used for overlays/json)

    Do NOT import from `.cursor/skills/.../block_spike_lib` or `.planning/spikes/` at runtime.

    ISO-01/02: production module isolation maintained.
  </action>
  <verify>
    <automated>cd /Users/minhmice/Documents/Projects/block_detected && PYTHONPATH=src python -m block_detection_v2.benchmark 2>&1 | python -c "import sys,json; d=json.load(sys.stdin); assert d['accept_rate']>=0.80, d"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m block_detection_v2.benchmark` prints JSON summary with accept_rate >= 0.80
    - benchmark.json and overlays written under benchmark_output/
    - fail_roi and fail_fit both 0 on full dataset
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify full test suite + phase requirements</name>
  <files>tests/test_block_detection_v2_benchmark.py</files>
  <read_first>
    - tests/test_block_detection_v2_roi.py
    - tests/test_block_detection_v2_fit.py
    - tests/test_block_detection_v2_score.py
    - .planning/REQUIREMENTS.md (SPIKE-BENCH-01)
  </read_first>
  <action>
    Run full phase 17 test suite together:
    `pytest tests/test_block_detection_v2_roi.py tests/test_block_detection_v2_fit.py tests/test_block_detection_v2_score.py tests/test_block_detection_v2_benchmark.py -q`

    Fix any import or path issues surfaced by combined run.

    Confirm `run_benchmark(write_overlays=False)` still meets accept_rate >= 0.80 (faster CI path).
  </action>
  <verify>
    <automated>cd /Users/minhmice/Documents/Projects/block_detected && PYTHONPATH=src pytest tests/test_block_detection_v2_roi.py tests/test_block_detection_v2_fit.py tests/test_block_detection_v2_score.py tests/test_block_detection_v2_benchmark.py -q --tb=no</automated>
  </verify>
  <acceptance_criteria>
    - All phase 17 tests pass in single pytest invocation
    - `test_benchmark_accept_rate` asserts accept_rate >= 0.80
    - No imports from block_detected v1 in new modules
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark writes local JPG/JSON | Filesystem output only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Information Disclosure | benchmark.json paths | accept | Local dev artifact; gitignored overlays |
| T-17-SC | Tampering | pip installs | mitigate | ISO-02; no new packages |
</threat_model>

<verification>
- `python -m block_detection_v2.benchmark` accept_rate ≥ 0.80
- Full pytest suite for phase 17 green
- 108 overlays generated when write_overlays=True
</verification>

<success_criteria>
- SPIKE-BENCH-01: harness on dt1–dt108 with overlays + JSON
- Accept rate ≥ 80% (target 84% per spike)
- Production modules only — spike lib not a runtime dependency
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Description |
|----------|------|-------------|
| `src/block_detection_v2/benchmark.py` | 17-04 | `run_benchmark`, `process_image`, CLI |
| `tests/test_block_detection_v2_benchmark.py` | 17-04 | Accept-rate ≥80% gate |
| `benchmark_output/` (gitignored) | 17-04 | overlays + benchmark.json |
| `.gitignore` entry | 17-04 | Exclude benchmark output from git |

<output>
Create `.planning/phases/17-roi-fit-score-detection-pipeline/17-04-SUMMARY.md` when done
</output>
