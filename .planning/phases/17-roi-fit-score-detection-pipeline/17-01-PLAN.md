---
phase: 17-roi-fit-score-detection-pipeline
plan: 01
type: execute
wave: 1
depends_on:
  - 16-01
files_modified:
  - src/block_detection_v2/roi.py
  - src/block_detection_v2/config.py
  - tests/test_block_detection_v2_roi.py
autonomous: true
requirements:
  - SPIKE-ROI-01
  - SPIKE-ROI-02
must_haves:
  truths:
    - "extract_cluster_roi isolates block cluster from edges on dt50 and dt1"
    - "3-block mode trims ~22% from ROI right edge per D-01"
    - "ROI constants live in config.py (no magic pallet/trim literals in roi.py)"
  artifacts:
    - path: src/block_detection_v2/roi.py
      provides: "Cluster ROI extraction"
      exports: ["extract_cluster_roi", "ROIBox"]
    - path: src/block_detection_v2/config.py
      provides: "ROI threshold constants"
      contains: "BLOCK_MODE"
    - path: tests/test_block_detection_v2_roi.py
      provides: "ROI unit tests on dataset images"
  key_links:
    - from: src/block_detection_v2/roi.py
      to: src/block_detection_v2/config.py
      via: "BLOCK_MODE, ROI_PALLET_FRAC, ROI_RIGHT_TRIM_FRAC"
      pattern: "config\\.ROI_"
    - from: tests/test_block_detection_v2_roi.py
      to: src/block_detection_v2/roi.py
      via: "preprocess → detect_edges → extract_cluster_roi"
      pattern: "extract_cluster_roi"
---

<objective>
Port spike-validated `extract_cluster_roi()` into production `roi.py` with config-driven 3-block silhouette trimming (D-01, SPIKE-ROI-01/02).

Purpose: ROI must run before hex fit — full-frame contour contest is the baseline bug.
Output: `roi.py`, ROI config constants, passing ROI unit tests on `block_dataset/`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-roi-fit-score-detection-pipeline/17-CONTEXT.md
@.planning/phases/17-roi-fit-score-detection-pipeline/17-RESEARCH.md
@.planning/phases/17-roi-fit-score-detection-pipeline/17-PATTERNS.md
@.cursor/skills/spike-findings-block-detected/SKILL.md
@.cursor/skills/spike-findings-block-detected/references/roi-silhouette.md
@.cursor/skills/spike-findings-block-detected/sources/shared/block_spike_lib.py
@src/block_detection_v2/config.py
@src/block_detection_v2/edges.py
@src/block_detection_v2/preprocessing.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ROI unit tests (RED)</name>
  <files>tests/test_block_detection_v2_roi.py</files>
  <read_first>
    - tests/conftest.py
    - src/block_detection_v2/preprocessing.py
    - src/block_detection_v2/edges.py
    - src/block_detection_v2/config.py
    - .cursor/skills/spike-findings-block-detected/sources/shared/block_spike_lib.py (lines 124-196)
    - src/block_detection_v2/block_dataset/dt50.jpg
    - src/block_detection_v2/block_dataset/dt1.jpg
  </read_first>
  <behavior>
    - test_extract_cluster_roi_dt50: ROI not None, mask.sum() > 0, area > 800
    - test_extract_cluster_roi_dt1: ROI not None, block_mode == config.BLOCK_MODE
    - test_three_block_right_trim: ROI width &lt; full component bbox width (trim applied when block_mode=3)
  </behavior>
  <action>
    Create `tests/test_block_detection_v2_roi.py` importing `preprocess`, `detect_edges`, and (once exists) `extract_cluster_roi`.

    Image paths: `Path(__file__).resolve().parents[1] / "src/block_detection_v2/block_dataset/dt50.jpg"` (same pattern for dt1).

  Per ISO-01: imports only from `block_detection_v2` — no v1 packages.

    Run pytest — tests MUST fail (module missing).
  </action>
  <verify>
    <automated>cd /Users/minhmice/Documents/Projects/block_detected && PYTHONPATH=src pytest tests/test_block_detection_v2_roi.py -x 2>&1 | grep -q "ImportError\|ModuleNotFoundError\|FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists with three test functions named above
    - pytest collects tests and fails before implementation
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: roi.py + config ROI constants</name>
  <files>src/block_detection_v2/roi.py, src/block_detection_v2/config.py</files>
  <read_first>
    - .cursor/skills/spike-findings-block-detected/sources/shared/block_spike_lib.py (lines 30-41, 124-196)
    - src/block_detection_v2/edges.py (import header pattern)
    - .planning/phases/17-roi-fit-score-detection-pipeline/17-CONTEXT.md (D-01)
    - .planning/phases/17-roi-fit-score-detection-pipeline/17-PATTERNS.md (roi.py section)
  </read_first>
  <action>
    Add to `config.py` (per RESEARCH + D-01):
    - `BLOCK_MODE = 3`
    - `ROI_PALLET_FRAC = 0.78`
    - `ROI_RIGHT_TRIM_FRAC = 0.22`

    Create `src/block_detection_v2/roi.py`:
    - `@dataclass class ROIBox` with fields x, y, w, h, mask, area, block_mode; method `as_tuple()`
    - `extract_cluster_roi(edges, frame_shape, *, block_mode=None, pallet_frac=None) -> Optional[ROIBox]`
    - Port algorithm from `block_spike_lib.py` lines 124-196 verbatim in behavior:
      pallet mask at `int(h * pallet_frac)`; morph 9×9 close×2 + dilate×2; CC scoring `area * (0.5 + 0.3*centrality + 0.2*upper_bonus)`; min area 800; reject cy > h*0.88; pad `max(12, int(min(bw,bh)*0.04))`
    - Per D-01 / SPIKE-ROI-02: when `block_mode == 3`, trim right edge by `int(full_w * config.ROI_RIGHT_TRIM_FRAC)` with `x1 = max(x0 + int(full_w * 0.45), x1 - trim)`
    - Default `block_mode` and `pallet_frac` from config when args omitted
    - Return `None` on failure (no exceptions); omit `ForensicLog` from production
    - ISO-01/02: only `cv2`, `numpy`, and `block_detection_v2` imports

    Do NOT add fit/score/benchmark code in this task.
  </action>
  <verify>
    <automated>cd /Users/minhmice/Documents/Projects/block_detected && PYTHONPATH=src pytest tests/test_block_detection_v2_roi.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `from block_detection_v2.roi import extract_cluster_roi, ROIBox` succeeds
    - All three ROI tests pass
    - `grep -v '^#' src/block_detection_v2/roi.py | grep -c block_detected` equals 0 (no v1 imports)
    - `grep -v '^#' src/block_detection_v2/config.py | grep -c ROI_RIGHT_TRIM_FRAC` equals 1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| block_dataset images → roi.py | Untrusted file input; read-only OpenCV imread in tests only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | block_dataset paths in tests | accept | Local dev dataset only; no network |
| T-17-SC | Tampering | pip/cv2 imports | mitigate | ISO-02: OpenCV+NumPy only; no new pip packages |
</threat_model>

<verification>
- `PYTHONPATH=src pytest tests/test_block_detection_v2_roi.py -q` all green
- Manual: `python -c "from block_detection_v2.roi import extract_cluster_roi"` from repo root with PYTHONPATH=src
</verification>

<success_criteria>
- SPIKE-ROI-01 and SPIKE-ROI-02 satisfied with config-driven 3-block ROI on dt1/dt50
- ROI module isolated under `block_detection_v2` per ISO-01
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Description |
|----------|------|-------------|
| `src/block_detection_v2/roi.py` | 17-01 | `extract_cluster_roi`, `ROIBox` |
| ROI config constants in `config.py` | 17-01 | `BLOCK_MODE`, `ROI_PALLET_FRAC`, `ROI_RIGHT_TRIM_FRAC` |
| `tests/test_block_detection_v2_roi.py` | 17-01 | Dataset ROI regression tests |

<output>
Create `.planning/phases/17-roi-fit-score-detection-pipeline/17-01-SUMMARY.md` when done
</output>
