---
phase: 09-next-js-fastapi-detection-console-ui-with-websocket-telemetr
plan: 02
type: execute
wave: 1
depends_on: ["09-01"]
files_modified:
  - backend/app/main.py
  - backend/app/schemas/wire.py
  - backend/app/schemas/__init__.py
  - backend/app/services/wire_builder.py
  - backend/app/services/frame_source_factory.py
  - backend/app/routes/health.py
  - backend/app/__init__.py
autonomous: true
requirements:
  - UI-09-08
  - UI-09-05
must_haves:
  truths:
    - "FastAPI app boots with CORS from CORS_ORIGINS env"
    - "GET /health returns camelCase SystemStatus JSON"
    - "MOCK_CAMERA=true selects ImageSequenceFrameSource without opening USB camera"
    - "Wire builder maps DetectionResult contract to camelCase detection object"
  artifacts:
    - path: backend/app/schemas/wire.py
      provides: "Pydantic v2 camelCase wire models"
      exports: ["DetectionResultWire", "DetectionTelemetryWire", "ClassificationScoresWire"]
    - path: backend/app/services/wire_builder.py
      provides: "Contract → wire conversion"
      pattern: "from_contract"
    - path: backend/app/routes/health.py
      provides: "/health endpoint"
  key_links:
    - from: backend/app/routes/health.py
      to: backend/app/schemas/wire.py
      via: "response_model_by_alias=True"
      pattern: "SystemStatusWire"
    - from: backend/app/services/frame_source_factory.py
      to: src/block_detected/camera.py
      via: "create_frame_source / ImageSequenceFrameSource"
      pattern: "ImageSequenceFrameSource"
---

<objective>
Bootstrap FastAPI backend: Pydantic wire schemas, contract→camelCase builder, mock frame-source factory, CORS, and `/health`.

Purpose: Stable API contract for Next.js scaffold and remaining backend routes (Plan 03).
Output: Runnable `uvicorn app.main:app` with health + factory tests unskipped.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-01-SUMMARY.md
@src/block_detected/detection_contract.py
@src/block_detected/camera.py
@config/camera.example.json
@tests/fixtures/wire/detection_success.json

<interfaces>
From `detection_contract.py`:
```python
@dataclass(frozen=True)
class DetectionResult:
    block_id: Optional[BlockID]
    confidence: float
    center_px: Optional[PointPx]
    corners_px: Optional[CornersPx]
    angle_deg: Optional[float]
    pickup_pose: Optional[PickupPose]
    status: DetectionStatus
    debug: Optional[DebugInfo] = None

def result_to_dict(result: DetectionResult) -> Dict[str, Any]: ...
```

From `camera.py`:
```python
def create_frame_source(settings: CameraSettings) -> FrameSource: ...
class ImageSequenceFrameSource: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" validation_task_id="09-01-01">
  <name>Task 1: Pydantic wire schemas and wire builder</name>
  <read_first>
    - `tests/fixtures/wire/detection_success.json` → exact key names for tests
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-RESEARCH.md` → Pattern 8 Pydantic camelCase, explicit pickupPoseMm aliases
    - `src/block_detected/detection_contract.py` → CornersPx uses top_left not tl internally
  </read_first>
  <files>backend/app/schemas/wire.py, backend/app/schemas/__init__.py, backend/app/services/wire_builder.py</files>
  <behavior>
    - `build_telemetry_from_contract(ok_result)` produces JSON matching golden keys when serialized with `model_dump(by_alias=True)`
    - `valid` is True iff `status == "ok"`; `rejectReason` from `debug.rejection_reason` when invalid
    - `classification_scores_from_classifier(None)` returns equal 0.25 splits with `# TODO: wire classifier softmax when exposed` comment
  </behavior>
  <action>
    Create `backend/app/` package with `schemas/wire.py` using Pydantic v2 `ConfigDict(alias_generator=to_camel, populate_by_name=True)`.
    Models (minimum): `PointWire`, `CornersWire` (fields `tl`,`tr`,`br`,`bl`), `PickupPoseWire` with explicit `serialization_alias` for `xMm`,`yMm`,`thetaDeg` under parent `pickupPoseMm`, `DetectionResultWire`, `ClassificationScoresWire` (`block01`..`block04` floats 0–1), `DetectionTelemetryWire` (envelope matching golden fixture), `DetectionParamsWire` (bounds: blur 1–15, canny 0–255, areas 0–200000, confidence 0–1, aspect 0–1), `SystemStatusWire` (`status`, `mockCamera`, `detectionRunning`, `cameraBackend`).
    Implement `wire_builder.py`:
    - Map `CornersPx.top_left` → `tl`, etc.
    - Do **not** modify `detection_contract.py` for camelCase.
    - `classification_scores_from_classifier(scores: dict[str,float] | None)`: if None, return mock `{block01:0.25,...}` per open question (research); if dict provided, normalize keys to block01..block04.
    Add `tests/test_wire_schema.py` extension OR new `tests/test_wire_builder.py` with `test_builder_matches_golden_structure` comparing keys to fixture (values may differ slightly).
  </action>
  <verify>
    <automated>pytest tests/test_wire_schema.py tests/test_wire_builder.py -x 2>/dev/null || pytest tests/test_wire_schema.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'class DetectionTelemetryWire' backend/app/schemas/wire.py` succeeds
    - `grep -q 'pickupPoseMm' backend/app/schemas/wire.py` succeeds
    - `grep -q 'classification_scores_from_classifier' backend/app/services/wire_builder.py` succeeds
  </acceptance_criteria>
  <done>Wire models and builder convert contract results to camelCase envelope</done>
</task>

<task type="auto" tdd="true" validation_task_id="09-02-03">
  <name>Task 2: Frame source factory and mock mode</name>
  <read_first>
    - `config/camera.example.json` → image_sequence profile paths
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-CONTEXT.md` → Mock mode D-12
    - `src/block_detected/camera.py` → `create_frame_source`, `CameraSettings` loading pattern
  </read_first>
  <files>backend/app/services/frame_source_factory.py</files>
  <behavior>
    - `MOCK_CAMERA=true` forces `image_sequence` backend using `CAMERA_CONFIG` path (default `config/camera.example.json`)
    - `test_mock_camera_uses_image_sequence` PASS: backend is `image_sequence`, not `usb`
  </behavior>
  <action>
    Implement `create_frame_source_from_env()`:
    - Read `MOCK_CAMERA` (truthy: 1,true,yes) OR `DETECTION_MODE=mock` → use image_sequence profile from JSON config.
    - Else load active profile from `CAMERA_CONFIG` same as `camera.example.json` structure.
    - Add `# TODO: mock telemetry injection` comment at factory when mock (for Plan 03 loop).
    Unskip and implement `tests/test_frame_source_factory.py::test_mock_camera_uses_image_sequence` using factory only (no uvicorn).
  </action>
  <verify>
    <automated>pytest tests/test_frame_source_factory.py::test_mock_camera_uses_image_sequence -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'MOCK_CAMERA' backend/app/services/frame_source_factory.py` succeeds
    - `grep -q 'image_sequence' backend/app/services/frame_source_factory.py` succeeds
    - `pytest tests/test_frame_source_factory.py::test_mock_camera_uses_image_sequence -x` passes
  </acceptance_criteria>
  <done>Mock camera path uses existing ImageSequenceFrameSource</done>
</task>

<task type="auto" tdd="true" validation_task_id="09-01-02">
  <name>Task 3: FastAPI main, CORS, health route</name>
  <read_first>
    - `.env.example` → CORS_ORIGINS, PORT
    - `tests/test_api_health.py` → test_health_returns_200 expectations
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-RESEARCH.md` → Lifespan stub, CORSMiddleware
  </read_first>
  <files>backend/app/main.py, backend/app/routes/health.py, backend/app/__init__.py</files>
  <action>
    Create `backend/app/main.py`:
    - `FastAPI(title="block_detected console")` with empty lifespan (start/stop loop deferred to Plan 03).
    - `CORSMiddleware`: `allow_origins=os.getenv("CORS_ORIGINS","http://localhost:3000").split(",")`, `allow_credentials=True`, `allow_methods=["*"]`, `allow_headers=["*"]` — **no wildcard origin** in code default.
    - Include `health` router.
    `routes/health.py`: `GET /health` returns `SystemStatusWire` with `status="ok"`, `mockCamera` from env, `detectionRunning=false`, `cameraBackend` string from factory preview (do not start camera on health).
    Unskip `tests/test_api_health.py::test_health_returns_200` using `httpx.AsyncClient` + `ASGITransport(app=...)`, assert status 200 and response JSON contains camelCase key `mockCamera`.
    Document in `backend/app/main.py` module docstring: run from `backend/` with `PYTHONPATH=.` or install layout `app` package.
  </action>
  <verify>
    <automated>pytest tests/test_api_health.py::test_health_returns_200 -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'CORSMiddleware' backend/app/main.py` succeeds
    - `grep -q 'mockCamera' backend/app/routes/health.py` succeeds
    - `pytest tests/test_api_health.py::test_health_returns_200 -x` passes
  </acceptance_criteria>
  <done>Health endpoint green; CORS driven by env list</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → FastAPI | Cross-origin REST; untrusted JSON on future POST routes |
| env → frame source | CAMERA_CONFIG path selects filesystem images |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | DetectionParamsWire | mitigate | Field ge/le constraints on all numeric params in schema |
| T-09-03 | Spoofing | CORS | mitigate | Split `CORS_ORIGINS`; reject `*` in production README |
| T-09-04 | Information Disclosure | /health | accept | Exposes mock/backend mode only; no secrets |
</threat_model>

<verification>
- `pytest tests/test_wire_schema.py tests/test_api_health.py tests/test_frame_source_factory.py -x`
- `grep -q CORSMiddleware backend/app/main.py`
</verification>

<success_criteria>
- UI-09-08 wire builder testable; UI-09-05 mock factory proven
- `/health` returns 200 with camelCase JSON
</success_criteria>

<output>
After completion, create `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-02-SUMMARY.md`
</output>
