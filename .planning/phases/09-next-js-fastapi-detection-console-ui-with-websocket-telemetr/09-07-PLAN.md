---
phase: 09-next-js-fastapi-detection-console-ui-with-websocket-telemetr
plan: 07
type: execute
wave: 5
depends_on: ["09-03", "09-05", "09-06"]
files_modified:
  - docker-compose.yml
  - backend/Dockerfile
  - frontend/Dockerfile
  - README.md
  - .planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-VALIDATION.md
autonomous: false
requirements:
  - UI-09-06
  - UI-09-07
must_haves:
  truths:
    - "docker compose config validates; backend and frontend services define healthchecks"
    - "README documents dev:all, mock camera, Pi deploy, env vars"
    - "Operator can verify MJPEG + WS + overlay in browser (checkpoint)"
    - "example_ui/ remains untouched"
  artifacts:
    - path: docker-compose.yml
      provides: "frontend + backend services"
      contains: "healthcheck"
    - path: README.md
      provides: "Console UI section"
      contains: "MOCK_CAMERA"
  key_links:
    - from: docker-compose.yml
      to: backend/Dockerfile
      via: "build context ./backend"
      pattern: "8000:8000"
---

<objective>
Deliver Docker Compose deployment, README operator docs, validation sign-off, and human UAT checkpoint for the full console stack.

Purpose: Close UI-09-06/07 and phase gate; nginx deferred per research (comment-only optional profile).
Output: `docker compose config` clean; README section; 09-VALIDATION.md nyquist_compliant true.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-RESEARCH.md
@.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-VALIDATION.md
@.env.example
@package.json

</context>

<tasks>

<task type="auto" validation_task_id="09-04-01">
  <name>Task 1: Dockerfiles and compose with healthchecks</name>
  <read_first>
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-RESEARCH.md` → Pattern 7 Docker Compose
    - `backend/requirements.txt`
    - Open question: **nginx deferred** — add commented optional profile only, not enabled service
  </read_first>
  <files>docker-compose.yml, backend/Dockerfile, frontend/Dockerfile</files>
  <action>
    `backend/Dockerfile`: Python 3.11 slim; copy `backend/`, `src/`, `config/`, `tests/fixtures/`; `pip install -r requirements.txt && pip install -e .` from repo root context; `CMD uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 1`; env `MOCK_CAMERA=true` default in compose.
    `frontend/Dockerfile`: node 22 alpine; build Next standalone if configured else `npm run build && npm start`; build-args for `NEXT_PUBLIC_*` pointing to `http://backend:8000` **for container build** — document browser-on-host uses `.env` with localhost (client-side only).
    `docker-compose.yml` services:
    - `backend` ports 8000, healthcheck `curl -f http://localhost:8000/health`, volumes ro config+fixtures.
    - `frontend` ports 3000, depends_on backend healthy, healthcheck `curl -f http://localhost:3000`.
    - Comment block at bottom:
      `# Optional nginx reverse proxy (not enabled v1): docker compose --profile nginx up`
      `# profiles: nginx — image nginx:alpine, proxy_buffering off for MJPEG`
    Do **not** add active nginx service without profile.
  </action>
  <verify>
    <automated>docker compose config -q && docker compose up --build --wait --timeout 120 2>&1 | tail -5 || echo "compose-smoke-deferred-to-uat"</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config -q` exits 0
    - `grep -q healthcheck docker-compose.yml` succeeds
    - `grep -q 'MOCK_CAMERA' docker-compose.yml` succeeds
    - `grep -q 'nginx' docker-compose.yml` succeeds (comment or profile only)
  </acceptance_criteria>
  <done>Compose file validates; healthchecks defined for both services</done>
</task>

<task type="auto">
  <name>Task 2: README console section and validation doc update</name>
  <read_first>
    - `.env.example`
    - `package.json` dev:all script
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-VALIDATION.md`
  </read_first>
  <files>README.md, .planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-VALIDATION.md</files>
  <action>
    Create or extend `README.md` with **Detection Console UI** section:
    1. Prereqs: Node 20+, Python 3.11 venv, `pip install -e ".[dev]"` + backend requirements.
    2. Copy `.env.example` → `.env`; set `MOCK_CAMERA=true` for Mac dev.
    3. `npm install` at repo root; `cd backend && python -m venv .venv && pip install -r requirements.txt && pip install -e ..`
    4. `npm run dev:all` → open http://localhost:3000
    5. Docker: `docker compose up --build`
    6. Pi deploy notes: USB camera, `MOCK_CAMERA=false`, tflite model path, picamera2 apt.
    7. Explicit: `example_ui/` is reference-only — do not delete.
    Update `09-VALIDATION.md` frontmatter: `nyquist_compliant: true`, `wave_0_complete: true`, mark automated rows ✅ where tests now exist.
    Confirm `example_ui/` git status unchanged (no deletes).
  </action>
  <verify>
    <automated>pytest tests/ -q --tb=no 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'dev:all' README.md` succeeds
    - `grep -q 'MOCK_CAMERA' README.md` succeeds
    - `grep -q 'example_ui' README.md` succeeds
    - `test -d example_ui/real_time_detection_console_v2` succeeds
    - `pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>README documents local/docker/mock; full pytest green; validation doc updated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser integration UAT</name>
  <read_first>
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-VALIDATION.md` → Manual-Only Verifications
  </read_first>
  <files>README.md</files>
  <action>Human verifies end-to-end console per steps below after Plans 01–06 are executed. No code changes unless UAT finds defects.</action>
  <verify>
    <automated>pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - MJPEG feed visible in camera panel at http://localhost:3000 after `npm run dev:all`
    - TopStatusBar shows FPS greater than 0 when detection loop running in mock mode
    - VisionOverlay corners align with block geometry at 1366×768 viewport width
    - Stopping backend shows CONNECTION ERROR in TopStatusBar; restart shows reconnect log in LogTerminal
    - Moving a detection slider produces no API error lines in LogTerminal within 500ms
    - Sidebar and 8/4 column layout render without horizontal scroll at 1366×768
  </acceptance_criteria>
  <done>Operator confirms MJPEG, overlay alignment, WS reconnect, and 1366×768 layout; types "approved" or files issues</done>
  <what-built>Full stack: FastAPI MJPEG + WebSocket + Next.js console with overlay and panels</what-built>
  <how-to-verify>
    1. Copy `.env.example` to `.env`; set `MOCK_CAMERA=true`.
    2. Start `npm run dev:all`; wait for backend :8000 and frontend :3000.
    3. Open http://localhost:3000 — confirm MJPEG feed visible in camera panel.
    4. Click `RUN_DETECTION` (or confirm auto-started in mock) — TopStatusBar FPS &gt; 0, overlay draws geometry on valid frames.
    5. Move a detection slider — after ~400ms no errors in LogTerminal `API` lines.
    6. Toggle WebSocket off (stop backend) — UI shows `CONNECTION ERROR`; restart — reconnect log line appears.
    7. At 1366×768 window width, confirm sidebar + 8/4 column layout without horizontal scroll.
  </how-to-verify>
  <resume-signal>Type "approved" or describe overlay/WS issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker network | frontend/backend inter-container |
| Host browser | connects to published ports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Spoofing | compose CORS | mitigate | README: set CORS_ORIGINS to actual frontend origin |
| T-09-08 | Information Disclosure | Docker images | accept | Dev console; no secrets in image layers |
| T-09-02 | Elevation | dataset volume mount | mitigate | Compose mounts dataset dir only; backend path confinement from Plan 03 |
</threat_model>

<verification>
- `docker compose config -q`
- `pytest tests/ -q`
- Human checkpoint Task 3 approved
</verification>

<success_criteria>
- UI-09-06 compose validates
- UI-09-07 dev:all documented
- Manual overlay + reconnect UAT approved
- `example_ui/` preserved
</success_criteria>

<output>
After completion, create `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-07-SUMMARY.md`
</output>
