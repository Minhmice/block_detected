---
phase: 09-next-js-fastapi-detection-console-ui-with-websocket-telemetr
plan: 06
type: execute
wave: 4
depends_on: ["09-04"]
files_modified:
  - frontend/components/ClassificationPanel.tsx
  - frontend/components/PickupTelemetry.tsx
  - frontend/components/CalibrationPanel.tsx
  - frontend/components/DatasetPanel.tsx
  - frontend/components/LogTerminal.tsx
  - frontend/app/calibration/page.tsx
  - frontend/app/datasets/page.tsx
  - frontend/app/analysis/page.tsx
  - frontend/app/system/page.tsx
autonomous: true
requirements:
  - UI-09-04
must_haves:
  truths:
    - "Classification panel shows block id, confidence, 4-class bars from classificationScores"
    - "Pickup telemetry mirrors latest detection fields in monospace"
    - "Calibration and dataset routes call save APIs"
    - "Log terminal appends WS/API events with 500 line cap"
  artifacts:
    - path: frontend/components/ClassificationPanel.tsx
      provides: "4-class score bars"
    - path: frontend/components/LogTerminal.tsx
      provides: "append-only terminal"
      pattern: "500"
  key_links:
    - from: frontend/components/CalibrationPanel.tsx
      to: frontend/lib/api.ts
      via: "postCalibrationSave"
      pattern: "postCalibrationSave"
---

<objective>
Implement remaining UI-SPEC components: ClassificationPanel, PickupTelemetry, CalibrationPanel, DatasetPanel, LogTerminal, and route pages.

Purpose: Complete component architecture (5 of 11 remaining after Plan 05) and secondary routes.
Output: All 11 named components exist under `frontend/components/`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-UI-SPEC.md
@config/calibration.example.json
@frontend/stores/useVisionStore.ts

</context>

<tasks>

<task type="auto">
  <name>Task 1: ClassificationPanel and PickupTelemetry</name>
  <read_first>
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-UI-SPEC.md` → ClassificationPanel, PickupTelemetry
    - `tests/fixtures/wire/detection_success.json` → classificationScores shape
  </read_first>
  <files>frontend/components/ClassificationPanel.tsx, frontend/components/PickupTelemetry.tsx, frontend/app/page.tsx</files>
  <action>
    `ClassificationPanel`: 128×128 warp placeholder box; bind `latestResult.detection`; show `BLOCK_0${blockId}`; confidence % with secondary/warning color by threshold 0.55; render 4 bars from `classificationScores` when present else `SCORES PENDING` label (no bars).
    Badge: `MOCK` when `detectionMode==='mock'`, else `CNN_LOADED` when connected.
    `PickupTelemetry`: monospace scroll area; print lines per UI-SPEC format; `> REJECTED` + rejectReason when invalid; `SEND TO ROBOT` disabled when `!valid || disconnected`; on click append log `> DRY-RUN: pose logged` (v1 no robot POST).
    Add both to camera column bottom row `h-64` grid in `page.tsx`.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && grep -q 'ClassificationPanel' frontend/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'classificationScores' frontend/components/ClassificationPanel.tsx` succeeds
    - `grep -q 'PICKUP_TELEMETRY' frontend/components/PickupTelemetry.tsx` succeeds
    - `grep -q 'DRY-RUN' frontend/components/PickupTelemetry.tsx` succeeds
    - `ls frontend/components/ClassificationPanel.tsx frontend/components/PickupTelemetry.tsx` succeeds
  </acceptance_criteria>
  <done>Telemetry row components on home page</done>
</task>

<task type="auto">
  <name>Task 2: CalibrationPanel, DatasetPanel, routes</name>
  <read_first>
    - `config/calibration.example.json` → field structure for form
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-UI-SPEC.md` → CalibrationPanel, DatasetPanel
  </read_first>
  <files>frontend/components/CalibrationPanel.tsx, frontend/components/DatasetPanel.tsx, frontend/app/calibration/page.tsx, frontend/app/datasets/page.tsx, frontend/app/analysis/page.tsx, frontend/app/system/page.tsx</files>
  <action>
    `CalibrationPanel`: route `/calibration` replaces params column content; LOAD FROM FILE via `<input type="file">` JSON parse client-side preview; SAVE → `postCalibrationSave(parsed)`.
    `DatasetPanel`: route `/datasets`; list last 10 saves from Zustand `datasetEntries[]` (add slice in store if missing); Quick capture + Save Failed Frame → `postDatasetSaveFrame`; empty copy `NO FRAMES SAVED THIS SESSION`.
    `/analysis`: empty state component "Analysis deferred — v2".
    `/system`: display `getHealth()` fields in data-md list (poll on mount).
    Each page uses `AppShell` with same sidebar active state.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && test -f frontend/app/calibration/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'postCalibrationSave' frontend/components/CalibrationPanel.tsx` succeeds
    - `grep -q 'postDatasetSaveFrame' frontend/components/DatasetPanel.tsx` succeeds
    - `test -f frontend/app/calibration/page.tsx` succeeds
    - `test -f frontend/app/datasets/page.tsx` succeeds
  </acceptance_criteria>
  <done>Secondary routes and panels wired to REST client</done>
</task>

<task type="auto">
  <name>Task 3: LogTerminal drawer</name>
  <read_first>
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-UI-SPEC.md` → LogTerminal
    - `frontend/lib/ws.ts` → ensure connection events log via store
  </read_first>
  <files>frontend/components/LogTerminal.tsx, frontend/components/TopStatusBar.tsx, frontend/stores/useVisionStore.ts</files>
  <action>
    `LogTerminal`: slide-over `w-96` or bottom `h-48` on `<lg`; toggle from TopStatusBar terminal icon; autoscroll unless user scrolled up; line color prefixes per UI-SPEC; trim logs to 500 lines in store.
    Ensure `ws.ts` and api.ts append `WS` / `API` prefixed lines on success/failure.
    Empty state: `> Log terminal ready — waiting for events`.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && grep -q 'LogTerminal' frontend/components/TopStatusBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'logTerminalOpen' frontend/stores/useVisionStore.ts` succeeds
    - `grep -q 'LogTerminal' frontend/components/TopStatusBar.tsx` succeeds
    - `ls frontend/components/*.tsx | wc -l` reports at least 11 component files (AppShell through LogTerminal)
  </acceptance_criteria>
  <done>All 11 UI-SPEC components exist; log drawer toggles from top bar</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file picker → calibration JSON | Client parses JSON before POST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Tampering | client calibration file | transfer | Backend validates calibration schema on save (Plan 03) |
| T-09-06 | Tampering | log injection | accept | Logs are operator-local display only |
</threat_model>

<verification>
- `cd frontend && npm run lint && npx tsc --noEmit`
- `ls frontend/components/{AppShell,Sidebar,TopStatusBar,CameraViewport,VisionOverlay,DetectionControls,ClassificationPanel,PickupTelemetry,CalibrationPanel,DatasetPanel,LogTerminal}.tsx`
</verification>

<success_criteria>
- All 11 components from CONTEXT exist as files
- UI-09-04 no dead buttons on calibration/dataset/save paths
</success_criteria>

<output>
After completion, create `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-06-SUMMARY.md`
</output>
