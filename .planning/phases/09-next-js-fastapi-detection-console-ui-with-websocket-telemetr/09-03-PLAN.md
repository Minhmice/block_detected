---
phase: 09-next-js-fastapi-detection-console-ui-with-websocket-telemetr
plan: 03
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - backend/app/main.py
  - backend/app/services/detection_loop.py
  - backend/app/ws/manager.py
  - backend/app/routes/stream.py
  - backend/app/routes/detection.py
  - backend/app/routes/calibration.py
  - backend/app/routes/dataset.py
  - backend/app/routes/__init__.py
autonomous: true
requirements:
  - UI-09-01
  - UI-09-02
  - UI-09-04
  - UI-09-05
  - CONT-01
must_haves:
  truths:
    - "Single detection loop owns camera; MJPEG and WebSocket read shared latest frame/telemetry"
    - "POST /api/detection/start and /stop control loop; params POST validates with Pydantic"
    - "When MOCK_CAMERA=true or DETECTION_MODE=mock, detection loop auto-starts on app lifespan; otherwise idle until POST /api/detection/start"
    - "POST /api/dataset/save-frame writes only under configured DATASET_DIR"
  artifacts:
    - path: backend/app/services/detection_loop.py
      provides: "capture + detect_block + publish"
      pattern: "asyncio.to_thread"
    - path: backend/app/routes/stream.py
      provides: "GET /video/stream MJPEG"
    - path: backend/app/ws/manager.py
      provides: "WebSocket broadcast"
      pattern: "broadcast_json"
  key_links:
    - from: backend/app/services/detection_loop.py
      to: src/block_detected/pipeline.py
      via: "detect_block(frame, settings)"
      pattern: "detect_block"
    - from: backend/app/routes/stream.py
      to: backend/app/services/detection_loop.py
      via: "latest_jpeg()"
      pattern: "multipart/x-mixed-replace"
    - from: backend/app/ws/manager.py
      to: backend/app/services/detection_loop.py
      via: "broadcast_json(telemetry)"
      pattern: "model_dump\\(by_alias=True\\)"
---

<objective>
Implement FastAPI detection loop, MJPEG stream, WebSocket telemetry, and REST control endpoints wrapping `detect_block` without rewriting pipeline logic.

Purpose: Satisfy UI-09-01/02/04/05 and CONT-01 regression via existing pipeline.
Output: Full backend I/O surface per CONTEXT API table.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-02-SUMMARY.md
@src/block_detected/pipeline.py
@config/vision.example.json
@config/calibration.example.json

<interfaces>
From `pipeline.py`:
```python
class PipelineSettings: ...
def detect_block(frame, settings: PipelineSettings | None = None) -> DetectionResult: ...
```

Capture frame: `CaptureFrame.image_bgr` shape `(480, 640, 3)` uint8.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" validation_task_id="09-03-01">
  <name>Task 1: DetectionLoopService and ConnectionManager</name>
  <read_first>
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-RESEARCH.md` → Pattern 1 producer-consumer, Pattern 3 WS manager, Pitfall 1 single camera
    - `src/block_detected/pipeline.py` → PipelineSettings fields
    - `backend/app/services/wire_builder.py` → telemetry builder
  </read_first>
  <files>backend/app/services/detection_loop.py, backend/app/ws/manager.py</files>
  <action>
    `DetectionLoopService` (singleton on app.state):
    - `start()` / `stop()` idempotent; `_running` flag.
    - Loop: `create_frame_source_from_env()` → `start()` → while running: `read()` frame → `await asyncio.to_thread(detect_block, frame.image_bgr, settings)` → `await asyncio.to_thread(encode_jpeg, frame.image_bgr)` store `_latest_jpeg` → build `DetectionTelemetryWire` with fps/latency from loop timing → `await ws_manager.broadcast_json(telemetry.model_dump(by_alias=True))` → sleep to target ~30 FPS.
    - `latest_jpeg() -> bytes | None`, `latest_telemetry()`.
    - Load vision params from `VISION_CONFIG` JSON into mutable settings object updated by params route.
    - **Mock auto-start (open question):** In `main.py` lifespan, if `MOCK_CAMERA` true OR `DETECTION_MODE=mock`, call `detection_loop.start()` after create; else remain idle until POST start.
    - `# TODO: replace mock classificationScores when classifier exposes softmax` at telemetry build.
    `ws/manager.py`: FastAPI ConnectionManager pattern; `broadcast_json`, prune dead sockets.
    Wire `app.state.detection_loop` and `app.state.ws_manager` in main lifespan (shutdown stops loop).
  </action>
  <verify>
    <automated>cd backend && python -c "from app.services.detection_loop import DetectionLoopService; from app.ws.manager import ConnectionManager; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'asyncio.to_thread' backend/app/services/detection_loop.py` succeeds
    - `grep -q 'detect_block' backend/app/services/detection_loop.py` succeeds
    - `grep -q 'class ConnectionManager' backend/app/ws/manager.py` succeeds
    - `grep -q 'MOCK_CAMERA' backend/app/main.py` succeeds
  </acceptance_criteria>
  <done>Loop service and WS manager implemented; mock auto-start in lifespan when env set</done>
</task>

<task type="auto" tdd="true" validation_task_id="09-02-01">
  <name>Task 2: MJPEG stream and WebSocket routes</name>
  <read_first>
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-RESEARCH.md` → Pattern 2 MJPEG, headers Cache-Control and X-Accel-Buffering
    - `tests/test_api_stream.py`, `tests/test_api_ws.py`
  </read_first>
  <files>backend/app/routes/stream.py, backend/app/main.py</files>
  <action>
    `GET /video/stream`: `StreamingResponse` async generator yielding `--frame\\r\\nContent-Type: image/jpeg\\r\\n\\r\\n` + jpeg + `\\r\\n`; read `request.is_disconnected()`; sleep 1/30; **never** open VideoCapture in route.
    `@app.websocket("/ws/detection")`: accept, append to manager, loop `receive_text()` (ignore payload), disconnect cleanup.
    Register routers in `main.py`.
    Unskip and implement:
    - `test_mjpeg_content_type`: assert `multipart/x-mixed-replace` in content-type after start loop (mock env).
    - `test_ws_telemetry_after_start`: connect WS, start loop, receive one message with `"type":"telemetry"` and `"fps"` key within timeout 5s.
  </action>
  <verify>
    <automated>pytest tests/test_api_stream.py::test_mjpeg_content_type tests/test_api_ws.py::test_ws_telemetry_after_start -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'multipart/x-mixed-replace' backend/app/routes/stream.py` succeeds
    - `grep -q '/ws/detection' backend/app/main.py` succeeds
    - `pytest tests/test_api_stream.py::test_mjpeg_content_type -x` passes
    - `pytest tests/test_api_ws.py::test_ws_telemetry_after_start -x` passes
  </acceptance_criteria>
  <done>MJPEG and WebSocket endpoints green with shared loop</done>
</task>

<task type="auto" tdd="true" validation_task_id="09-03-03">
  <name>Task 3: REST detection, calibration, dataset routes</name>
  <read_first>
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-CONTEXT.md` → API table, no dead buttons
    - `config/calibration.example.json` → save schema reference
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-VALIDATION.md` → T-09-02 path confinement
  </read_first>
  <files>backend/app/routes/detection.py, backend/app/routes/calibration.py, backend/app/routes/dataset.py, backend/app/routes/__init__.py, backend/app/main.py</files>
  <action>
    Routes (all JSON camelCase responses on success):
    - `POST /api/detection/start` → `detection_loop.start()`; 200 `{started:true}`
    - `POST /api/detection/stop` → `detection_loop.stop()`; 200 `{started:false}`
    - `POST /api/detection/params` body `DetectionParamsWire` → validate bounds (Pydantic); update loop settings; 200 `{ok:true}`
    - `POST /api/calibration/save` body matches calibration JSON structure (Pydantic model); write to path from env `CALIBRATION_PATH` default `config/calibration.json` with atomic write (tmp + rename); reject path outside repo config dir via `Path.resolve().is_relative_to`
    - `POST /api/dataset/save-frame` optional `{reason?: string}`; save latest JPEG from loop to `DATASET_DIR` (env default `datasets/captured`); filename `frame_{utc_iso}.jpg`; **resolve path and assert `resolved.is_relative_to(dataset_root.resolve())`**; create dir if missing; 413 if body too large not applicable (no upload v1)
    Include routers in main.
    Run regression: `pytest tests/test_integration_pipeline.py -x` unchanged green.
  </action>
  <verify>
    <automated>pytest tests/test_integration_pipeline.py tests/test_api_health.py tests/test_api_stream.py tests/test_api_ws.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '/api/detection/start' backend/app/routes/detection.py` succeeds
    - `grep -q 'is_relative_to' backend/app/routes/dataset.py` succeeds
    - `grep -q 'DetectionParamsWire' backend/app/routes/detection.py` succeeds
    - `pytest tests/test_integration_pipeline.py -x` passes
  </acceptance_criteria>
  <done>All CONTEXT REST endpoints exist; dataset save path-confined; pipeline regression green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /api/* | Untrusted JSON bodies |
| API → filesystem | calibration + dataset writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | POST /api/detection/params | mitigate | DetectionParamsWire Field(ge=, le=) on every numeric field |
| T-09-02 | Elevation | POST /api/dataset/save-frame | mitigate | `Path.resolve()` + `is_relative_to(DATASET_DIR.resolve())`; reject `..` segments |
| T-09-05 | Denial of Service | save-frame | mitigate | Only writes pre-encoded latest_jpeg bytes; no arbitrary upload body |
| T-09-03 | Spoofing | CORS | mitigate | Inherited from Plan 02; document no credentials to untrusted origins |
</threat_model>

<verification>
- `pytest tests/test_api_stream.py tests/test_api_ws.py tests/test_api_health.py tests/test_integration_pipeline.py -x`
- `grep -q is_relative_to backend/app/routes/dataset.py`
</verification>

<success_criteria>
- UI-09-01 MJPEG stream test passes
- UI-09-02 WebSocket telemetry test passes
- UI-09-04 REST control routes implemented
- CONT-01 pipeline tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-03-SUMMARY.md`
</output>
