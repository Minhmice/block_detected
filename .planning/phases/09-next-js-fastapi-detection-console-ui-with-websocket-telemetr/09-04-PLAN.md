---
phase: 09-next-js-fastapi-detection-console-ui-with-websocket-telemetr
plan: 04
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/next.config.ts
  - frontend/tailwind.config.ts
  - frontend/postcss.config.mjs
  - frontend/app/globals.css
  - frontend/app/layout.tsx
  - frontend/app/page.tsx
  - frontend/types/vision.ts
  - frontend/lib/api.ts
  - frontend/lib/ws.ts
  - frontend/stores/useVisionStore.ts
  - frontend/.eslintrc.json
autonomous: true
requirements:
  - UI-09-08
  - UI-09-07
must_haves:
  truths:
    - "Next.js App Router app builds with Tailwind tokens from UI-SPEC"
    - "Zustand store exposes connection, detection, params, logs slices"
    - "api.ts and ws.ts read only NEXT_PUBLIC_* env vars (no localhost literals in lib/)"
    - "TypeScript types mirror golden wire fixture keys"
  artifacts:
    - path: frontend/types/vision.ts
      provides: "camelCase wire types"
    - path: frontend/stores/useVisionStore.ts
      provides: "Zustand slices per CONTEXT"
    - path: frontend/tailwind.config.ts
      provides: "Precision Optic color tokens"
      contains: "4cd7f6"
  key_links:
    - from: frontend/lib/ws.ts
      to: frontend/stores/useVisionStore.ts
      via: "getState().applyTelemetry"
      pattern: "applyTelemetry"
    - from: frontend/lib/api.ts
      to: process.env.NEXT_PUBLIC_API_BASE_URL
      via: "required env guard"
      pattern: "NEXT_PUBLIC_API_BASE_URL"
---

<objective>
Scaffold Next.js 16 App Router frontend with Tailwind design tokens, Zustand store, typed wire contract, REST client, and WebSocket auto-reconnect.

Purpose: Unlock UI component plans (05–06) with stable client infrastructure per CONTEXT D-01, D-03, D-09, D-10.
Output: `frontend/` directory runnable via `npm run dev` on port 3000.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-UI-SPEC.md
@.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-CONTEXT.md
@tests/fixtures/wire/detection_success.json
@example_ui/precision_optic_interface/DESIGN.md

<interfaces>
Golden fixture keys → `frontend/types/vision.ts` must include matching optional/required fields.
Env (components use via lib only): `NEXT_PUBLIC_API_BASE_URL`, `NEXT_PUBLIC_WS_URL`, `NEXT_PUBLIC_STREAM_URL`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js project and Tailwind token config</name>
  <read_first>
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-UI-SPEC.md` → Tailwind Config Contract, Color, Typography
    - `example_ui/precision_optic_interface/DESIGN.md` → token hex values
    - Do **not** delete or modify `example_ui/`
  </read_first>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/next.config.ts, frontend/tailwind.config.ts, frontend/postcss.config.mjs, frontend/app/globals.css, frontend/app/layout.tsx, frontend/.eslintrc.json</files>
  <action>
    Scaffold `frontend/` with `npx create-next-app@16` flags: TypeScript, Tailwind, App Router, ESLint, **no** src/ alias unless using `@/*` → map to project root in tsconfig paths.
    Pin: `next@16`, `react`, `react-dom`, `zustand@^5`, `tailwindcss@^4` + `@tailwindcss/postcss` per research.
    `tailwind.config.ts`: `darkMode: 'class'`, full color map from UI-SPEC (`primary: '#4cd7f6'`, `surface: '#0b1326'`, etc.), fontFamily Inter + JetBrains Mono, fontSize tokens, spacing gutter/container-padding/margin.
    `globals.css`: utilities `.panel` and `.data-input` per UI-SPEC; `@import` fonts Inter + JetBrains Mono (Google fonts or next/font).
    `layout.tsx`: `<html className="dark h-screen">`, minimal body; **no** hardcoded API URLs.
    Add `npm run lint` script; eslint config extends next/core-web-vitals.
  </action>
  <verify>
    <automated>cd frontend && npm run lint 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/package.json` succeeds
    - `grep -q '#4cd7f6' frontend/tailwind.config.ts` succeeds
    - `grep -q 'darkMode' frontend/tailwind.config.ts` succeeds
    - `grep -q '\\.panel' frontend/app/globals.css` succeeds
  </acceptance_criteria>
  <done>Next.js + Tailwind tokens compile; lint script exists</done>
</task>

<task type="auto" tdd="true" validation_task_id="09-04-02">
  <name>Task 2: vision.ts types and Zustand store</name>
  <read_first>
    - `tests/fixtures/wire/detection_success.json`
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-UI-SPEC.md` → Zustand Store Slices
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-CONTEXT.md` → Zustand slices locked
  </read_first>
  <files>frontend/types/vision.ts, frontend/stores/useVisionStore.ts</files>
  <action>
    `types/vision.ts`: export interfaces `PointPx`, `CornersPx`, `PickupPoseMm`, `DetectionResultWire`, `ClassificationScoresWire`, `DetectionTelemetryWire`, `DetectionParamsWire`, `ConnectionStatus` union.
    `stores/useVisionStore.ts` slices:
    - `connectionStatus`, `fps`, `latencyMs`, `setConnection`, `setTransportMetrics`
    - `detectionMode`, `cameraRunning`, `latestResult`, `overlayFlags` (booleans per UI-SPEC defaults: squareCorners/center/angle/pickup on)
    - params keys matching `vision.example.json` + UI-SPEC table (blurKernel, cannyMin, cannyMax, contourAreaMin/Max, aspectTolerance, confidenceThresh, thresholdMode, reject toggles)
    - `logs: {ts:number, level:string, message:string}[]` max 500 with trim
    - `applyTelemetry(msg: DetectionTelemetryWire)` maps WS payload to state
    - `appendLog(line: string)` prefixes handled by caller
    All files `'use client'` where hooks used.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'DetectionTelemetryWire' frontend/types/vision.ts` succeeds
    - `grep -q 'applyTelemetry' frontend/stores/useVisionStore.ts` succeeds
    - `grep -q 'connectionStatus' frontend/stores/useVisionStore.ts` succeeds
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Types and store match wire contract and UI-SPEC slices</done>
</task>

<task type="auto">
  <name>Task 3: api.ts, ws.ts, minimal page shell</name>
  <read_first>
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-RESEARCH.md` → Pattern 4 api/ws examples
    - `.env.example` at repo root
  </read_first>
  <files>frontend/lib/api.ts, frontend/lib/ws.ts, frontend/app/page.tsx</files>
  <action>
    `lib/api.ts`:
    - Throw at module load if `!process.env.NEXT_PUBLIC_API_BASE_URL` (clear error message).
    - Functions: `getHealth()`, `postDetectionStart()`, `postDetectionStop()`, `postDetectionParams(params)`, `postCalibrationSave(body)`, `postDatasetSaveFrame(body?)` — each `fetch` to documented paths, throw on `!res.ok`.
    `lib/ws.ts`:
    - Require `NEXT_PUBLIC_WS_URL`; exponential backoff reconnect 1s→8s cap; on open/close/error append via store `appendLog`.
    - `connectDetectionWs(): WebSocket` updates store connection + `applyTelemetry` on message JSON parse.
    `page.tsx`: client component placeholder `"Console loading…"` + call `connectDetectionWs()` in `useEffect` once; display env-missing error if vars undefined.
    **Forbidden:** `localhost` or `127.0.0.1` string literals in `frontend/lib/` and `frontend/components/` (grep gate).
  </action>
  <verify>
    <automated>cd frontend && npm run lint && ! rg -n 'localhost|127\\.0\\.0\\.1' lib/ components/ 2>/dev/null; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'NEXT_PUBLIC_API_BASE_URL' frontend/lib/api.ts` succeeds
    - `grep -q 'postDetectionStart' frontend/lib/api.ts` succeeds
    - `grep -q 'scheduleReconnect' frontend/lib/ws.ts` succeeds
    - `rg 'localhost|127\\.0\\.0\\.1' frontend/lib frontend/components` returns no matches
  </acceptance_criteria>
  <done>Integration libs complete; placeholder page connects WS when env set</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend JSON → browser | WebSocket/REST responses parsed in client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | WS message parse | mitigate | `JSON.parse` in try/catch; ignore malformed messages; log ERR line |
| T-09-03 | Spoofing | API base URL | accept | Operator configures NEXT_PUBLIC_* to trusted backend |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit`
- `cd frontend && npm run lint`
- `rg 'localhost|127\\.0\\.0\\.1' frontend/lib` exits non-zero (no matches)
</verification>

<success_criteria>
- UI-09-07 dev:frontend path ready (with root dev:all)
- UI-09-08 TypeScript types align with golden fixture keys
</success_criteria>

<output>
After completion, create `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-04-SUMMARY.md`
</output>
