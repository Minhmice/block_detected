---
phase: 09-next-js-fastapi-detection-console-ui-with-websocket-telemetr
plan: 05
type: execute
wave: 4
depends_on: ["09-04"]
files_modified:
  - frontend/components/AppShell.tsx
  - frontend/components/Sidebar.tsx
  - frontend/components/TopStatusBar.tsx
  - frontend/components/CameraViewport.tsx
  - frontend/components/VisionOverlay.tsx
  - frontend/components/DetectionControls.tsx
  - frontend/app/page.tsx
autonomous: true
requirements:
  - UI-09-03
  - UI-09-04
must_haves:
  truths:
    - "Operator sees cyber-console layout with sidebar, top bar, camera column, params column at 1366×768"
    - "MJPEG displays via NEXT_PUBLIC_STREAM_URL img tag only"
    - "Canvas overlay draws corners/center/angle when telemetry valid"
    - "Sliders update Zustand immediately and debounced POST params after 400ms"
  artifacts:
    - path: frontend/components/VisionOverlay.tsx
      provides: "640×480 scaled canvas overlay"
      pattern: "ResizeObserver"
    - path: frontend/components/DetectionControls.tsx
      provides: "Debounced POST /api/detection/params"
      pattern: "400"
  key_links:
    - from: frontend/components/TopStatusBar.tsx
      to: frontend/lib/api.ts
      via: "postDetectionStart/Stop"
      pattern: "postDetectionStart"
    - from: frontend/components/VisionOverlay.tsx
      to: frontend/stores/useVisionStore.ts
      via: "latestResult + overlayFlags"
      pattern: "latestResult"
---

<objective>
Implement core console shell and real-time viewport: AppShell, Sidebar, TopStatusBar, CameraViewport, VisionOverlay, DetectionControls wired to backend.

Purpose: Deliver primary operator surface (UI-09-03, UI-09-04) per UI-SPEC components 1–6.
Output: `/` route fully functional against running backend.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-UI-SPEC.md
@example_ui/real_time_detection_console_v2/code.html
@config/vision.example.json
@frontend/stores/useVisionStore.ts

</context>

<tasks>

<task type="auto">
  <name>Task 1: AppShell, Sidebar, TopStatusBar</name>
  <read_first>
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-UI-SPEC.md` → AppShell, Sidebar, TopStatusBar specs
    - `example_ui/real_time_detection_console_v2/code.html` → layout structure (reference only, do not edit)
  </read_first>
  <files>frontend/components/AppShell.tsx, frontend/components/Sidebar.tsx, frontend/components/TopStatusBar.tsx, frontend/app/page.tsx</files>
  <action>
    Implement `'use client'` components per UI-SPEC dimensions and copy (ALL CAPS buttons).
    `AppShell`: flex column `h-screen overflow-hidden bg-surface-dim`; children main area.
    `Sidebar`: `w-64`, nav items Real-time (`/`), Calibration (`/calibration`), Datasets (`/datasets`), Analysis (`/analysis` placeholder), System (`/system`); active route highlights `border-l-4 border-primary`; `hidden md:flex`.
    `TopStatusBar`: connection pill from store; FPS/latency from store; buttons wire to `postDetectionStart`, `postDetectionStop`, `getHealth`+`connectDetectionWs` for INITIALIZE, EMERGENCY_STOP calls stop + resets `cameraRunning`; `MOCK_MODE` badge when health returns `mockCamera:true`; disable buttons when `connectionStatus==='disconnected'` except INITIALIZE.
    `page.tsx`: compose AppShell with 12-col grid: camera col `lg:col-span-8`, params `lg:col-span-4`; mount TopStatusBar + Sidebar.
    Material Symbols via `next/font` or link in layout (UI-SPEC icon names).
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'VISION_OS' frontend/components/TopStatusBar.tsx` succeeds
    - `grep -q 'postDetectionStart' frontend/components/TopStatusBar.tsx` succeeds
    - `grep -q 'w-64' frontend/components/Sidebar.tsx` succeeds
    - `grep -q 'AppShell' frontend/app/page.tsx` succeeds
  </acceptance_criteria>
  <done>Shell layout matches UI-SPEC; top bar buttons call real API functions</done>
</task>

<task type="auto" validation_task_id="09-05-02">
  <name>Task 2: CameraViewport and VisionOverlay</name>
  <read_first>
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-UI-SPEC.md` → CameraViewport, VisionOverlay, Overlay Drawing Rules
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-RESEARCH.md` → Pattern 5 canvas scaling
  </read_first>
  <files>frontend/components/CameraViewport.tsx, frontend/components/VisionOverlay.tsx</files>
  <action>
    `CameraViewport`:
    - `<img src={process.env.NEXT_PUBLIC_STREAM_URL}>` with `object-contain`, `ref` for layout measurement.
    - Overlay toggle chips in header bound to `overlayFlags` in store.
    - Footer buttons: Snapshot (canvas drawImage optional v1): **must** call `postDatasetSaveFrame()` for "Save Failed Frame".
    - Status badges: VALID/INVALID from `latestResult.valid` / `rejectReason`.
    - Empty states exact copy from UI-SPEC when env stream URL missing.
    `VisionOverlay`:
    - Absolute `<canvas>` over img; `pointer-events-none`; ResizeObserver sets canvas backing store to **rendered image rect** (letterbox offsets).
    - Scale from native 640×480 per UI-SPEC formulas.
    - Draw layers per UI-SPEC when flags on: grid+crosshair always; bbox/corners/center/angle/pickup text when `valid && detection`.
    - `requestAnimationFrame` loop subscribed to store changes (max 30fps).
    Do not draw MJPEG into canvas for display.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && grep -q 'NEXT_PUBLIC_STREAM_URL' frontend/components/CameraViewport.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'NEXT_PUBLIC_STREAM_URL' frontend/components/CameraViewport.tsx` succeeds
    - `grep -q 'ResizeObserver' frontend/components/VisionOverlay.tsx` succeeds
    - `grep -q '640' frontend/components/VisionOverlay.tsx` succeeds
    - `grep -q 'postDatasetSaveFrame' frontend/components/CameraViewport.tsx` succeeds
  </acceptance_criteria>
  <done>Stream img + canvas overlay implemented per UI-SPEC drawing rules</done>
</task>

<task type="auto">
  <name>Task 3: DetectionControls with 400ms debounce</name>
  <read_first>
    - `config/vision.example.json` → default numeric values for RESET DEFAULTS
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-UI-SPEC.md` → DetectionControls table
  </read_first>
  <files>frontend/components/DetectionControls.tsx, frontend/app/page.tsx</files>
  <action>
    Implement all controls from UI-SPEC table bound to Zustand params slice.
    On slider change: update store immediately; debounce **400ms** then `postDetectionParams` mapped to `DetectionParamsWire` field names expected by backend (match backend schema keys in camelCase).
    `RESET DEFAULTS`: reset store to values from `vision.example.json` (hardcode same numbers in a `DEFAULT_VISION_PARAMS` constant object in component file) then POST.
    `THRESHOLD_MODE` select options: `canny`, `adaptive`, `hsv` (lowercase wire values).
    Panel uses `.panel` + `.data-input` classes.
    Wire into page params column.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && grep -q '400' frontend/components/DetectionControls.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'postDetectionParams' frontend/components/DetectionControls.tsx` succeeds
    - `grep -q 'RESET DEFAULTS' frontend/components/DetectionControls.tsx` succeeds
    - `grep -q 'blurKernel' frontend/components/DetectionControls.tsx` succeeds
    - `grep -q '400' frontend/components/DetectionControls.tsx` succeeds
  </acceptance_criteria>
  <done>Parameter panel live with debounced backend sync</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → sliders | Client-side state; server validates on POST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | params POST | transfer | Backend Pydantic bounds (Plan 03) |
| T-09-06 | Tampering | WS telemetry | mitigate | Overlay ignores draw when `valid===false` |
</threat_model>

<verification>
- `cd frontend && npm run lint`
- Manual UAT note in SUMMARY: start backend mock, open `/`, confirm stream img loads
</verification>

<success_criteria>
- UI-09-03 overlay scaling code present with 640/480 constants
- UI-09-04 START/STOP/params/dataset-save call api.ts
</success_criteria>

<output>
After completion, create `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-05-SUMMARY.md`
</output>
