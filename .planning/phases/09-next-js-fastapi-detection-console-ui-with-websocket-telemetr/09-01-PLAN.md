---
phase: 09-next-js-fastapi-detection-console-ui-with-websocket-telemetr
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/requirements.txt
  - tests/fixtures/wire/detection_success.json
  - tests/test_wire_schema.py
  - tests/test_api_health.py
  - tests/test_api_stream.py
  - tests/test_api_ws.py
  - tests/test_frame_source_factory.py
  - package.json
  - Makefile
  - .env.example
autonomous: true
requirements:
  - UI-09-08
must_haves:
  truths:
    - "Golden camelCase detection JSON fixture exists and is validated by pytest"
    - "API test modules exist with skip/xfail until backend routes land"
    - "Root dev:all script, Makefile dev target, and .env.example document all NEXT_PUBLIC_* and backend env vars"
  artifacts:
    - path: tests/fixtures/wire/detection_success.json
      provides: "Golden wire-format telemetry sample"
      contains: "\"blockId\""
    - path: package.json
      provides: "concurrently dev:all orchestration"
      contains: "dev:all"
    - path: .env.example
      provides: "Documented env contract for frontend and backend"
      contains: "NEXT_PUBLIC_API_BASE_URL"
  key_links:
    - from: tests/test_wire_schema.py
      to: tests/fixtures/wire/detection_success.json
      via: "json.load + key assertions"
      pattern: "detection_success.json"
---

<objective>
Close Phase 9 Wave 0 Nyquist gaps: wire golden fixture, API test scaffolding, backend dependency pins, and local dev orchestration entrypoints.

Purpose: Executors implement backend/frontend against stable automated checks; no subjective "looks correct" gates.
Output: `backend/requirements.txt`, wire fixture, four pytest modules (mostly xfail/skip until Plan 02), root `package.json`, `.env.example`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-CONTEXT.md
@.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-RESEARCH.md
@.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-VALIDATION.md
@src/block_detected/detection_contract.py

<interfaces>
Wire camelCase keys locked by CONTEXT (executor must match fixture exactly):
`blockId`, `confidence`, `centerPx`, `cornersPx` (`tl`,`tr`,`br`,`bl` each `{x,y}`), `angleDeg`, `pickupPoseMm` (`xMm`,`yMm`,`thetaDeg`), `status`, `valid`, `rejectReason`, `fps`, `latencyMs`, `classificationScores` (`block01`..`block04`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" validation_task_id="09-01-01">
  <name>Task 1: Golden wire fixture and schema test</name>
  <read_first>
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-CONTEXT.md` → Frontend TypeScript contract, Pydantic schemas
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-RESEARCH.md` → Pattern 8 telemetry envelope, Pydantic camelCase
    - `src/block_detected/detection_contract.py` → field names for realistic values in fixture
  </read_first>
  <files>tests/fixtures/wire/detection_success.json, tests/test_wire_schema.py</files>
  <behavior>
    - `test_golden_detection_json` loads fixture and asserts required top-level and nested keys exist
    - `test_corners_named_tl_tr_br_bl` asserts cornersPx has tl, tr, br, bl with numeric x/y
    - `test_pickup_pose_mm_aliases` asserts pickupPoseMm has xMm, yMm, thetaDeg (not snake_case)
  </behavior>
  <action>
    Create `tests/fixtures/wire/detection_success.json` as a **telemetry envelope** (not bare DetectionResult only):
    ```json
    {
      "type": "telemetry",
      "fps": 28.4,
      "latencyMs": 14.2,
      "valid": true,
      "rejectReason": null,
      "detection": {
        "blockId": 1,
        "confidence": 0.94,
        "status": "ok",
        "centerPx": {"x": 320.0, "y": 240.0},
        "cornersPx": {
          "tl": {"x": 280.0, "y": 200.0},
          "tr": {"x": 360.0, "y": 200.0},
          "br": {"x": 360.0, "y": 280.0},
          "bl": {"x": 280.0, "y": 280.0}
        },
        "angleDeg": 12.5,
        "pickupPoseMm": {"xMm": 120.0, "yMm": 45.0, "thetaDeg": 12.5}
      },
      "classificationScores": {
        "block01": 0.94,
        "block02": 0.02,
        "block03": 0.02,
        "block04": 0.02
      }
    }
    ```
    Implement `tests/test_wire_schema.py` with the three tests named above. Do **not** import FastAPI yet.
  </action>
  <verify>
    <automated>pytest tests/test_wire_schema.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"blockId"' tests/fixtures/wire/detection_success.json` succeeds
    - `grep -q '"pickupPoseMm"' tests/fixtures/wire/detection_success.json` succeeds
    - `pytest tests/test_wire_schema.py::test_golden_detection_json -x` passes
  </acceptance_criteria>
  <done>Golden fixture committed; wire schema tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend requirements and API test scaffolding</name>
  <read_first>
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-VALIDATION.md` → Wave 0 file list
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-RESEARCH.md` → Installation (backend dev) pins
    - `pyproject.toml` → keep block_detected editable install separate from backend venv
  </read_first>
  <files>backend/requirements.txt, tests/test_api_health.py, tests/test_api_stream.py, tests/test_api_ws.py, tests/test_frame_source_factory.py</files>
  <behavior>
    - `test_golden_detection_json` already green from Task 1
    - `test_health_returns_200` skipped with reason `Plan 09-02` until FastAPI app exists
    - `test_mjpeg_content_type` skipped until stream route exists
    - `test_ws_telemetry_after_start` skipped until WS + loop exist
    - `test_mock_camera_uses_image_sequence` skipped until `frame_source_factory` exists
  </behavior>
  <action>
    Add `backend/requirements.txt` pinning (minimum versions per research):
    `fastapi>=0.136`, `uvicorn[standard]>=0.48`, `pydantic>=2.13`, `opencv-python-headless>=4.11,<4.14`, `numpy>=2,<3`, `python-multipart`, `websockets`, `httpx` (for AsyncClient tests).
    Add comment line: `# Install block_detected: pip install -e ..` from repo root.
    Create pytest modules with **exact test function names** expected by 09-VALIDATION.md:
    - `tests/test_api_health.py::test_health_returns_200`
    - `tests/test_api_stream.py::test_mjpeg_content_type`
    - `tests/test_api_ws.py::test_ws_telemetry_after_start`
    - `tests/test_frame_source_factory.py::test_mock_camera_uses_image_sequence`
    Each skipped test body should contain a one-line docstring referencing Plan 09-02. No FastAPI imports in skipped tests.
  </action>
  <verify>
    <automated>pytest tests/test_wire_schema.py tests/test_api_health.py tests/test_api_stream.py tests/test_api_ws.py tests/test_frame_source_factory.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/requirements.txt` succeeds
    - `grep -q 'fastapi' backend/requirements.txt` succeeds
    - `pytest tests/test_api_health.py -q` reports skipped (not failed)
  </acceptance_criteria>
  <done>Backend pins file exists; API tests reserved with correct names</done>
</task>

<task type="auto">
  <name>Task 3: Root dev orchestration and environment template</name>
  <read_first>
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-CONTEXT.md` → DevOps, env vars only in components
    - `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-RESEARCH.md` → Pattern 8 root package.json, CORS_ORIGINS
  </read_first>
  <files>package.json, Makefile, .env.example</files>
  <action>
    Create root `package.json` (no workspaces required v1) with scripts:
    - `dev:all`: `concurrently -n backend,frontend -c blue,green "npm run dev:backend" "npm run dev:frontend"`
    - `dev:backend`: `cd backend && .venv/bin/uvicorn app.main:app --reload --host 127.0.0.1 --port 8000 --workers 1` (document venv setup in README Plan 06)
    - `dev:frontend`: `cd frontend && npm run dev -- --port 3000`
    DevDependency: `concurrently@^10.0.0`.
    Create `Makefile` at repo root with:
    - `.PHONY: dev`
    - `dev:` target that runs `npm run dev:all` (single entry for `make dev` per CONTEXT)
    Create `.env.example` at repo root with commented sections:
    - Backend: `MOCK_CAMERA=true`, `DETECTION_MODE=mock`, `CORS_ORIGINS=http://localhost:3000`, `CAMERA_CONFIG=config/camera.example.json`, `VISION_CONFIG=config/vision.example.json`, `DATASET_DIR=datasets/captured`, `HOST=127.0.0.1`, `PORT=8000`
    - Frontend: `NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000`, `NEXT_PUBLIC_WS_URL=ws://127.0.0.1:8000/ws/detection`, `NEXT_PUBLIC_STREAM_URL=http://127.0.0.1:8000/video/stream`
    Use `127.0.0.1` in example (not bare `localhost` in TS components — example file only).
    Do **not** create `frontend/` or `backend/app/` yet (Plans 02–03).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts['dev:all']) throw new Error('missing dev:all'); console.log('ok')" && grep -q '^dev:' Makefile && grep -q 'dev:all' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'dev:all' package.json` succeeds
    - `grep -q '^dev:' Makefile` succeeds
    - `grep -q 'npm run dev:all' Makefile` succeeds
    - `grep -q 'NEXT_PUBLIC_WS_URL' .env.example` succeeds
    - `grep -q 'CORS_ORIGINS' .env.example` succeeds
  </acceptance_criteria>
  <done>Local dev entrypoint and env template committed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture file → pytest | Static JSON only; no user input |
| .env.example → developer | Template only; not loaded as secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | POST bodies (future) | mitigate | Wave 1+ Pydantic bounds on DetectionParams (Plan 02) |
| T-09-02 | Elevation | dataset save path (future) | mitigate | Plan 02 path confinement under DATASET_DIR |
| T-09-03 | Spoofing | CORS | mitigate | `CORS_ORIGINS` comma-separated list in .env.example; no `*` in example |
</threat_model>

<verification>
- `pytest tests/test_wire_schema.py -x` passes
- `pytest tests/test_api_*.py tests/test_frame_source_factory.py -q` exits 0 with skips
- `grep -q dev:all package.json`
</verification>

<success_criteria>
- UI-09-08 golden wire contract testable before backend exists
- Wave 0 checklist in 09-VALIDATION.md satisfied except items owned by Plans 02–06
</success_criteria>

<output>
After completion, create `.planning/phases/09-next-js-fastapi-detection-console-ui-with-websocket-telemetr/09-01-SUMMARY.md`
</output>
