---
phase: 01-contract-pipeline-skeleton
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/__init__.py
  - tests/test_detection_contract.py
  - tests/test_pipeline.py
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
must_haves:
  truths:
    - "Calling detect_block(frame) (once implemented) returns a DetectionResult that passes validate_detection_result()."
    - "Synthetic success input populates block_id, confidence, ordered corners, center_px, and angle_deg."
    - "Rejected and ambiguous inputs return status with debug.rejection_reason and no fabricated candidate geometry."
  artifacts:
    - path: "tests/__init__.py"
      provides: "Importable unittest test package"
    - path: "tests/test_detection_contract.py"
      provides: "Contract regression coverage for samples, serialization, mismatch guard, and multiple_candidates no-geometry expectation"
    - path: "tests/test_pipeline.py"
      provides: "Public detect_block API tests for CONT-01, CONT-02, and CONT-03"
  key_links:
    - from: "tests/test_pipeline.py"
      to: "src/block_detected/pipeline.py"
      via: "from block_detected import detect_block"
      pattern: "detect_block"
    - from: "tests/test_detection_contract.py"
      to: "src/block_detected/detection_contract.py"
      via: "contract imports and validate_detection_result"
      pattern: "validate_detection_result"
---

<objective>
Create the Phase 1 Wave 0 test contract before production changes.

Purpose: Lock CONT-01, CONT-02, and CONT-03 with stdlib unittest so later implementation cannot drift from the public API or no-fake-geometry semantics.
Output: `tests/__init__.py`, `tests/test_detection_contract.py`, and `tests/test_pipeline.py`.
</objective>

<execution_context>
@/Users/minhmice/.codex/get-shit-done/workflows/execute-plan.md
@/Users/minhmice/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-contract-pipeline-skeleton/01-RESEARCH.md
@.planning/phases/01-contract-pipeline-skeleton/01-VALIDATION.md
@.planning/notes/task-01-contract.md
@detection_contract.py
@CLAUDE.md

<interfaces>
Existing contract exports in `detection_contract.py`:
```python
class DetectionStatus(str, Enum):
    OK = "ok"
    LOW_CONFIDENCE = "low_confidence"
    INVALID_GEOMETRY = "invalid_geometry"
    NO_DETECTION = "no_detection"
    MULTIPLE_CANDIDATES = "multiple_candidates"

def make_no_detection_result(...) -> DetectionResult: ...
def validate_detection_result(result: DetectionResult) -> DetectionResult: ...
def result_to_dict(result: DetectionResult) -> Dict[str, Any]: ...
def result_to_json(result: DetectionResult) -> str: ...
SAMPLE_SUCCESS_BLOCK_1: DetectionResult
SAMPLE_LOW_CONFIDENCE: DetectionResult
SAMPLE_NO_DETECTION: DetectionResult
```

Known mismatch to encode as RED: current validation treats `MULTIPLE_CANDIDATES` like a candidate-bearing status. Phase 1 must resolve this so ambiguous/rejected status can carry `debug.rejection_reason` with no candidate geometry.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add contract regression tests</name>
  <files>tests/__init__.py, tests/test_detection_contract.py</files>
  <objective>Preserve existing contract behavior while adding the expected `MULTIPLE_CANDIDATES` no-geometry regression.</objective>
  <read_first>
    - `.planning/phases/01-contract-pipeline-skeleton/01-VALIDATION.md`
    - `detection_contract.py`
    - `.planning/notes/task-01-contract.md`
  </read_first>
  <action>Create `tests/__init__.py` as an empty package marker. Create `tests/test_detection_contract.py` using only stdlib `unittest`. Cover: sample success validates and serializes; `SAMPLE_NO_DETECTION` has nullable candidate fields plus `debug.rejection_reason`; block_id/label mismatch raises `DetectionContractError`; a constructed `DetectionResult` with `status=DetectionStatus.MULTIPLE_CANDIDATES`, `confidence=0.0`, nullable candidate fields, `pickup_pose=None`, and `DebugInfo(rejection_reason="multiple candidates")` is expected to validate. Import from root `detection_contract` first so tests run before package migration.</action>
  <acceptance_criteria>The test file contains concrete assertions for CONT-02 and CONT-03, including the explicit failing assertion for `MULTIPLE_CANDIDATES` no-geometry semantics. No third-party imports are present.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest tests/test_detection_contract.py -v</automated>
    <expectation>RED is acceptable in Wave 0 only if the failure is the known `MULTIPLE_CANDIDATES` no-geometry mismatch; syntax/import errors are not acceptable.</expectation>
  </verify>
  <done>`tests/test_detection_contract.py` exists, uses `unittest`, and fails only on the known `MULTIPLE_CANDIDATES` mismatch before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add public pipeline API tests</name>
  <files>tests/test_pipeline.py</files>
  <objective>Define the `detect_block(frame)` behavior before `src/block_detected/pipeline.py` exists.</objective>
  <read_first>
    - `.planning/phases/01-contract-pipeline-skeleton/01-VALIDATION.md`
    - `.planning/phases/01-contract-pipeline-skeleton/01-RESEARCH.md`
    - `.planning/REQUIREMENTS.md`
  </read_first>
  <action>Create `tests/test_pipeline.py` using stdlib `unittest`. At the top of the test file, insert the repository `src/` directory into `sys.path` using `pathlib.Path(__file__).resolve().parents[1] / "src"` so the planned `src/block_detected` package imports under the validation command from `01-VALIDATION.md`. Test imports from `block_detected` must include `detect_block`, `DetectionResult`, `DetectionStatus`, `validate_detection_result`, and `result_to_dict`. Cover: ordinary unsupported frame input such as `{"frame_id": "ordinary"}` returns a validated `DetectionResult` with `status=NO_DETECTION`, confidence `0.0`, all candidate geometry fields `None`, and a non-empty `debug.rejection_reason`; synthetic success input such as `{"__block_detected_synthetic__": "success_block_1", "frame_id": "synthetic"}` returns validated status `OK` with `block_id`, `label`, `confidence`, `center_px`, `corners_px` in TL/TR/BR/BL attributes, and `angle_deg`; synthetic ambiguous input such as `{"__block_detected_synthetic__": "multiple_candidates", "frame_id": "ambiguous"}` returns validated `MULTIPLE_CANDIDATES` with no candidate geometry and a rejection reason.</action>
  <acceptance_criteria>The test file fully covers CONT-01, CONT-02, and CONT-03 from the public package import path and does not import OpenCV, NumPy, TFLite, pytest, or camera hardware libraries.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest tests/test_pipeline.py -v</automated>
    <expectation>RED is acceptable in Wave 0 only because `block_detected`/`detect_block` do not exist yet; syntax errors or third-party dependency errors are not acceptable.</expectation>
  </verify>
  <done>`tests/test_pipeline.py` exists, uses package-level imports, covers ordinary rejection, synthetic success, and synthetic ambiguous inputs, and fails only because the package/API is not implemented yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test input -> public API contract | Tests model future untrusted caller input to `detect_block(frame)` without camera or CV dependencies. |
| result object -> JSON/logging consumers | Contract serialization must not silently emit invalid enum or geometry state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `DetectionResult` validation | high | mitigate | Tests must assert invalid block_id/label mismatch raises `DetectionContractError`; ASVS L1 high findings block completion. |
| T-01-02 | Information Disclosure | `DebugInfo.rejection_reason` | low | accept | Phase 1 debug strings are deterministic and contain no frame pixels, filesystem paths, or secrets. |
| T-01-03 | Elevation of Privilege | fake geometry in rejected status | high | mitigate | Tests must assert `NO_DETECTION` and `MULTIPLE_CANDIDATES` carry no `block_id`, `center_px`, `corners_px`, `bbox_px`, `face_area_px`, or `pickup_pose`. |
</threat_model>

<verification>
Run the Wave 0 commands:

```bash
python3 -m unittest tests/test_detection_contract.py -v
python3 -m unittest tests/test_pipeline.py -v
```

Expected Wave 0 state: tests are syntactically valid and fail only for missing package/API or the known `MULTIPLE_CANDIDATES` contract mismatch.
</verification>

<success_criteria>
- Wave 0 test files from `01-VALIDATION.md` exist.
- Tests use stdlib `unittest` only.
- Tests encode CONT-01, CONT-02, CONT-03 and explicitly cover the `MULTIPLE_CANDIDATES` no-fake-geometry requirement.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-pipeline-skeleton/01-01-SUMMARY.md`.
</output>
