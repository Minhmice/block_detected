---
phase: 01-contract-pipeline-skeleton
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/__init__.py
  - tests/test_detection_contract.py
  - tests/test_pipeline.py
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
must_haves:
  truths:
    - "Failing unittest suite exists for detect_block contract behavior before implementation"
    - "Contract sample serialization and identity mismatch guard are locked by regression tests"
    - "CONT-01/02/03 behaviors are asserted with grep-verifiable test names and imports"
  artifacts:
    - path: tests/__init__.py
      provides: "unittest discovery anchor"
      min_lines: 1
    - path: tests/test_detection_contract.py
      provides: "contract regression before package move"
      contains: "test_block_id_label_mismatch_raises"
    - path: tests/test_pipeline.py
      provides: "CONT-01/02/03 + import-path tests (01-01-01 through 01-01-04)"
      contains: "DetectBlockContractTests"
  key_links:
    - from: tests/test_pipeline.py
      to: block_detected.pipeline.detect_block
      via: "import detect_block (expected to fail until Plan 02)"
      pattern: "from block_detected import detect_block|from block_detected.pipeline import detect_block"
    - from: tests/test_detection_contract.py
      to: detection_contract.py
      via: "import contract symbols from root shim during Wave 0"
      pattern: "import detection_contract|from detection_contract import"
---

<objective>
Create Wave 0 failing unittest coverage for Phase 01 contract and pipeline requirements.

Purpose: Nyquist validation — tests define expected `detect_block` behavior before any package move or pipeline implementation. Executors implement against red tests, not ad-hoc smoke snippets.
Output: `tests/` tree with `test_detection_contract.py` and `test_pipeline.py` mapping to validation task IDs 01-01-01 through 01-01-04.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-contract-pipeline-skeleton/01-RESEARCH.md
@.planning/phases/01-contract-pipeline-skeleton/01-VALIDATION.md
@.planning/notes/task-01-contract.md
@detection_contract.py

<interfaces>
From detection_contract.py (existing — do NOT re-implement):

```python
class DetectionContractError(ValueError): ...
class BlockID(IntEnum): ...
class BlockLabel(str, Enum): ...
class DetectionStatus(str, Enum): ...
class DetectionResult: ...  # frozen dataclass
def make_no_detection_result(...) -> DetectionResult: ...
def validate_detection_result(result: DetectionResult) -> DetectionResult: ...
def result_to_dict(result: DetectionResult) -> Dict[str, Any]: ...
def result_to_json(result: DetectionResult) -> str: ...
SAMPLE_SUCCESS_BLOCK_1: DetectionResult
SAMPLE_LOW_CONFIDENCE: DetectionResult
SAMPLE_NO_DETECTION: DetectionResult
SAMPLE_OUTPUTS_JSON: Mapping[str, str]
```

Expected public API after Plan 02 (tests import these now; ModuleNotFoundError is acceptable RED state):

```python
# block_detected/__init__.py (Plan 02)
from block_detected.pipeline import detect_block, SYNTHETIC_SUCCESS_FRAME

# block_detected/pipeline.py (Plan 02)
def detect_block(frame) -> DetectionResult: ...
SYNTHETIC_SUCCESS_FRAME = object()  # sentinel for CONT-02 tests only
```

Stub policy (Plan 02): ordinary frames → `make_no_detection_result()`; sentinel → validated copy of `SAMPLE_SUCCESS_BLOCK_1`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="01-01-01">
  <name>Task 1: Contract regression tests (pre-move lock)</name>
  <read_first>
    detection_contract.py
    .planning/notes/task-01-contract.md
    .planning/phases/01-contract-pipeline-skeleton/01-VALIDATION.md
  </read_first>
  <files>tests/__init__.py, tests/test_detection_contract.py</files>
  <behavior>
    - Test: SAMPLE_OUTPUTS_JSON keys exist and each value parses as JSON with a "status" field
    - Test: validate_detection_result(SAMPLE_SUCCESS_BLOCK_1) returns same instance
    - Test: block_id/label mismatch raises DetectionContractError
    - Test: make_no_detection_result() has status no_detection, null geometry, non-empty debug.rejection_reason
  </behavior>
  <action>
    Create `tests/__init__.py` (empty or docstring only).

    Create `tests/test_detection_contract.py` using stdlib `unittest`. Import from root `detection_contract` module (Wave 0 — contract still at repo root). Do NOT move or modify `detection_contract.py` in this task.

    Test class `ContractRegressionTests` with methods:
    - `test_sample_outputs_json_keys_and_parse` — assert keys `successful_detection_block_1`, `low_confidence_detection`, `no_detection`; json.loads each; assert parsed dict has `status`
    - `test_validate_success_sample` — assert validate returns SAMPLE_SUCCESS_BLOCK_1
    - `test_block_id_label_mismatch_raises` — construct DetectionResult with block_id=BLOCK_01, label=BLOCK_02, status=OK, full geometry from SAMPLE_SUCCESS_BLOCK_1; expect DetectionContractError
    - `test_no_detection_has_null_geometry_and_reason` — assert SAMPLE_NO_DETECTION and make_no_detection_result() have null block_id/center_px/corners_px/angle_deg and debug.rejection_reason is truthy string

    No OpenCV, NumPy, pytest, or third-party deps.
  </action>
  <acceptance_criteria>
    - `rg "class ContractRegressionTests" tests/test_detection_contract.py` matches
    - `rg "test_block_id_label_mismatch_raises" tests/test_detection_contract.py` matches
    - `rg "SAMPLE_OUTPUTS_JSON" tests/test_detection_contract.py` matches
    - `python3 -m unittest tests.test_detection_contract -v` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest tests.test_detection_contract -v</automated>
  </verify>
  <done>Contract regression tests pass against root detection_contract.py; behavior locked before package move.</done>
</task>

<task type="auto" tdd="true" id="01-01-02">
  <name>Task 2: Pipeline contract tests — RED for CONT-01/02/03/04</name>
  <read_first>
    .planning/phases/01-contract-pipeline-skeleton/01-RESEARCH.md
    .planning/phases/01-contract-pipeline-skeleton/01-VALIDATION.md
    detection_contract.py
  </read_first>
  <files>tests/test_pipeline.py</files>
  <behavior>
    - 01-01-01 CONT-01: detect_block(ordinary_frame) returns DetectionResult; validate_detection_result(result) is result; no exception on unsupported input
    - 01-01-02 CONT-02: detect_block(SYNTHETIC_SUCCESS_FRAME) populates block_id, confidence, center_px, corners_px TL/TR/BR/BL, angle_deg; status ok
    - 01-01-03 CONT-03: detect_block(ordinary_frame) returns no_detection with debug.rejection_reason and null candidate geometry
    - 01-01-04 CONT-01: root detection_contract shim and block_detected package expose same DetectionResult type
  </behavior>
  <action>
    Create `tests/test_pipeline.py` with stdlib unittest. Import `detect_block` and `SYNTHETIC_SUCCESS_FRAME` from `block_detected` (or `block_detected.pipeline`). Import contract helpers from `block_detected.detection_contract` where needed; fall back to root `detection_contract` only for shim comparison in import test.

    Class `DetectBlockContractTests`:
    - `test_detect_block_returns_validated_result` (01-01-01): `result = detect_block(object())`; assert isinstance(result, DetectionResult); assert validate_detection_result(result) is result
    - `test_synthetic_success_populates_geometry` (01-01-02): `result = detect_block(SYNTHETIC_SUCCESS_FRAME)`; assert status OK; assert block_id is not None; assert 0 &lt; confidence &lt;= 1; assert center_px and corners_px; assert corners order TL/TR/BR/BL via `.as_ordered_tuple()` length 4; assert angle_deg is not None
    - `test_ordinary_frame_rejects_without_fake_geometry` (01-01-03): `result = detect_block({"width": 640, "height": 480})`; assert status NO_DETECTION; assert block_id/center_px/corners_px/angle_deg/bbox_px/face_area_px all None; assert result.debug and result.debug.rejection_reason
    - `test_root_shim_and_package_share_detection_result` (01-01-04): import DetectionResult from root `detection_contract` and from `block_detected.detection_contract`; assert same class object (`is`)

    Tests MUST fail with import or attribute errors until Plan 02 implements package + pipeline. Running the file is expected RED (non-zero exit) in Wave 0 — that is correct TDD state.
  </action>
  <acceptance_criteria>
    - `rg "class DetectBlockContractTests" tests/test_pipeline.py` matches
    - `rg "test_detect_block_returns_validated_result|test_synthetic_success_populates_geometry|test_ordinary_frame_rejects_without_fake_geometry|test_root_shim_and_package_share_detection_result" tests/test_pipeline.py` matches all four
    - `rg "SYNTHETIC_SUCCESS_FRAME" tests/test_pipeline.py` matches
    - `rg "from block_detected" tests/test_pipeline.py` matches
    - `python3 -m unittest tests.test_pipeline -v` exits non-zero (RED — missing module or failing assertions)
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest tests.test_pipeline -v; test $? -ne 0</automated>
  </verify>
  <done>Failing pipeline tests exist and map to validation task IDs 01-01-01 through 01-01-04; contract tests from Task 1 still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test author → production contract | Tests must not modify contract semantics in Wave 0 |
| future detect_block caller → frame input | Tests assume arbitrary objects must not crash public API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | detect_block(frame) | mitigate | Test 01-01-01 asserts no throw on ordinary unsupported frame (Plan 02 implements fail-closed) |
| T-01-02 | Tampering | rejected result geometry | mitigate | Test 01-01-03 asserts null candidate fields for ordinary frames |
| T-01-03 | Spoofing | synthetic success sentinel | accept | Explicit test-only sentinel documented; not used for real frames |
</threat_model>

<verification>
Wave 0 gate:
- `python3 -m unittest tests.test_detection_contract -v` — GREEN
- `python3 -m unittest tests.test_pipeline -v` — RED (expected)
- `python3 -m unittest discover -v` — may exit non-zero due to pipeline RED; document in SUMMARY
</verification>

<success_criteria>
- tests/__init__.py, test_detection_contract.py, test_pipeline.py exist
- Validation task IDs 01-01-01 through 01-01-04 have named test methods
- Contract regression tests pass; pipeline tests fail until Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-pipeline-skeleton/01-01-SUMMARY.md`
</output>
