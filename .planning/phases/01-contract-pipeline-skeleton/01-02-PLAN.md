---
phase: 01-contract-pipeline-skeleton
plan: 02
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - block_detected/__init__.py
  - block_detected/detection_contract.py
  - block_detected/pipeline.py
  - detection_contract.py
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
must_haves:
  truths:
    - "Integrators call detect_block(frame) and receive validate_detection_result()-checked DetectionResult"
    - "Ordinary frames return no_detection with rejection_reason and no fabricated geometry"
    - "SYNTHETIC_SUCCESS_FRAME sentinel triggers validated success geometry for tests only"
    - "Root detection_contract import path remains compatible via re-export shim"
    - "MULTIPLE_CANDIDATES is a no-geometry rejection status aligned with REJ-04"
  artifacts:
    - path: block_detected/pipeline.py
      provides: "public detect_block entry point"
      contains: "def detect_block"
    - path: block_detected/detection_contract.py
      provides: "relocated contract (not re-written)"
      contains: "def validate_detection_result"
    - path: block_detected/__init__.py
      provides: "public package exports"
      exports: ["detect_block", "SYNTHETIC_SUCCESS_FRAME"]
    - path: detection_contract.py
      provides: "root compatibility shim"
      contains: "from block_detected.detection_contract import"
  key_links:
    - from: block_detected/pipeline.py
      to: block_detected/detection_contract.py
      via: "validate_detection_result + make_no_detection_result + SAMPLE_SUCCESS_BLOCK_1"
      pattern: "validate_detection_result\\("
    - from: detection_contract.py
      to: block_detected/detection_contract.py
      via: "re-export shim"
      pattern: "from block_detected.detection_contract import"
    - from: tests/test_pipeline.py
      to: block_detected
      via: "import detect_block"
      pattern: "from block_detected import detect_block"
---

<objective>
Wire existing contract into a flat `block_detected/` package, fix MULTIPLE_CANDIDATES semantics, and implement `detect_block(frame)` stub that satisfies CONT-01 through CONT-03.

Purpose: Deliver the Phase 1 public API boundary integrators will call before camera/vision stages exist. Contract module is moved, not re-implemented.
Output: Green unittest suite; `detect_block` exported from `block_detected`; root `detection_contract.py` shim preserved.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
@.planning/phases/01-contract-pipeline-skeleton/01-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-contract-pipeline-skeleton/01-RESEARCH.md
@.planning/phases/01-contract-pipeline-skeleton/01-VALIDATION.md
@detection_contract.py
@tests/test_pipeline.py
@tests/test_detection_contract.py

<interfaces>
MULTIPLE_CANDIDATES fix (per research recommendation — small contract change):

In `_validate_detection_result_fields`, move `DetectionStatus.MULTIPLE_CANDIDATES` from the geometry-required set `{OK, LOW_CONFIDENCE, MULTIPLE_CANDIDATES}` into the no-candidate rejection set `{NO_DETECTION, INVALID_GEOMETRY, MULTIPLE_CANDIDATES}`. Add/adjust contract test asserting a no-geometry MULTIPLE_CANDIDATES result validates when debug.rejection_reason is set.

Package layout (smallest path — flat `block_detected/` at repo root, no pyproject.toml):

```text
block_detected/
  __init__.py
  detection_contract.py   # moved from root (content preserved + MULTIPLE_CANDIDATES fix)
  pipeline.py
detection_contract.py     # shim: from block_detected.detection_contract import *
tests/                      # from Plan 01 — must all pass after this plan
```

detect_block stub policy:

```python
SYNTHETIC_SUCCESS_FRAME = object()  # module constant, exported from __init__.py

def detect_block(frame) -> DetectionResult:
    if frame is SYNTHETIC_SUCCESS_FRAME:
        return validate_detection_result(SAMPLE_SUCCESS_BLOCK_1)
    return validate_detection_result(
        make_no_detection_result(
            method="pipeline_stub",
            rejection_reason="pipeline stub has no detector implementation",
        )
    )
```

Do NOT import cv2, numpy, or tflite. Do NOT duplicate DetectionResult or validators.
</interfaces>
</context>

<tasks>

<task type="auto" id="01-02-01">
  <name>Task 1: Package layout — move contract, fix MULTIPLE_CANDIDATES, root shim</name>
  <read_first>
    detection_contract.py
    tests/test_detection_contract.py
    .planning/phases/01-contract-pipeline-skeleton/01-RESEARCH.md (Pitfall 1, Open Questions)
  </read_first>
  <files>block_detected/detection_contract.py, detection_contract.py, block_detected/__init__.py</files>
  <action>
    Create `block_detected/` package directory.

    Move existing `detection_contract.py` implementation into `block_detected/detection_contract.py` by relocation (preserve all types, helpers, samples). Apply ONE semantic fix per research: in `_validate_detection_result_fields`, remove `DetectionStatus.MULTIPLE_CANDIDATES` from the geometry-required branch and add it to the no-candidate rejection branch alongside `NO_DETECTION` and `INVALID_GEOMETRY`.

    Replace repo-root `detection_contract.py` with a thin compatibility shim:
    ```python
    from block_detected.detection_contract import *  # noqa: F401,F403
    ```

    Create minimal `block_detected/__init__.py` (pipeline exports added in Task 2). Ensure `from block_detected.detection_contract import DetectionResult` works when running tests from repo root.

    Update `tests/test_detection_contract.py` ONLY if imports break — prefer keeping root `detection_contract` imports (shim should preserve them). Add one test method `test_multiple_candidates_rejects_with_null_geometry` that builds a result via `make_no_detection_result(...)` pattern but with `status=DetectionStatus.MULTIPLE_CANDIDATES` and appropriate rejection_reason; assert `validate_detection_result` accepts it.

    Do NOT re-implement contract types from scratch. Do NOT add pyproject.toml or src/ layout.
  </action>
  <acceptance_criteria>
    - `rg "MULTIPLE_CANDIDATES" block_detected/detection_contract.py` shows status in no-candidate elif branch (not in geometry-required if branch with OK/LOW_CONFIDENCE only)
    - `rg "from block_detected.detection_contract import" detection_contract.py` matches
    - `rg "test_multiple_candidates_rejects_with_null_geometry" tests/test_detection_contract.py` matches
    - `python3 -m unittest tests.test_detection_contract -v` exits 0
    - `python3 -c "import detection_contract; from block_detected.detection_contract import DetectionResult; assert detection_contract.DetectionResult is DetectionResult"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest tests.test_detection_contract -v</automated>
  </verify>
  <done>Contract lives in block_detected package; root shim re-exports; MULTIPLE_CANDIDATES validates as no-geometry rejection; contract tests green.</done>
</task>

<task type="auto" id="01-02-02">
  <name>Task 2: Implement detect_block stub and public exports</name>
  <read_first>
    block_detected/detection_contract.py
    tests/test_pipeline.py
    .planning/phases/01-contract-pipeline-skeleton/01-RESEARCH.md (Pattern 2, Pattern 3)
  </read_first>
  <files>block_detected/pipeline.py, block_detected/__init__.py</files>
  <action>
    Create `block_detected/pipeline.py`:

    - Define module-level `SYNTHETIC_SUCCESS_FRAME = object()` (unique sentinel for tests/CONT-02 only).
    - Implement `detect_block(frame)` per stub policy in context: sentinel → `validate_detection_result(SAMPLE_SUCCESS_BLOCK_1)`; all other frames → `validate_detection_result(make_no_detection_result(method="pipeline_stub", rejection_reason="pipeline stub has no detector implementation"))`.
    - Every return path MUST call `validate_detection_result()` — no bypass helper that skips validation.
    - Wrap body in try/except only if needed to fail closed: on unexpected errors, return validated `make_no_detection_result(rejection_reason="pipeline stub failed to process frame")` rather than raising (per T-01-01 mitigation).

    Update `block_detected/__init__.py` to re-export:
    - `detect_block` from pipeline
    - `SYNTHETIC_SUCCESS_FRAME` from pipeline
    - Optionally re-export common contract symbols (`DetectionResult`, `validate_detection_result`, `DetectionStatus`, etc.) for integrator convenience — at minimum exports required by tests.

    No OpenCV/TFLite/NumPy imports. No contour/warp/classifier logic.
  </action>
  <acceptance_criteria>
    - `rg "def detect_block" block_detected/pipeline.py` matches
    - `rg "SYNTHETIC_SUCCESS_FRAME" block_detected/pipeline.py` matches
    - `rg "validate_detection_result" block_detected/pipeline.py` matches at least twice (both return paths)
    - `rg "make_no_detection_result" block_detected/pipeline.py` matches
    - `rg "detect_block|SYNTHETIC_SUCCESS_FRAME" block_detected/__init__.py` matches
    - `rg "import cv2|from cv2|import numpy|tflite" block_detected/` finds no matches
    - `python3 -m unittest tests.test_pipeline -v` exits 0
    - `python3 -m unittest discover -v` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -v</automated>
  </verify>
  <done>detect_block stub satisfies CONT-01/02/03; full unittest suite green; validation tasks 01-01-01 through 01-01-04 pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external caller → detect_block(frame) | Untrusted frame objects from camera glue or tests |
| detect_block → downstream integrator | Only validated DetectionResult crosses boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | detect_block | mitigate | Fail closed to validated make_no_detection_result on unexpected input/errors |
| T-01-02 | Tampering | stub geometry | mitigate | Ordinary frames never return SAMPLE_SUCCESS_BLOCK_1; only SYNTHETIC_SUCCESS_FRAME sentinel |
| T-01-03 | Spoofing | MULTIPLE_CANDIDATES semantics | mitigate | Contract fix: no-geometry rejection group prevents fabricated ambiguous geometry |
| T-01-04 | Tampering | non-finite JSON values | accept | Existing _require_number guards in moved contract; no change needed |
</threat_model>

<verification>
Phase 1 gate (all must pass):
- `python3 -m unittest tests.test_detection_contract -v`
- `python3 -m unittest tests.test_pipeline -v`
- `python3 -m unittest discover -v`
- `python3 -c "from block_detected import detect_block; r=detect_block(object()); print(r.status)"` prints no_detection
- `python3 -c "from block_detected import detect_block, SYNTHETIC_SUCCESS_FRAME; r=detect_block(SYNTHETIC_SUCCESS_FRAME); print(r.status)"` prints ok
</verification>

<success_criteria>
- CONT-01: detect_block returns validated DetectionResult for ordinary and synthetic frames
- CONT-02: synthetic sentinel populates block_id, confidence, center_px, ordered corners_px, angle_deg
- CONT-03: ordinary frames return no_detection with rejection_reason and null geometry
- Root detection_contract shim and block_detected.detection_contract share DetectionResult class
- No OpenCV/TFLite in block_detected/
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-pipeline-skeleton/01-02-SUMMARY.md`
</output>
