---
status: pending
type: execute
quick_task: 260807-j2a-implement-all-pi-fixes-benchmark-supplie
autonomous: false
branch: main
files_modified:
  - pyproject.toml
  - requirements-pi.txt
  - src/block_detection_v2/benchmark.py
  - src/block_detection_v2/edges.py
  - src/block_detected/block_detected-pi-usb.json
  - tests/test_deps_layout.py
  - tests/test_block_detection_v2_benchmark.py
  - tests/test_block_detection_v2_edges.py
  - models/pose11.onnx
  - models/pose11-fp16.onnx
  - models/pose11-int8.onnx
must_haves:
  truths:
    - "Pi install declares onnx before runtime model inspection; desktop dependency sets and defaults are otherwise unchanged."
    - "block_detection_v2 finds all 108 root block_dataset images and accepts HoughLinesP arrays shaped (N,1,4) or (N,4)."
    - "Every available ONNX candidate is inspected and benchmarked; rbs-final_int8.onnx remains disqualified unless it produces detections."
    - "Pi USB config names an existing loadable model and uses the fastest measured Pi-only setting that stays inside the explicit quality gate."
    - "User model changes are preserved exactly: pose11.onnx deleted; pose11-fp16.onnx and pose11-int8.onnx added through Git LFS."
    - "Completed work is committed and pushed to origin/main before the matching commit is pulled with --ff-only on son@192.168.1.241; final tests and benchmarks run only on that pulled commit."
  artifacts:
    - path: requirements-pi.txt
      provides: explicit Pi ONNX inspection dependency
      contains: "onnx"
    - path: src/block_detection_v2/benchmark.py
      provides: root block_dataset resolution
      contains: "block_dataset"
    - path: src/block_detection_v2/edges.py
      provides: OpenCV 4/5 Hough output normalization
      contains: "reshape(-1, 4)"
    - path: tests/test_block_detection_v2_edges.py
      provides: regression coverage for both HoughLinesP layouts
    - path: src/block_detected/block_detected-pi-usb.json
      provides: benchmark-selected Pi-only model, imgsz, and max_det
  key_links:
    - from: tests/test_block_detection_v2_benchmark.py
      to: src/block_detection_v2/benchmark.py
      via: dataset_dir/list_dataset assertions against repository root
      pattern: "dataset_dir|list_dataset"
    - from: tests/test_block_detection_v2_edges.py
      to: src/block_detection_v2/edges.py
      via: monkeypatched cv2.HoughLinesP arrays in both supported shapes
      pattern: "HoughLinesP|detect_edges"
    - from: src/block_detected/block_detected-pi-usb.json
      to: models/
      via: inference.last_model_name selected from measured, valid candidates
      pattern: "last_model_name|imgsz|max_det"
---

<objective>
Apply only already-identified Raspberry Pi fixes, choose a Pi USB inference configuration from quality-aware measurements of supplied models, then ship to `main`, deploy safely to the known Pi, and validate the deployed commit end to end.

Purpose: remove known Pi setup/data/OpenCV failures without changing desktop behavior, while replacing the deleted `pose11.onnx` Pi default with measured model and image-size values.
Output: focused code/tests/config/model commit on `origin/main`, same commit on Pi 5, and recorded final dataset/camera benchmark evidence.
</objective>

<context>
@.planning/STATE.md
@AGENTS.md
@pyproject.toml
@requirements-pi.txt
@src/block_detection_v2/benchmark.py
@src/block_detection_v2/edges.py
@src/block_detected/block_detected-pi-usb.json
@tests/test_deps_layout.py
@tests/test_block_detection_v2_benchmark.py
@tests/test_onnx_inference.py
@tests/test_model_loader.py

Known target: Raspberry Pi 5 8GB, Debian 13, Python 3.13.5, Logitech C270 at `/dev/video0`, measured camera rate 29.82 FPS, no throttling. Prior temporary-fix dataset baseline: 94/108 accepted (87.04%), 166.35 s, 0.65 image/s. Prior `pose11` dynamic-input Pi speeds: imgsz 640 = 7.73 FPS, 512 = 11.68, 416 = 17.19, 320 = 25.15. `models/rbs-final_int8.onnx` previously returned zero detections and is not eligible as a default without contrary reproducible evidence.

Scope guard: Pi profile/config only. Do not tune shared desktop defaults, desktop dependency groups, or desktop runtime paths. Do not edit `models/*.pt`.

Preservation guard: before edits, capture `git status --short`, `git diff -- models/`, `git lfs status`, and SHA-256 hashes for `models/pose11-fp16.onnx` and `models/pose11-int8.onnx`. Never restore deleted `models/pose11.onnx`, regenerate supplied models, or discard unrelated work. Stage by explicit path, not `git add -A`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock known Pi fixes with focused regressions</name>
  <files>pyproject.toml, requirements-pi.txt, src/block_detection_v2/benchmark.py, src/block_detection_v2/edges.py, tests/test_deps_layout.py, tests/test_block_detection_v2_benchmark.py, tests/test_block_detection_v2_edges.py</files>
  <action>
    1. Add `onnx>=1.17.0` to `requirements-pi.txt` and `[project.optional-dependencies].pi` only, keeping both Pi lists aligned. Do not add it to core/default or desktop extras. Extend `tests/test_deps_layout.py` to require `onnx` in the Pi profile and retain existing no-CUDA/no-PySide checks.
    2. Change `src/block_detection_v2/benchmark.py::dataset_dir()` to resolve repository-root `block_dataset/` from the module path. Strengthen `tests/test_block_detection_v2_benchmark.py` so dataset resolution cannot silently skip: assert the resolved directory equals `<repo>/block_dataset`, exactly 108 `dt*.jpg` files are returned in numeric order (`dt1.jpg` through `dt108.jpg`), and remove the incomplete-dataset skip from this repository regression.
    3. Normalize non-`None` `cv2.HoughLinesP` output once with `raw.reshape(-1, 4)` in `src/block_detection_v2/edges.py`, then unpack each normalized row. Add `tests/test_block_detection_v2_edges.py`; disable LSD, monkeypatch Canny/morphology/Hough as needed, and independently prove identical `LineSegment` output for arrays shaped `(2,1,4)` and `(2,4)`, plus the existing empty result for `None`.
    Keep fix mechanical. Do not retune thresholds or alter desktop paths/config.
  </action>
  <verify>
    <automated>python -m pytest tests/test_deps_layout.py tests/test_block_detection_v2_benchmark.py tests/test_block_detection_v2_edges.py -q</automated>
  </verify>
  <done>Focused tests pass; dataset count is 108 without skip; both OpenCV Hough layouts produce same lines; Pi dependency declarations contain `onnx` and remain desktop-isolated.</done>
</task>

<task type="auto">
  <name>Task 2: Inspect supplied models and select measured Pi config</name>
  <files>models/pose11.onnx, models/pose11-fp16.onnx, models/pose11-int8.onnx, src/block_detected/block_detected-pi-usb.json</files>
  <action>
    Preserve supplied LFS model state unchanged. Inspect every available `models/*.onnx` candidate with `onnx.load(..., load_external_data=False)`, `onnx.checker.check_model`, metadata (`task`, names), opset, input dtype/dynamic shape, output shapes, file bytes/hash, and an ONNX Runtime CPU session. Record failures instead of auto-installing or rewriting models.

    Run a reproducible 108-image matrix through the application path (`load_detector(...).predict(...)`) for each loadable ONNX model at imgsz `640,512,416,320`, `conf=0.25`, `iou=0.45`, `max_det=100`, with five untimed warmups. Record model/imgsz, successful images, images with at least one detection, total detections, mean confidence, mean/p50/p95 inference ms, FPS, and exceptions to `/tmp/block-detected-model-benchmark.json`. Confirm the two supplied pose model hashes still match the pre-edit capture afterward.

    Selection policy:
    - Exclude any invalid/unloadable model and any model with zero detections (therefore keep `rbs-final_int8.onnx` excluded unless this run disproves the known result).
    - Rank model quality before speed: eligible candidates must retain at least 99% of the best candidate's 640 detection-image coverage and at least 95% of its total detection count.
    - For the winning model, choose the fastest imgsz that loses no more than one detection-image percentage point and retains at least 95% of that model's 640 total detections. Use prior Pi FPS only as speed evidence; use current 108-image results as quality evidence.
    - Set Pi-only `max_det` to `max(8, ceil(1.25 * observed p99 detections/image))`, capped at 100.

    Update only `src/block_detected/block_detected-pi-usb.json` fields `inference.last_model_name`, `imgsz`, and `max_det` to the selected existing file/values. Leave desktop `src/block_detected/block_detected.json` and shared defaults untouched. Run the full `block_detection_v2` benchmark without overlays and require at least the prior 94/108 acceptance; save timing/acceptance evidence in `/tmp`, not tracked output directories.
  </action>
  <verify>
    <automated>python -m pytest tests/test_block_detection_v2_benchmark.py tests/test_onnx_inference.py tests/test_model_loader.py -q &amp;&amp; python -c "from pathlib import Path; from block_detection_v2.benchmark import run_benchmark; s=run_benchmark(write_overlays=False, out_dir=Path('/tmp/block-detection-v2-final')); assert s['total'] == 108 and s['accepted'] &gt;= 94, s"</automated>
  </verify>
  <done>JSON matrix covers every ONNX model and four image sizes; invalid/zero-detection candidates are visibly excluded; selected Pi config points to a present loadable model and passes stated quality gates; classical benchmark is at least 94/108; supplied model hashes and requested add/delete state are unchanged.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Commit, push, safe-pull Pi, then run deployed E2E gates</name>
  <files>all explicit files from Tasks 1-2 plus this plan; Pi checkout at son@192.168.1.241</files>
  <action>
    Execute in this order:

    1. Before commit, inspect `git status --short`, staged/unstaged diffs, `git diff --check`, `git lfs status`, and `git lfs fsck`. Confirm no unrelated path is staged and model state is exactly `pose11.onnx` deleted plus `pose11-fp16.onnx`/`pose11-int8.onnx` added. Stage only listed implementation/test/config/plan paths and those three model paths. Commit on `main` with repository-style message; capture commit SHA.
    2. Push normally with `git push origin main` (never force), then verify `git rev-parse HEAD` equals `git rev-parse origin/main` and GitHub/LFS upload completed.
    3. SSH to `son@192.168.1.241`. Locate the checkout whose `origin` URL matches `https://github.com/Minhmice/block_detected.git`; print its path, branch, HEAD, `git status --short`, and `git lfs status`. If dirty, wrong branch, or divergent, stop: preserve a patch/untracked-file inventory and reconcile with the user; do not reset, checkout, clean, stash, or overwrite Pi files blindly. Once clean and confirmed on `main`, run `git pull --ff-only origin main`, `git lfs pull`, and assert Pi `HEAD` equals pushed SHA.
    4. Only after that pull, install the Pi profile (`python main.py --install-pi` or equivalent active-venv commands), verify `python -c "import onnx, onnxruntime; print(onnx.__version__, onnxruntime.__version__)"`, and run focused plus full automated tests on Pi.
    5. Re-run the Task 2 model/imgsz matrix on Pi and record final JSON. Require selected config to satisfy the same quality gates and report mean/p50/p95 latency plus FPS. Then exercise `/dev/video0` through the real `WebcamEngine`/Pi USB config for at least 300 consecutive processed frames: zero camera/inference exceptions, nonzero detections on a visible target, measured capture FPS, end-to-end FPS, p95 inference latency, CPU temperature/throttling before and after, and peak RSS. Verify camera rate remains near its 29.82 FPS baseline and selected end-to-end performance is consistent with the model benchmark.
    6. If any final Pi gate fails, do not edit only on Pi. Return to Tasks 1-2 locally, make a new explicit commit, push, repeat clean `--ff-only` pull, then rerun all final tests. Add no post-test production edits outside this loop.
  </action>
  <verify>
    <automated>On pulled Pi SHA: python -m pytest tests/test_deps_layout.py tests/test_block_detection_v2_benchmark.py tests/test_block_detection_v2_edges.py tests/test_onnx_inference.py tests/test_model_loader.py -q &amp;&amp; python -m pytest -q</automated>
    <manual>Confirm final Pi report names pushed SHA, selected model/imgsz/max_det, 108-image quality counts, benchmark FPS/latencies, 300-frame camera result, temperature/throttling, and peak RSS.</manual>
  </verify>
  <done>`origin/main` and clean Pi checkout share exact SHA; LFS objects are present; all Pi tests pass; dataset remains at least 94/108; chosen model passes explicit quality gate and is nonzero; 300-frame USB-camera E2E run has no errors and reports final measurable performance/health evidence.</done>
</task>

</tasks>

<verification>
- [ ] Only Pi dependency/profile/config receives optimization changes; desktop defaults remain byte-for-byte unchanged.
- [ ] Dataset regression resolves root `block_dataset/` and sees ordered `dt1.jpg`-`dt108.jpg` without skip.
- [ ] Hough regressions pass for `(N,1,4)`, `(N,4)`, and `None`.
- [ ] Supplied pose models remain unmodified by hash and are committed through Git LFS; deleted FP32 model stays deleted.
- [ ] Model matrix records validation, quality, and latency for all ONNX candidates and all four image sizes; zero-detection model cannot become default.
- [ ] Push precedes Pi pull; Pi pull precedes final tests/benchmarks; no dirty Pi checkout is overwritten.
- [ ] Final Pi SHA equals `origin/main`, full pytest passes, dataset acceptance is at least 94/108, and 300-frame `/dev/video0` test completes without errors.
</verification>

<success_criteria>
1. Known Pi failures (missing `onnx`, wrong dataset path, OpenCV 5 Hough shape) are fixed with focused tests.
2. Pi USB config uses an existing benchmark-selected model and quality-preserving image size/max-det; desktop behavior is untouched.
3. User-supplied model add/delete changes are preserved exactly and LFS-backed.
4. One normal commit sequence reaches `origin/main`, then reaches Pi via safe fast-forward pull.
5. Tests and final model/camera benchmarks pass on the deployed Pi commit with stated measurable evidence.
</success_criteria>

<output>
After execution, create `.planning/quick/260807-j2a-implement-all-pi-fixes-benchmark-supplie/260807-j2a-SUMMARY.md` containing commit SHA, pushed/pulled proof, model matrix path/summary, selected config rationale, test results, and final Pi benchmark metrics.
</output>
