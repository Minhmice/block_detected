# Raspberry Pi — TUI + stream_server (no PySide6 GUI)
# Install: pip install -r requirements-pi.txt && pip install -e . --no-deps
# Python 3.10+

ultralytics>=8.4.0
opencv-python-headless>=4.8.0
textual>=8.0
rich>=13.7
